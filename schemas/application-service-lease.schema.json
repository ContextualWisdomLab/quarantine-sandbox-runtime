{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contextualwisdomlab.org/schemas/quarantine/application-service-lease-1.2.0.schema.json",
  "title": "Isolated Application Service Lease",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "request_id",
    "image_reference",
    "backend_id",
    "backend_version",
    "sandbox_id",
    "network_id",
    "policy_id",
    "policy_sha256",
    "endpoint",
    "started_at_epoch_seconds",
    "expires_at_epoch_seconds",
    "shutdown_grace_seconds",
    "isolation_attestation"
  ],
  "properties": {
    "schema_version": { "const": "1.2.0" },
    "request_id": { "type": "string", "minLength": 1, "maxLength": 128 },
    "image_reference": {
      "type": "string",
      "pattern": "^[^\\s\\u0000-\\u001F\\u007F-\\u009F]+@sha256:[0-9a-f]{64}$",
      "maxLength": 512
    },
    "backend_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]{1,63}$"
    },
    "backend_version": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[^\\u0000-\\u001F\\u007F-\\u009F]+$"
    },
    "sandbox_id": {
      "type": "string",
      "pattern": "^[a-z0-9-]{1,64}$"
    },
    "network_id": {
      "type": "string",
      "pattern": "^[a-z0-9-]{1,64}$"
    },
    "policy_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "policy_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "endpoint": {
      "type": "object",
      "additionalProperties": false,
      "required": ["host", "port", "protocol"],
      "properties": {
        "host": { "const": "127.0.0.1" },
        "port": { "type": "integer", "minimum": 1, "maximum": 65535 },
        "protocol": { "enum": ["tcp", "http"] }
      }
    },
    "started_at_epoch_seconds": { "type": "integer", "minimum": 0 },
    "expires_at_epoch_seconds": { "type": "integer", "minimum": 1 },
    "shutdown_grace_seconds": { "type": "integer", "minimum": 1 },
    "isolation_attestation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "rootless",
        "read_only_root_filesystem",
        "all_capabilities_dropped",
        "no_new_privileges",
        "isolated_user_namespace",
        "external_egress_denied",
        "loopback_only_publication",
        "seccomp_enforced",
        "lsm_enforced",
        "resource_limits_verified",
        "credentials_available"
      ],
      "properties": {
        "rootless": { "$ref": "#/$defs/control_status" },
        "read_only_root_filesystem": { "$ref": "#/$defs/control_status" },
        "all_capabilities_dropped": { "$ref": "#/$defs/control_status" },
        "no_new_privileges": { "$ref": "#/$defs/control_status" },
        "isolated_user_namespace": { "$ref": "#/$defs/control_status" },
        "external_egress_denied": { "$ref": "#/$defs/control_status" },
        "loopback_only_publication": { "$ref": "#/$defs/control_status" },
        "seccomp_enforced": { "$ref": "#/$defs/control_status" },
        "lsm_enforced": { "$ref": "#/$defs/control_status" },
        "resource_limits_verified": { "$ref": "#/$defs/control_status" },
        "credentials_available": { "const": false }
      }
    }
  },
  "$defs": {
    "control_status": {
      "enum": ["verified", "not_applicable", "unavailable"]
    }
  }
}
