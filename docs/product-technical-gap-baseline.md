# Product and Technical Gap Baseline

Last reviewed on 2026-09-23 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea` and the live owner PR/Issue state below. This file is the repository-wide single-writer current baseline. Historical causal detail remains in [`product-technical-gap-history-2026-09-16.md`](product-technical-gap-history-2026-09-16.md) and owner-local TRACEABILITY. Predecessor checks never transfer to a moved head, mutable PR heads are not consumer authority, and GitHub `mergeable=true` is not release readiness.

## Integration root and release authority

Integration root #1 remains exact `a85dc86c00f00354d9ddb9bf7c291c2c1cd40884`, Draft/open, targeting protected `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`. Historical hosted verify/coverage/negative-runtime evidence on that root remains diagnostic; positive SELinux did not execute. Central dependency/security/governance blockers and organization ruleset approval/bypass convergence remain outside this repository's local authority.

Release owner #10 remains exact `bb587667e049c900f0dec578f083b35ed50f21c7`, Draft/RED. Generic `.github#1552` classifies `default_branch=develop` as Git Flow, so protected `develop` is development integration rather than production release authority. Issue #140 owns the missing protected production-branch/immutable-publication contract. Fresh Release inventory remains empty. No package, tag, GitHub Release, SBOM, provenance, reproducibility, rollback, or consumer bump is authorized until one unchanged dependency-safe protected production candidate satisfies every gate.

## Current owner authority

### Command runtime and repository fitness

Focused #89 remains historical succession evidence for the Podman missing-isolation-evidence defect. Canonical owner-adapted succession is Draft #143 on repository-fitness #112 ancestry.

Causal RED `0bf65d831f2130dbf414fef96cfbfa2828aabfdd / 35685623657` proved that missing `EffectiveCaps`, missing `BoundingCaps`, missing `dns_enabled`, and explicit null capability arrays crossed the parser/ACL boundary instead of failing closed, while explicit empty capability arrays remained valid configured evidence. Production repair `f65d0982c756b67c85b3e60f3649e3ca201d50ca` makes both capability arrays and `dns_enabled` required concrete evidence and removes generic null-to-empty normalization.

Repaired exact `fba1118776f1fbd3595ad222022ee9b0ae552201 / 35740227414` received hosted execution. All six focused missing/null parser controls and hosted rootless/AppArmor negative acceptance were GREEN. Remaining failures were repository-fitness work rather than parser regression. Repository-fitness successor `33eb250aa52eccfa03a413163ed0aadee6934206 / 35766950064` then executed repository policy, CI-evidence contracts, rustfmt and hosted negative acceptance GREEN, but a newly added coordinator coverage test itself failed to compile because it called crate-private `CleanupReceipt::complete` and shadowed the `owner(...)` helper. Current test-only #143 exact `9e214ee9afb526fa067f43d0b1f99c6819c80f1b`, native CI `35791713591`, repairs only those test-construction defects. Production parser semantics and domain visibility are unchanged; the current exact still needs its own repository/full-test/coverage/runtime evidence.

Command/runtime owner #14 remains exact `73a191c65cfe1dd16447921acc8f96b306583439`. Repository-fitness #112 remains predecessor coverage authority on `19c450249e1779cd1d22c4837c8154fed91e1faf / 35543654304`; its physical-source coverage admission, configured/live capability-source separation, and cleanup-owner separation remain valid. After #143 becomes dependency-safe, integrate/adopt the complete parser repair through ordinary non-force ancestry and reacquire exact-head coverage/runtime evidence rather than copying #89 source.

Process-lifecycle #125 remains dependent on stable #143/#112 parser authority. Lease-ownership #6 retains its own exact verify/hosted-negative evidence but is not release GREEN because coverage admissions and positive-LSM evidence remain incomplete.

### Application-service and network lifecycle

Application-service runtime/lifecycle owner #21 remains exact `717edef989d8b6bb7e1f672d91d9258f7942a6b2`. Its historical hosted verify/production coverage/branch coverage/negative-runtime evidence remains valid predecessor evidence; positive SELinux did not execute. #21 retains crate-private, non-serializable cleanup authority, so public/deserialized lease evidence cannot reconstruct destructive selectors.

Canonical network/lifecycle owner #127 has execution-backed proof of the creation-provenance defect and now carries the bounded creation-receipt production candidate. Earlier executed exact `67095685df0e03530f78ad11e2e87ee427692c27 / 35721035315` established acquired-ID behavior, all three effective-attachment hostile cases, hosted negative runtime acceptance, and exact-ID/non-force partial-launch cleanup GREEN. Those semantics remain predecessor evidence only.

Hardened creation-receipt exact `3a94ef6cc158bd12a47c5fb7701e2158d7276341 / 35778989881` received hosted execution and reproduced the intended causal RED in `first_name_inspect_cannot_promote_a_same_name_replacement_network`: predecessor production executed `network create` and then learned private network identity from `network inspect --format json <generated-name>`. A same-name replacement could therefore become attachment/destructive authority before container creation. Hosted rootless/AppArmor negative acceptance was GREEN; verify independently stopped at deterministic rustfmt changes in the receipt witness, later applied without production change.

Review `5285160963` found a separate false-GREEN hazard in `podman_application_service_network_identity_provenance_owner_red`: that test still treated the obsolete public-name inspect path as expected behavior. Test-only `2637ffd98ee69649b082a171e20bff857b7fc6f0` preserves the invariant through creation receipt ID → exact-ID P0 inspection. It requires the exact admitted ID to resolve to the expected generated network with `internal=true` and DNS disabled before container creation and explicitly forbids public-name identity inspection.

Review `5285402062` then found a positive-fixture contradiction in the dedicated creation-receipt witness: exact-ID P0 inspection returned synthetic name `created-object` even though the sibling provenance/P0 contract requires the admitted ID to identify the invocation's generated `qsr-net-*` object. Test-only `709d574107f6cb0f024a5780e6e89920292f37a9` repairs only that fixture, preserving bounded event-query grammar, launch-local clock bounds, same-name replacement, ambiguity, exact-ID attachment, and cleanup assertions.

Production exact `0e44b88b7ecf72a5c641b050a7d8450d604da4e4` introduced the minimum causal repair inside the existing Podman CLI Anti-Corruption Layer. It captures invocation-local `SystemTime` bounds around successful non-`--ignore` network creation; performs exactly one non-streaming JSON `podman events` history query for that interval; admits private authority only from exactly one matching generated name plus canonical lower-case 64-hex ID; fails closed on malformed/missing/ambiguous history without public-name identity or deletion fallback; and then inspects P0 state by that exact ID before binding/cleanup.

Review `5285457161` found one execution-fidelity defect in that first production candidate: Podman/pflag's boolean `--stream` false value must be expressed as the explicit assignment `--stream=false`; a detached `--stream`, `false` pair can select the no-value boolean form and leave `false` as a positional argument. Production successor `421b8913cd4f74a0728a138f856409de1700fcb6` changes only that argv spelling. The current query is therefore `podman events --stream=false --format json --since <lower> --until <upper> --filter type=network --filter event=create`; exact event/ID/P0 semantics are unchanged.

Docs-only `dd96c06fdac4ebf4fb42fe363f9bf575e0d48f59` made `PODMAN_NETWORK_IDENTITY_PROVENANCE_TRACEABILITY.md` code-current with the executed causal RED, receipt fixture, production candidate, canonical `--stream=false`, DDD/ACL boundary and rejected alternatives.

Review `5285757824` then found that `tests/podman_missing_isolation_evidence_owner_red.rs` still modelled the pre-receipt valid path. Current test-only #127 exact `c99cb097e4657920af5f433bf67667b28affe33a / 35804875966` migrates only that fake backend: successful network creation records the generated correlation name, the bounded event query emits one matching creation receipt for `OWNED_NETWORK_ID`, exact-ID P0 inspection returns that name and ID, and otherwise-valid container evidence has exactly one matching `NetworkSettings.Networks[*].NetworkID`. Its later network-inspection fixture remains independently mutable, preserving the intended missing post-start `dns_enabled` witness. Production Rust/API/schema are unchanged. Current CI remains queued, so the production candidate and predecessor GREEN do not transfer to this exact.

Podman primary documentation makes an additional availability boundary explicit. `events_logger=none` disables events entirely, and containers/common permits file-backed event history with a maximum size that rotates the log and deletes the old file. Therefore the creation-event receipt is an admissible launch-time identity source, but not a durable recovery journal: successful `network create` can be followed by missing bounded history before any immutable ID is admitted. Review `5286101359` records this finding on #127. Issue #141 now owns disabled/rotated/unavailable event-history recovery and must not recover by public name, prefix, label, age, dangling state, or by depending solely on the same event history whose loss triggered recovery.

Successful-lease authority remains downstream. Public `qsr-net-*` is correlation only. Current successful-lease metadata still retains public network correlation and explicit termination still uses network-level `--force`; final lifecycle authority must retain the creation-bound/admitted ID privately through lease issuance, recovery and foreign-safe non-force termination. Dependent #142 `3aa1425be987851a3c151402bd3efe9edf244323` owns post-admission continuity and must later prove that effective-isolation verification continues to consume the same immutable ID rather than re-resolving the public name. Issue #141 owns durable recovery when no admitted ID exists. #133 `4191ac0051093c30150734f6b4e98c05f36a0ef4` preserves the one-literal Podman `--` image-operand repair and must remain stale until #127 is dependency-safe.

Issue #128 remains downstream of the network authority chain. A successful launch must hold the exact acquired container behind a trusted runtime-owned gate until effective isolation is positively attested, then perform exactly one acknowledged release transition before readiness; cleanup after rejected attestation is not proof that hostile payload execution never occurred.

### Lease evidence and applied-image execution integrity

Issue #84 / Draft #85 retains the focused invariant-preserving lease wire admission delta. Current application-service ancestry does not yet contain that complete repair. After #127 becomes dependency-safe, ordinary/non-force adopt/adapt #85 while preserving crate-private cleanup authority and exact-ID lifecycle semantics. Valid deserialized lease evidence remains evidence-only and cannot reconstruct cleanup capability.

Applied-image execution-integrity issue #46 remains separate P0 authority. Current application-service #127 still does not positively observe and compare the exact acquired container's applied canonical SHA-256 digest before port/readiness/lease. After #143 integration, #127 dependency safety, and lease-evidence succession, execute exact/mismatch/missing/null/malformed/noncanonical/different-algorithm/name-substitution hostile cases and require canonical applied digest equality on the exact acquired container.

### Artifact-analysis owner stack

Artifact-analysis parent #18 remains exact `703b4b1a047321bb08d1d6cb1de78d01cb350696`. Focused leaves must not copy command/runtime or application-service source; owner ancestry is adopted ordinary/non-force only after prerequisites become dependency-safe.

#53 dynamic-attestation remains a focused inverse-evidence lane. #61 evidence identity, #63 policy binding, #67 descriptor/self-verifiability, #70 Analyzer Worker, #76 strict evidence deserialization, and #51/#55/#59 prerequisites remain independent active obligations.

#### EvidenceBundle public-schema cardinality

Rust foundation cardinality #65 remains focused GREEN. Public-schema parity #130 has now progressed beyond its original test-only RED. Executed predecessor `ab40d33787c8eb1e01546d51fd3f554d461b59de / 35521761067` passed ten structural hostile/positive controls while both real-schema cardinality assertions failed for the intended checked-in contract defect: `/properties/evidence` lacked the authorized direct `allOf` occurrence surface. Verify separately exposed only deterministic rustfmt deltas in the owner tests. Self-hosted positive SELinux did not execute.

After those formatting-only repairs, current #130 exact `a89330f2e496e2d758132686638ccbb771d9a5e1 / 35796262593` applies the minimum public-schema candidate: exactly three unconditional occurrence constraints for `artifact_identity`, `file_format`, and `policy_boundary`, each narrowed by required `evidence_kind` plus `minContains: 1` and `maxContains: 1`. Optional evidence kinds remain repeatable. The witness still pins the complete non-cardinality contract with normalized SHA-256 `e92a6562641ed7f4fc21f1d247cb97f78709a6dd8aaa34e8820c6343c1b90206` after removing only the authorized `allOf` delta. Current exact requires independent full validation before merge or parent adoption.

#### Shared RFC 3339 vocabulary publication

#102 shared-vocabulary owner and #135 Gregorian owner retain their original causal publication evidence. The later hardened generic/six-edge succession has also executed far enough to expose a prerequisite defect rather than the intended vector absence.

Exact `12d618b4b8e1b8057e3e16a1ade53665ebd72849 / 35784946982` received hosted execution. The repaired generic `required_vocabulary_declares_no_undefined_custom_keywords` witness was GREEN and hosted rootless/AppArmor negative acceptance was GREEN, resolving the earlier Markdown-specific `**MUST` false RED. Coverage then failed before the dedicated six-edge Gregorian publication witness in the existing serialized-byte boundary test: its positive `original_file_name` fixture serialized to 1076 raw bytes although the contract requires 1004 raw and 1024 after nullable-field materialization.

RCA showed the fixture contained 262 quote characters plus trailing `a`; with the fixed sibling fields the intended 1004→1024 boundary requires 226 quotes plus `a` (227 characters total). The excess 36 quotes add exactly 72 escaped bytes and also exceed the request schema's 255-character filename bound. Current fixture-only #102 exact `98effc65769476399b0a638cc2b1dcfc82877f5e / 35796918500` restores only that positive boundary; the existing overflow vector remains one serialized byte over the limit. Vocabulary prose, schema, dialect, runtime validator and all six Gregorian publication obligations are otherwise unchanged.

Do not add the six same-version publication vectors until `98effc...` proves the serialized-byte prerequisite GREEN and execution reaches the dedicated Gregorian witness for the intended absent-vector cause. The required independent branches remain: reject `1900-02-29`, accept `2000-02-29`, reject April 31, reject lower-case `z`, accept fractional seconds, and reject lower-case `t`. Only then add exactly those vectors plus explicit upper-case `T` normative wording, reacquire generic vocabulary completeness and complete #135 succession, and publish immutable authority before #139 adoption.

Issue #136/#137 remains the self-hosted network-boundary lane. Acceptance requires ops-owned actual host-side/bridge identity and observed pre-lease denial; guest loopback, arbitrary private endpoints, and closed-port proxies are not host-boundary proof.

## DDD and ownership boundary

`sandbox_execution` is Core. `application_service` and `artifact_analysis` are Supporting bounded contexts. Podman/gVisor/containerd/Kubernetes/VM are infrastructure adapters behind ACLs. Generated names and serialized receipts are correlation/evidence unless a reviewed versioned contract grants stronger semantics. Requested configuration, backend-applied inspection, runtime-acquired backend identity and live effective enforcement are separate evidence levels. Destructive cleanup authority must be runtime-acquired, exact, private and non-serializable.

Core foundation repositories are optional released contract owners, not mandatory source dependencies. Consumers use immutable released contracts/ACLs; mutable branch dependencies, sibling source copies and cross-service SQL are prohibited. Aggregate transactions stay short; external/runtime I/O never runs while holding a long DB transaction or explicit lock.

## Buyer-visible gaps

| Gap | Current authority | Required next evidence |
| --- | --- | --- |
| Git Flow production-release authority and immutable publication are unresolved. | #140; #10 `bb587667...`; `.github#1552`; protected `develop`; empty Release inventory. | Establish released/versioned production-branch authority, integrate one protected production head normally, verify immutable-release administration, then publish exact source/artifact/package/SBOM/provenance/reproducibility/rollback evidence. |
| Missing/null Podman isolation evidence has an execution-backed parser GREEN, but current repository-fitness successor is not exact-head GREEN. | #143 causal RED `0bf65d83...`; parser GREEN `fba11187... / 35740227414`; executed test-construction failure `33eb250a... / 35766950064`; current test-only `9e214ee9... / 35791713591`. | Execute current #143 unchanged; reacquire repository/fmt/full tests/Clippy/rustdoc/100% owned-production coverage and applicable runtime evidence before complete succession. |
| Command/runtime physical-source coverage and positive effective-LSM remain release blockers. | #112 `19c450249...`; #143 current succession; positive SELinux unexecuted on relevant candidates. | After #143 stabilizes, ordinary/non-force adopt into command/runtime ancestry and repair only real deterministic coverage deficits without lowering thresholds/excluding hard paths. |
| Creation-bound application-service network identity has an execution-proven RED and an unverified production candidate. | #127 causal `3a94ef6c... / 35778989881`; production `421b891...`; current fixture successor `c99cb097... / 35804875966` queued. | Execute current #127 unchanged; require receipt/P0/effective-attachment prerequisites and the missing post-start DNS witness to classify causally, then repair only the proven cause. Never add public-name fallback. |
| Post-admission P0 continuity is stale behind moved parent authority. | #142 `3aa1425...`, dependent on #127. | After #127 becomes dependency-safe, ordinary/non-force adopt/adapt and prove all later P0/effective-isolation evidence uses the same admitted immutable ID. |
| No-admitted-ID recovery can leak orphan network objects, including when event history is disabled or rotated away after successful create. | Issue #141; #127 review `5286101359`; current #127 intentionally avoids generated-name destructive fallback. | Persist private recovery intent before create; cover `events_logger=none`/empty history and file-history rotation/loss; later reacquire and corroborate a canonical full ID, then remove only that ID non-force. Recovery must not depend solely on the same lost event history. |
| Successful-lease network cleanup does not yet preserve final private exact authority. | #127 downstream lifecycle; current lease metadata still carries public correlation and explicit termination remains forceful. | Retain admitted ID privately through lease/recovery/termination, remove only exact ID without network-level force, and fail closed on foreign membership. |
| Application-service evidence wire admission is absent from current canonical ancestry. | #84/#85 focused evidence; current #127 lacks complete repair. | After #127 dependency safety, adopt/adapt complete #85 delta while preserving non-serializable cleanup authority and execute hostile wire/forged-cleanup controls on one exact head. |
| Applied-image identity is not proven on the exact acquired application container before lease publication. | #46; current #127. | Execute exact/mismatch/missing/null/malformed/noncanonical/different-algorithm/substitution controls, then require canonical SHA-256 equality before port/readiness/lease. |
| Pre-attestation hostile payload execution can occur before effective isolation is accepted. | #128. | Introduce a trusted hold → attest exact container/network → one acknowledged release transition → readiness sequence; prove rejected attestation produces no hostile payload side effect. |
| EvidenceBundle foundation cardinality was execution-proven absent; current schema repair is not exact-head GREEN. | #130 RED `ab40d337... / 35521761067`; current `a89330f2... / 35796262593`. | Execute current schema repair unchanged; require both real-schema assertions, all hostile false-GREEN controls, pinned non-cardinality digest, full tests/coverage and applicable LSM gates. |
| Shared RFC3339 publication is blocked by a corrected serialized-byte prerequisite before the intended six-edge vector RED. | #102 executed `12d618b... / 35784946982`; current fixture-only `98effc657... / 35796918500`; inherited #135; #139 consumer. | Prove byte-boundary prerequisite GREEN, reach the six-edge absent-vector RED, then add exactly the six vectors + upper-case `T` wording and publish immutable vocabulary authority before consumer adoption. |
| Dynamic execution semantics, evidence identity and descriptor self-verifiability remain focused artifact-analysis gaps. | #53, #61, #67 and related #63/#70/#76 prerequisites. | Execute hardened exact REDs, apply only matching owner-local causal repairs, and preserve command/runtime ancestry through #18 rather than source-copying. |
| Self-hosted LAN/host-service denial can false-GREEN against the wrong endpoint. | #137; `linux-cluster-ops#326`. | Require ops-owned actual host-side/bridge identity and observed pre-lease denial. |
| Positive effective-LSM evidence is absent on release candidates. | Hosted negative evidence exists; self-hosted positive lanes repeatedly have no eligible runner. | Obtain exact-candidate positive selected-LSM execution; never substitute negative fail-closed evidence. |
| Protected integration governance still exposes approval/bypass gaps. | Organization ruleset and central governance owners. | Prove an ordinary protected production path without administrator bypass, self-approval, direct push or gate weakening. |
| No immutable consumer authority exists. | Release inventory empty; production branch governance unresolved. | Satisfy owner/security/runtime/governance gates on one protected production candidate, publish immutable source/artifact authority, then bump consumers only to that release. |

## Current bounded action order

1. Execute current #143 `9e214ee9afb526fa067f43d0b1f99c6819c80f1b / 35791713591` unchanged. Preserve the already execution-backed parser semantics; classify only current repository/full-test/coverage/runtime results.
2. Execute current #127 `c99cb097e4657920af5f433bf67667b28affe33a / 35804875966` unchanged. Require the migrated fixture to pass creation-receipt/P0/effective-binding prerequisites and classify the missing post-start `dns_enabled` witness causally; do not weaken the creation-receipt candidate or add public-name fallback.
3. Once #127 is dependency-safe, ordinary/non-force adopt/adapt #142 for post-admission continuity; then progress #141 durable no-admitted-ID recovery with disabled/rotated event-history cases, successful-lease private exact cleanup authority, foreign-safe non-force termination, #128 hold/attest/release, #85 lease admission, #46 applied-image evidence and #133 delimiter succession as dependencies allow.
4. Execute #102 `98effc65769476399b0a638cc2b1dcfc82877f5e / 35796918500`. Only after the corrected serialized-byte prerequisite is GREEN and the dedicated Gregorian witness fails specifically for the six absent vectors may the same-version vector/prose repair be added.
5. Execute #130 `a89330f2e496e2d758132686638ccbb771d9a5e1 / 35796262593`. Only exact-head GREEN may promote the schema-cardinality repair into its prerequisite stack; do not bypass the pinned non-cardinality digest or positive-LSM gate.
6. Stabilize #143/#112 before ordinary/non-force command/runtime adoption into #14, #6, #125 and artifact parent #18. Do not source-copy parser or coverage repairs.
7. Execute #53, #61, #67 and remaining artifact-analysis leaves only on dependency-safe parent ancestry, preserving each focused invariant and TRACEABILITY.
8. Execute #137 only with real ops-owned host-boundary identity/evidence.
9. Keep #10 Draft/RED until #140 and released central Git Flow governance establish production-branch authority; independently verify immutable-release administration before publication.
10. Keep root #1 Draft until positive effective-LSM, central dependency/security terminal evidence, ordinary protected production integration, governance convergence and immutable release evidence all exist.
11. Merge/release only from one unchanged dependency-safe protected production candidate satisfying every gate and one immutable source/artifact release receipt.