# Product and Technical Gap Baseline

Last reviewed on 2026-09-21 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea` and the live owner PR/Issue state below. This is the repository-wide single-writer current baseline. Predecessor checks never transfer to a moved head, mutable PR heads are not consumer authority, and GitHub `mergeable=true` is not release readiness.

The historical causal ledger remains in [`product-technical-gap-history-2026-09-16.md`](product-technical-gap-history-2026-09-16.md). Historical present-tense statements there are evidence, not current authority.

## Integration root and release authority

Canonical integration root #1 remains exact `a85dc86c00f00354d9ddb9bf7c291c2c1cd40884`, Draft/open, targeting protected `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`. Its historical native CI has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor evidence, but positive SELinux did not execute. Central dependency/security/governance blockers and organization ruleset bypass/approval convergence remain outside this repository's local authority. This repository must not self-approve, exercise administrator bypass, direct-push, fabricate review, weaken deterministic gates, or equate branch protection metadata with release readiness.

Release-evidence owner #10 remains exact `bb587667e049c900f0dec578f083b35ed50f21c7`, CI `35485675824`, Draft/RED. Generic `.github#1552` classifies `default_branch=develop` as Git Flow, so protected `develop` is development integration rather than production release authority. Product issue #140 owns the missing protected production-branch/immutable-publication contract. GitHub Release inventory is still empty. No package, tag, GitHub Release, SBOM, provenance, reproducibility, rollback, or consumer bump is authorized until one unchanged dependency-safe protected production candidate satisfies every gate.

## Current owner authority

### Command runtime and repository fitness

Lease-ownership owner #6 is formatting-only exact `b096f4bf1d4ae1a663538127bf1b1048734c0079`, CI `35541704662`. Its executed predecessor proved the owner-local Launching/Terminating/LeaseMismatch behavior before stopping at rustfmt and an inherited command/runtime fixture issue. #6 must not patch command/runtime foundation locally.

Focused P0 #89 is the authoritative missing-Podman-isolation-evidence repair. Production `80e6fd19ab5ef11eab3d18b3e80b43f880ce6058` removes secure serde defaults only from `ContainerInspection.EffectiveCaps`, `ContainerInspection.BoundingCaps`, and `NetworkInspection.dns_enabled`; absent evidence must fail through the malformed-inspection ACL while explicit `[]`, `[]`, and `false` remain accepted observations. Predecessor `1a17944d4f591511d723354b4f213d065a599c80` / CI `35169293917` executed repository/fmt/full locked tests, the focused missing-evidence controls, and hosted negative rootless/AppArmor GREEN before Clippy found two test-fixture initializers. Current test-only exact `8afa77c61af38121cbfcdc1051e6c45376e13a1b`, CI `35546707858`, repairs only those initializers and awaits exact-head execution. #14/#112 must adopt the complete valid production/test/TRACEABILITY delta only through ordinary/non-force ancestry after #89 is dependency-safe.

Command/runtime owner #14 remains exact `73a191c65cfe1dd16447921acc8f96b306583439`. Repository-fitness descendant #112 remains current test-only exact `19c450249e1779cd1d22c4837c8154fed91e1faf`, CI `35543654304`, Draft/open/mergeable. Its physical-source coverage admission remains canonical. Review `5261966999` removed unrelated `network rm --force` assertions from effective-security coverage, leaving network cleanup command shape to #127/#141. Review `5262030576` found correlated capability evidence; current #112 independently injects configured `EffectiveCaps`/`BoundingCaps` and live `CAPEFF`/`CAPBND`/`CAPINH`/`CAPPRM`/`CAPAMB`, each requiring `IsolationVerificationFailed { control_name: "all_capabilities_dropped" }` when hostile. Production Rust/API/schema/network semantics did not change. #89 remains an explicit prerequisite; #112 cannot become a complete successor merely by reaching 100% coverage on ancestry that still normalizes absent isolation evidence.

Independent process-lifecycle owner #125 remains exact `5bced55c107aff2a2cabb84ebc6d980134e7ed07`; its runtime tests are GREEN, while coverage completeness depends on #112's canonical physical-source parser. After #112 stabilizes, adopt parser authority non-force and reacquire exact evidence rather than duplicating parser/test behavior.

### Application-service, network lifecycle, and execution integrity

Canonical application-service runtime/lifecycle owner #21 remains exact `717edef989d8b6bb7e1f672d91d9258f7942a6b2`. Historical exact CI has GREEN verify/production coverage/branch coverage/hosted negative rootless-AppArmor, but no positive SELinux execution.

Canonical network successor #127 is exact `ba37ca70ca1dba08c87e2bdafebbda46cdf6fa22`, CI `35534547447`, Draft/open/mergeable. Executed predecessor `d2c22a423870e5566b198587e285ecbf66ea0526` exposed missing backend network-ID fixture evidence. Test-only `4053fc1a245a2a49b619f4d26db15cd07bc98087` supplies distinct canonical backend network IDs. Review `5261603733` then separated the identity-race witness from cleanup command shape; test-only `0daa56eff16dc582332c04cc01bb05d736d9befa` requires distinct public correlations and distinct acquired backend network selectors without asserting `network rm --force <public correlation>`. Production cleanup/provenance/termination semantics remain owned by the dedicated #127 chain.

Issue #141 owns post-failure no-admitted-ID recovery. Immediate launch behavior must remain non-destructive when network creation succeeded but canonical backend-ID admission failed. Public names, prefixes, labels, age, and dangling state are candidate locators, never deletion authority. Later recovery requires durable private intent, bounded candidate discovery, canonical full-ID reacquisition, receipt/provenance/P0-state/no-foreign-attachment corroboration, and exact-ID non-force removal. Broad prune, name-only/label-only deletion, re-resolved-name deletion, network-level force removal, or long DB transaction/lock across Podman I/O remain prohibited.

Issue #128 remains downstream of #127. A successful service launch must hold the exact acquired container behind a trusted runtime-owned gate until effective isolation is positively attested, then perform exactly one acknowledged release transition bound to that exact container/request before readiness. Arbitrary entrypoint presence is not a trusted hold.

#133 exact `4191ac0051093c30150734f6b4e98c05f36a0ef4` remains 13 commits ahead / 8 behind current #127 from merge base `71063882948daf5819839d8059c37857a6255abf`. Preserve its one-literal Podman `--` image-operand repair; restack only after #127 causal classification and only ordinary/non-force.

Applied-image execution-integrity issue #46 remains P0, but current-owner review materially broadens the live failure state. Historical #19 exact `3b4d2bf99b5ff610be52bd76b995cae1151606b8` is stale/non-mergeable and remains evidence only. Command/runtime #112 exact `19c450249...` already deserializes `ContainerInspection.ImageDigest` and rejects a present mismatch, but models the field as optional/defaulted and checks `immutable_image_identity` only when present; missing/null evidence therefore fails open. More importantly, canonical application-service #127 exact `ba37ca70...` does not deserialize `ImageDigest` at all and its `verify_effective_isolation(...)` can reach resource/LSM/network/port admission without observing applied image identity. Review `5262989006` records this current-head finding. Thus a present mismatched applied digest is also unchecked on the live application-service path, not merely missing/null evidence.

Issue #46 is the execution-integrity authority. On dependency-safe current application-service ancestry its RED must bind the **exact acquired container ID** and separate exact positive, mismatched, missing, null, malformed, and different-algorithm applied-digest states. Every hostile state must fail before port/readiness/lease publication. Requested immutable reference and `--pull=never` are configuration, not applied-image evidence. Historical #19/#45/#46/#47 tests/contracts/TRACEABILITY must be preserved by ordinary/non-force adoption, but stale `network rm --force <qsr-net correlation>` assertions must be removed because cleanup syntax is #127/#141 authority. The smallest GREEN is trustworthy canonical SHA-256 applied-digest observation on the exact acquired container plus exact comparison to the already-validated requested digest; do not synthesize absent evidence from the request or substitute image name/correlation data.

Owner order for #46 is now explicit: execute #89; dependency-safely integrate its parsing repair while preserving #112; causally classify #127 network/lifecycle current exact; ordinary/non-force adopt the complete valid #19/#45/#46/#47 evidence onto that current application-service ancestry; execute the hardened image RED; only then apply the minimum fail-closed image binding. The #46 primary-source basis remains Podman container inspection semantics and NIST SP 800-190 image/container trust guidance.

Issue #136/#137 remains the self-hosted runner network-boundary lane. #137 exact `fdb1f3543044507f9fc4dd4a087afe96a4cc5ca3` owns reusable target-shape evidence; `linux-cluster-ops#326` must supply the actual s1 host-side/bridge probe identity and real pre-lease observed-denial evidence. Do not use guest loopback, arbitrary private endpoints, or closed-port proxies as host-boundary proof.

### Artifact-analysis owner stack

Artifact-analysis parent #18 remains exact `703b4b1a047321bb08d1d6cb1de78d01cb350696`. Focused artifact-analysis leaves must not copy command/runtime fixes; after #14/#112 stabilizes, foundation adoption is ordinary/non-force through #18 with every valid leaf delta/test/fixture/contract/TRACEABILITY item preserved.

#53 dynamic-attestation is test/TRACEABILITY-only exact `78f721afbf269a00721c82c155d9686d0309883f`, CI `35509858228`; its inverse RED requires `RuntimeBoundaryViolated { boundary_name: "dynamic_execution_without_runtime_behavior" }` and a direct public-schema execution=true → RuntimeBehavior relation. #61 evidence-record identity remains formatter-only exact `6d49d3fea3df2ab38ca466586dc60a1a66675c5b`, CI `35475300672`, retaining typed `InvalidEvidenceIdentity` controls. #63 policy-boundary binding `b3204f8e1c694c18489b41a1c2a1591117e6e58f`, #70 Analyzer Worker `991ecccabcf23380443d86873cf2777967112fbf`, and #76 strict evidence deserialization `60d19a531c06f035df5291948382de7ee10484c6` retain focused GREEN semantics; #51/#55/#59 remain independent result/provenance/subject-binding prerequisites.

#67 exact `010330580634fc277ae2a18e9504e2d1f8c5d712`, CI `35400068729`, retains the self-verifiability/descriptor-identity RED: materially distinct canonical artifact descriptors must not alias the same analysis-job identity. #65 Rust foundation cardinality exact `3bb72c6cdbbc07823ba1c5e59f6a7b3528b29591` retains owner-local focused GREEN. Public-schema parity remains #130 test/TRACEABILITY-only exact `ab40d33787c8eb1e01546d51fd3f554d461b59de`, CI `35521761067`; it must execute both positive controls and all eleven hostile controls, including complete EvidenceRecord preservation and whole-schema normalized digest guards, before any public-schema mutation. Only after the real checked-in schema fails for missing direct `properties.evidence.allOf` may exactly three narrow foundation occurrence branches be added.

#102 exact `71e63d9e24ae0f6fbe7c098c8a0a66e4eac500f7` owns CWL byte-vocabulary publication/version semantics. #135 exact `107ce5356c477f2a5a67392b2ac4e8bb7fe6169f` owns Gregorian profile semantics. #139 exact `ab42260cda53dc4ce02bae4fb56b492a81c99456` is an application-service consumer RED and may consume only immutable released vocabulary authority. Never mutate required semantics of a released vocabulary URI/version.

## DDD and ownership boundary

`sandbox_execution` is Core. `application_service` and `artifact_analysis` are Supporting bounded contexts. Podman/gVisor/containerd/Kubernetes/VM are infrastructure adapters. Generated names and serialized receipts are correlation/evidence unless a reviewed versioned contract grants stronger semantics. Requested configuration, backend-applied inspection, runtime-acquired backend identity, and live effective enforcement are separate evidence levels. Destructive cleanup authority must be runtime-acquired, exact, private, and non-serializable.

Core foundation repositories are optional released contract owners, not mandatory source dependencies. Consumers use immutable released contracts/ACLs; mutable branch dependencies, sibling source copies, and cross-service SQL are prohibited. Aggregate transactions stay short; external/runtime I/O never runs while holding a long DB transaction or explicit lock.

## Buyer-visible gaps

| Gap | Current authority | Required next evidence |
| --- | --- | --- |
| Git Flow production-release authority and immutable publication are unresolved. | #140; #10 `bb587667...`; `.github#1552`; protected `develop`; no current protected production release head; empty Release inventory. | Obtain released/versioned branch-model authority or explicit exception, admit one protected production head, integrate normally, verify immutable-release administration, then publish one exact source/artifact set with package/SBOM/provenance/reproducibility/rollback. |
| Missing Podman isolation-inspection fields can be normalized into secure values until #89 is adopted. | #89 `8afa77c...` / CI `35546707858`; production repair `80e6fd1...`; #14/#112 downstream. | Execute #89 exact, ordinary/non-force adopt the complete valid delta into command/runtime ancestry, and reacquire all exact-head gates. |
| Command/runtime coverage can false-GREEN through cross-owner cleanup assertions or correlated capability evidence. | #112 `19c450249...` / CI `35543654304`; reviews `5261966999`, `5262030576`. | Execute current exact; after #89 succession run one integrated exact head, preserve independent capability sources and leave cleanup syntax to #127/#141. |
| #125 coverage completeness depends on parser authority absent from its own ancestry. | #125 `5bced55...`; #112 parser. | Stabilize #112, adopt parser non-force, reacquire exact evidence. |
| Application-service can publish a lease without proving the exact acquired container uses the requested immutable digest. | P0 #46; #127 `ba37ca70...`; review `5262989006`; command/runtime #112 has a narrower missing/null fail-open; historical #19 is stale evidence. | After #89 integration and #127 causal classification, ordinary/non-force adopt #19/#45/#46/#47 evidence onto current app-service ancestry; execute exact/mismatch/missing/null/malformed/different-algorithm REDs bound to exact container ID; then require exact canonical applied digest before port/readiness/lease publication and verify on real rootless Podman. |
| Network identity, cleanup, attachment, termination, and pre-attestation hold are not yet one verified exact-ID lifecycle. | #127 `ba37ca7...` / CI `35534547447`; #128; review `5261603733`. | Execute #127 current exact, then progress provenance → no pre-ID deletion → exact-ID non-force cleanup → exact attachment → private authority → foreign-safe termination → positive isolation attest → one-time release/ack → readiness. |
| No-admitted-ID fail-closed behavior can leak orphan networks indefinitely. | P0 #141; #127 prerequisite. | After #127 classification, implement durable bounded reconciliation with full-ID reacquisition/corroboration and exact-ID non-force removal; cover replacement, collision, ambiguity, in-use, already-absent, crash/restart, and idempotent retry. |
| Self-hosted LAN/host-service denial can false-GREEN against the wrong endpoint. | #137 `fdb1f35...`; `linux-cluster-ops#326`. | Require ops-owned actual host-side/bridge identity and pre-lease observed denial. |
| Dynamic execution boolean can stand alone without RuntimeBehavior evidence. | #53 `78f721a...` / CI `35509858228`. | Execute hardened RED, then add only the matching inverse Rust/schema repair while preserving StaticOnly/unavailable/network/credential constraints. |
| Evidence IDs can contradict enclosing analysis job/sequence. | #61 `6d49d3f...` / CI `35475300672`. | Execute typed invalid-identity controls and full gates; do not substitute schema-only cross-instance claims. |
| Analysis-job identity can alias materially distinct canonical artifact descriptors. | #67 `010330580...` / CI `35400068729`. | Execute descriptor/self-verifiability RED; repair only with stable analyzer provenance. |
| Rust foundation cardinality is focused GREEN but public schema parity is unproven. | #65 focused GREEN; #130 `ab40d337...` / CI `35521761067`. | Execute #130 positive + eleven hostile controls; require real missing-`allOf` RED; then add only three narrow exact-count branches with no unrelated schema drift. |
| Focused artifact-analysis leaves remain blocked by stale command/runtime ancestry. | #63/#65/#70/#76; #18 parent; #51/#55/#59 prerequisites. | Stabilize #14/#112, ordinary/non-force adopt foundation through #18, and reacquire leaf exact-head evidence. |
| Shared byte/Gregorian vocabulary can be incomplete or mutated after release. | #102 `71e63d9...`; #135 `107ce535...`; #139 `ab42260...`. | Execute owner REDs; publish one immutable internally complete version or a new version before consumer adoption. |
| Positive effective-LSM evidence is absent on release candidates. | Hosted negative evidence exists; dedicated positive lanes repeatedly lack eligible runners. | Obtain exact-candidate positive effective-LSM evidence; never substitute negative fail-closed evidence. |
| Protected integration governance still exposes administrator bypass/approval convergence gaps. | Ruleset `18156473`; central governance owners. | Prove an ordinary protected path without administrator bypass, self-approval, direct push, or gate weakening. |
| No immutable consumer authority exists. | Production-branch governance unresolved; Release inventory empty. | Satisfy owner/security/runtime/governance gates on one protected production candidate, publish immutable source/artifact authority, then bump consumers only to that release. |

## Current bounded action order

1. Execute #89 exact `8afa77c...` / CI `35546707858`. Require repository/fmt/full tests/Clippy/rustdoc and focused missing-evidence controls; classify coverage separately and transfer no predecessor status.
2. When #89 is dependency-safe, ordinary/non-force adopt its complete valid production/test/TRACEABILITY delta into #14/#112. Preserve #112 coverage-parser, owner-boundary, chronology, and independent capability-source work; reacquire one integrated exact-head gate.
3. Execute #127 exact `ba37ca7...` / CI `35534547447` independently. Causally classify acquired network-selector/provenance/cleanup/attachment/termination behavior; do not mix unrelated image GREEN into an unclassified network head.
4. After both prerequisites are dependency-safe, ordinary/non-force adopt/adapt the complete valid #19/#45/#46/#47 evidence onto the current application-service ancestry. Remove cleanup command-shape coupling. Execute exact/mismatched/missing/null/malformed/different-algorithm applied-image REDs on the exact acquired container; only then apply the minimum fail-closed image-binding GREEN before port/readiness/lease publication.
5. Continue #127 causal chain through exact-ID non-force cleanup, exact effective attachment, private authority, foreign-safe termination, and #128 trusted hold/attest/release. Start #141 only as an ordinary successor after #127 classification.
6. Restack #133 only after #127 becomes dependency-safe, preserving its delimiter delta and actual parent adoption.
7. Execute #6 and #125 only after the command/runtime foundation/parser stabilizes; preserve their owner-local semantics and adopt foundation non-force.
8. Execute #137 only with real ops-owned host-boundary identity/evidence.
9. Execute #53, #61, #67, and #130 hardened exact REDs before corresponding production/schema GREEN. Preserve focused semantics on #63/#65/#70/#76 and #51/#55/#59 prerequisites; repair stale command/runtime ancestry through #18 only.
10. Execute #102/#135/#139 in owner order; never consume mutable sibling source or mutate a released vocabulary version.
11. Keep #10 Draft/RED until #140 and released central Git Flow governance establish production-branch authority; independently verify immutable-release administration before publication.
12. Keep root #1 Draft until positive effective-LSM, central dependency/security terminal evidence, ordinary protected production integration, governance convergence, and immutable release evidence all exist.
13. Merge/release only from one unchanged dependency-safe protected production candidate satisfying every gate and one immutable source/artifact release receipt.
