# Product and Technical Gap Baseline

Last reviewed on 2026-09-21 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea` and the live owner PR refs below. This file is the repository-wide single-writer current baseline. Predecessor checks never transfer to a moved head, mutable PR heads are not consumer authority, and GitHub `mergeable=true` is not release readiness.

The full historical causal ledger is preserved in [`product-technical-gap-history-2026-09-16.md`](product-technical-gap-history-2026-09-16.md). Historical present-tense statements there are evidence, not current authority.

## Integration root and release authority

Canonical integration root #1 remains exact `a85dc86c00f00354d9ddb9bf7c291c2c1cd40884`, Draft/open, targeting protected `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`. Native CI `35180762143` has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor jobs; positive SELinux received no eligible runner and was cancelled. Central Dependency Review `.github#810` and CodeQL terminal-publication `.github#1929` remain release blockers. Effective organization ruleset `18156473` still exposes `OrganizationAdmin/always` bypass while requiring one approving review; `.github#1340` owns routine-bypass removal and `.github#772/#1351` own solo-maintainer approval-policy convergence. This repository must not exercise that bypass, self-approve, fabricate review, direct-push, or weaken deterministic gates. Fresh GitHub Release inventory remains empty.

Release-evidence owner #10 remains test-only exact `bb587667e049c900f0dec578f083b35ed50f21c7`, CI `35485675824` queued. Executed predecessor `b26642bb86bafa8b7f2cfb0b25c0f13b4aa5a6c4` / CI `35175389416` reached exact checkout, dependency lock, repository policy, coverage-parser tests, rustfmt and the full Rust suite before `repository_exposes_fail_closed_release_delivery_contract` failed because the repository contract still required CI on `[develop, main]`; hosted negative rootless/AppArmor was GREEN, positive SELinux received no runner, and statement/branch coverage generation failed independently before admission. Review `5258441154` classified the release failure as a real authority defect, not a test-only mismatch: repository default/protected authority is `develop@60a85c...`, legacy `main@6d27dba...` is unprotected, CI correctly push-triggers only `develop`, but the release preflight itself still fetched and required protected `main`. Causal repair `402534cc86a42035883e9ee1f4db353158aa5c5d` bound executable release preflight and checked-in contract to `develop`, and ordinary descendants preserved that repair. Reviews `5258705321`, `5258991565`, and `5259204682` then hardened a test-only witness toward one live `.default_branch` identity flowing through ref validation, explicit fetch/SHA comparison, URI-safe protection lookup, and a single raw/encoded assignment authority. Current `.github/workflows/release.yml` is still hard-coded to `develop`, so this exact remains RED.

Review `5259471263` now records a higher-order owner-contract contradiction and prevents that local witness from becoming production GREEN. Canonical generic release owner `ContextualWisdomLab/.github#1552` explicitly classifies repositories whose default branch is `develop` as Git Flow: `develop` remains protected development integration, while production tag/GitHub Release authority must be an exact protected production `main`/`master` integrated head; an unprotected/stale production branch must fail closed as a repository-scoped governance gap. Fresh branch reads confirm `develop@60a85c...` is protected and `main@6d27dba3d5b013b922e21757431330d048a91d70` is unprotected; both currently resolve to tree `31da69f8655caaa5cacb1758f4cfb243fd661ea8`, but tree equality does not make unprotected `main` release authority. Issue #140 now owns this product-scoped gap, and `.github#1552` has received the fresh QSR evidence. Do not implement current #10 as “release from live default branch,” and do not encode mutable central issue/PR source locally. Wait for a released/versioned central Git Flow contract or an explicitly accepted QSR branch-model exception, then adapt #10 through ordinary non-force ancestry as a thin product-specific evidence caller.

Fresh repository ruleset inventory exposes only organization ruleset `18156473`, target `branch`; that inventory does not establish tag/release immutability. GitHub immutable releases are a separate repository/organization control that locks published release assets and the associated tag and generates release attestation when enabled. The available repository connector cannot read the authenticated immutable-release administration endpoint, so QSR enablement is unverified rather than assumed disabled. #140 requires authorized policy verification/enforcement before any publication; `gh release create --verify-tag` is not by itself immutable-release evidence.

## Current owner authority

### Command runtime and repository fitness

Lease-ownership owner #6 is exact `854d602744fd6c37696eb12c8e4bd753df639e2a`, Draft/open/mergeable on current root #1. Exact predecessor `7029537b2165c5ad0adf64183c0aa733257a5d4a` executed CI `35389946520`: verify is GREEN through exact checkout, dependency lock, repository policy, coverage-parser tests, rustfmt, the full locked workspace/all-target test suite, Clippy, and rustdoc; hosted negative rootless/AppArmor is GREEN; positive SELinux received no eligible runner and was cancelled without steps. Branch coverage generated exact evidence and, under canonical #112 physical-source union semantics, exposes `471/476` physical branch outcomes. Four missing outcomes are owner-local coordinator state transitions: Launching conflict, both Terminating fingerprint outcomes, and LeaseMismatch before backend cleanup. Current test-only `854d602...` adds deterministic state-transition tests for those four outcomes without production changes; CI `35472781891` is queued. The fifth executed missing outcome is expired-cleanup backend-error + registry-error precedence and is independently exercised by repository-fitness #112 current exact `57da726...`; do not copy that descendant test backward before current #6 executes. Ordinary production coverage on `7029537...` failed during evidence generation and must be reclassified only from current exact evidence.

Command/runtime owner #14 is live exact `73a191c65cfe1dd16447921acc8f96b306583439`. Its runtime-gate path is design evidence for #128: verified artifact binding, exact `/qsr-runtime-gate` entrypoint, one-time release token, exact-container attach, bounded acknowledgement, then local attach-client cleanup. It is not mutable source-copy authority for application-service. Repository-fitness descendant #112 is now exact `57da726d5fe3e856ba696252e2f36c2539b30a88`, CI `35469503714` queued. Exact predecessor `8991d4a22004d07077db78e57e125088fcd16de4` executed CI `35379471155`: verify and hosted rootless/AppArmor negative acceptance were GREEN, positive SELinux had no eligible runner, and the repaired physical-source coverage admission exposed real deficits rather than LLVM/codegen duplicates. Exact artifact totals are functions `512/512`, physical lines `5203/5213`, physical regions `7111/7169`, and physical branch outcomes `724/726`. Review `5257464458` classifies uncovered coordinator lines 326/419 and Podman lines 608/614/1183/1187/1191/1196/1200/1888 plus region-only deficits in `podman_runtime_gate_binding.rs` and `pr_source_artifact.rs`. Test-only predecessor `65b44ee...` covers termination while a launch owns the lease key and must return `LaunchInProgress` before backend cleanup. Review `5257670931` identifies the distinct expired-cleanup precedence gap at coordinator line 419; current test-only `57da726...` uses the existing barrier-controlled failure backend to require `cleanup_expired_at` to preserve `Backend(CleanupFailed)` when registry recording also fails. Production runtime semantics are unchanged and neither moved exact inherits predecessor GREEN.

Independent process-lifecycle owner #125 remains exact `5bced55c107aff2a2cabb84ebc6d980134e7ed07`. CI `35378675229` has now executed: verify and hosted rootless/AppArmor negative acceptance are GREEN while its coverage jobs fail under the older pre-union parser. Re-evaluating those exact-head artifacts with #112’s canonical physical-source union semantics yields `2177/2177` physical lines, `3003/3003` physical regions, and `460/460` physical branch outcomes. Review `5257466810` classifies this as a coverage-admission false deficit. Do not add redundant behavioral tests or weaken thresholds; after #112 stabilizes, #125 must adopt the canonical parser through ordinary/non-force ancestry.

### Application-service and network lifecycle

Canonical application-service runtime/lifecycle owner #21 is exact `717edef989d8b6bb7e1f672d91d9258f7942a6b2`, Draft/open/mergeable. Exact CI `35165254755` has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor jobs; positive SELinux received no eligible runner, so the workflow is not release-GREEN.

Canonical network successor #127 is now exact `d2c22a423870e5566b198587e285ecbf66ea0526`, Draft/open/mergeable, normally containing #21 through ordinary two-parent merge `7934aa99085264db7ee654088c3df0f116a67b66`. Historical predecessor `71063882948daf5819839d8059c37857a6255abf` acquired hosted runners in CI `35384682678`: hosted negative rootless/AppArmor was GREEN, while verify reached the full workspace and all six `podman_application_service_create_identifier_red` cases failed before their intended container-receipt boundary with `BackendCommandFailed { operation: "network_identity_inspect" }`. Test-only `190762bc349590cb5742b8b4efad7b8a15534c9f` repaired that stale fixture by supplying the already-required canonical lower-case 64-hex/internal/DNS-disabled network inspection response; production semantics were unchanged.

Issue #128 remains P0. Review `5254951818` found the pre-attestation witness could false-GREEN because any `--entrypoint=*` suppressed the hostile side-effect marker. Test-only predecessor `7f21375cd79936d8ce0769999dc602112b500a15` closes that false-GREEN without changing production: a fake hold is recognized only when create argv contains `--interactive`, a test-owned gate artifact mounted read-only at `/qsr-runtime-gate`, and exact `--entrypoint=/qsr-runtime-gate`; independent fixture controls prove `/bin/false` still arms/runs the hostile payload while that exact binding holds it. Review `5255195712` records that repair.

Review `5255563318` found a second evidence false-GREEN in the network-provenance RED: WrongName/ExternalNetwork/DnsEnabled could all pass on an unrelated failure before exact-name inspection. Test-only predecessor #127 `f9a95889119f458954b3833e27f10907d403bb3c` therefore requires runtime-owned network creation, derives the exact generated `qsr-net-*` correlation from the invocation log, requires exactly one `network inspect --format json <that name>` after creation, and still forbids container create. The single-inspection requirement prevents retry-until-match identity admission.

That `f9a9588...` exact has now executed in CI `35439408261`. Repository policy, coverage-parser tests, rustfmt, all six create-identifier controls, and hosted negative rootless/AppArmor reached terminal evidence. Verify and coverage then failed all eight `podman_application_service_create_receipt_edges` cases before their intended receipt/create/start semantics with `BackendCommandFailed { operation: "network_identity_inspect" }`. Review `5256956939` classifies a second stale fixture prerequisite: the receipt-edge fake Podman handled `network:create`/`network:rm` but not the newly required `network:inspect`. Branch coverage also failed on the same moved exact; positive SELinux remains unassigned/queued. Current test-only #127 `d2c22a...` changes only that receipt fixture by adding the same canonical lower-case 64-hex/internal/DNS-disabled inspection response. Production Rust/API/schema/network behavior is unchanged. Native CI `35460751019` is queued; no result from `f9a9588...` transfers.

The production issue remains: current application-service `launch_at` starts the exact acquired container before `verify_effective_isolation`. Before production GREEN, #128 still requires an independent positive lifecycle RED proving that successful effective attestation enables exactly one runtime-owned release transition, with acknowledgement bound to the exact acquired container/request, and only then readiness. The active owner sequence remains exact network provenance → no destructive cleanup before ID admission → exact-ID non-force cleanup → exact effective attachment by admitted `NetworkID` → public correlation/private authority separation → foreign-safe explicit termination → mandatory effective isolation evidence → trusted hold/attest/release/ack.

Application-service option/operand successor #133 is exact `4191ac0051093c30150734f6b4e98c05f36a0ef4`, Draft/open/mergeable. Fresh compare against #127 `d2c22a...` is diverged with merge base `71063882948daf5819839d8059c37857a6255abf`: #133 is 13 commits ahead and 4 commits behind. Its semantic delta remains one literal `--` immediately before `request.image_reference`. Do not restack until #127 current exact is causally classified; then adopt the four parent commits non-force, repair its stale checked-in delimiter TRACEABILITY, and reacquire focused/full evidence.

Issue #138 / Draft #139 owns the application-service public-schema UTF-8-byte mismatch. #139 current exact `ab42260cda53dc4ce02bae4fb56b492a81c99456` is formatter-only over the same RED/TRACEABILITY and has CI `35461316380` pending. Reviews `5253486572` and `5253616141` removed arbitrary-dialect and URI-only false-GREEN paths. #101/#102 remains the immutable shared byte-vocabulary prerequisite; #139 may consume only a released contract for GREEN.

Issue #136/#137 is a separate self-hosted-runner boundary: `linux-cluster-ops#326` owns s1 placement/registration and the true pre-lease enablement gate; this repository owns reusable isolation-attestation evidence. Reviews `5255674039`, `5255831636`, `5255961705`, and `5255976931` removed raw-substring, helper-shape, unrelated-fixed-probe, inline-comment, and scope→target false-GREEN paths. Review `5256349715` then found an RFC1918 target could use an arbitrary closed service port while symbolic service aliases could fail DNS resolution and masquerade as denial. Test-only predecessor `44d9beb6eaffe4e42c987b95a4ce064c78a567ba` required RFC1918 host targets without arbitrary ports and exact Redis/PostgreSQL/DNS ports on literal private/loopback/link-local IPv4 addresses. Review `5256617995` found loopback is the wrong authority under the selected dedicated-VM topology: `127.0.0.1:*` probes the guest, not the s1 host boundary. Test-only `247c19ccc5d8096f6fd938964c3b20c3c997ebeb` repaired the probe-semantics witness, but fresh reviews `5256925734` and `5256936716` found the older target-binding witness still admitted guest loopback and RFC1918 targets carrying arbitrary service ports. Current test-only #137 exact `fdb1f3543044507f9fc4dd4a087afe96a4cc5ca3` aligns both contracts: Redis/PostgreSQL/DNS require their exact service port on literal non-loopback private/link-local IPv4, while the three RFC1918 scopes require portless in-range literal IPv4. Production workflow behavior remains unchanged and the live workflow still has no attestation step, so this exact remains intentional RED; CI `35460412420` is materialized and queued. One authority gap remains before workflow GREEN: an arbitrary nonexistent private endpoint can still fail without proving it is the actual host-side boundary. `linux-cluster-ops#326` must publish the real host-side/bridge probe identity or equivalent immutable operator-owned endpoint contract and real pre-lease observed-denial evidence. QSR must not invent that identity. Finite point probes are not proof of complete CIDR non-reachability.

### Artifact-analysis owner stack

Artifact-analysis parent #18 is live exact `703b4b1a047321bb08d1d6cb1de78d01cb350696`, Draft/open/mergeable. Do not copy unstable command/runtime source into artifact-analysis leaves; foundation adoption must be ordinary/non-force and preserve every valid test/fixture/contract/TRACEABILITY delta.

- #53 dynamic-attestation current test/TRACEABILITY-only exact `78f721afbf269a00721c82c155d9686d0309883f`, CI `35509858228` queued: formatter-clean predecessor `9d0da2a50e3247fd6cd2e52301d57408d404557d` / CI `35342943374` executed the original seven-control regression with two guard controls passing and five causal Rust/wire failures. Candidate commits `387406c...` and `4a9cd59...` repair truthful dynamic completion, false completion, and ghost `RuntimeBehavior` in one direction. Review `5260321336` found the remaining inverse evidence gap: `dynamic_execution_performed=true` can still validate without any `RuntimeBehavior`. Review `5260530224` then found two false-GREEN paths in that inverse witness before production repair: the Rust RED accepted any validation error, and the schema matcher allowed hidden sibling predicates that could make the intended implication vacuous. Test-only `1454ebb...` now requires the exact execution=true trigger and direct RuntimeBehavior consequence with hostile sibling-predicate controls; test-only `402b605...` requires the exact `RuntimeBoundaryViolated { boundary_name: "dynamic_execution_without_runtime_behavior" }` failure. Docs-only current `78f721a...` makes TRACEABILITY current. Production Rust/schema remain unchanged from the prior candidate until this hardened exact executes causally.
- #61 evidence-record identity exact `6d49d3fea3df2ab38ca466586dc60a1a66675c5b`: executed predecessor `acb615de...` / CI `35348644513` passed exact checkout, dependency lock, repository policy, coverage-parser tests and hosted negative rootless/AppArmor, then stopped at Rust 1.97.1 `cargo fmt --check` before semantic tests/Clippy/rustdoc. Review `5258231836` records that prerequisite; current exact is formatter-only and CI `35475300672` is queued. The typed `InvalidEvidenceIdentity` semantics remain the candidate under test.
- #63 policy-boundary binding exact `b3204f8e1c694c18489b41a1c2a1591117e6e58f`: owner-local contract focused GREEN; broader failure is inherited command/runtime ancestry.
- #65 foundation-cardinality exact `3bb72c6cdbbc07823ba1c5e59f6a7b3528b29591`: CI `35323306415` has now executed the formatter-clean exact. Exact checkout, dependency lock, repository policy, coverage-parser tests, rustfmt, and the focused `v1_foundation_evidence_records_must_exist_exactly_once` control are GREEN, proving the Rust aggregate cardinality repair on this owner head. Whole-workspace verify later fails the inherited ENTRYPOINT control `podman_command_execution_entrypoint_red::requested_command_is_encoded_as_exact_entrypoint_argv`. Ordinary and branch coverage independently pass the same cardinality control, then fail a different stale parent fixture: `podman_command_execution::run_command_at_cleans_up_when_container_create_returns_a_malformed_identifier` observes `BackendInvocationFailed { operation: "backend_security_info" }` before its expected `MalformedIsolationInspection { operation: "container_create" }`. Hosted negative rootless/AppArmor is GREEN; positive SELinux received no runner and was cancelled. Reviews `5259763136` and corrective classification `5259769146` therefore establish owner-local focused GREEN but no whole-head GREEN. Current #14 already reworks this legacy command-execution test path and trusted-create-ID semantics. The old artifact-analysis parent also executes checkout with `persist-credentials: true`; root #1 owns canonical `persist-credentials: false`. Do not patch any of these parent-owned failures in #65; ordinary/non-force adopt current root/#14 into #18 and then #65 before merge promotion.
- #130 schema-cardinality current test/TRACEABILITY-only exact `ab40d33787c8eb1e01546d51fd3f554d461b59de`, CI `35521761067` queued. Executed predecessor `038d0ce36c04b598c246acf09a82ee2967a9aec8` / CI `35333036539` passed exact checkout, dependency lock, repository policy and coverage-parser tests, then stopped at Rust 1.97.1 `cargo fmt --check` before the schema-cardinality witness. Reviews `5259587377`, `5260036723`, `5260141554`, `5260636868`, `5260783003`, `5260890616`, and `5260908266` record the formatter prerequisite and successive false-GREEN classes: dead/optional constraint placement, hidden predicates inside `contains`, hidden sibling assertions/applicators on a counted occurrence object, array-parent or extra-`allOf` restrictions such as `maxItems: 3` that suppress optional non-foundation evidence, weakening of the evidence-array/item surface, weakening of nested EvidenceRecord field semantics, `evidence_kind` sibling narrowing, and item-level sibling assertions. Test-only `d98835d50b367d018686045be64aabbc3447d16b` requires the complete existing EvidenceRecord `items` schema and retains the unchanged-baseline positive control plus the nine structural hostile controls. Review `5261015098` found a broader whole-schema false-GREEN: pointer-local cardinality checks could still pass while unrelated public EvidenceBundle semantics changed. Test-only `53f8ce621010f35a382c0fa31a443d7da632eb52` removes only the authorized `properties.evidence.allOf` delta, pins every other normalized public-schema contract to SHA-256 `e92a6562641ed7f4fc21f1d247cb97f78709a6dd8aaa34e8820c6343c1b90206`, retains a positive intended-cardinality candidate, and adds hostile root `not: {}` plus unrelated `analysis_job_id.maxLength` drift controls. Docs-only current `ab40d337...` makes that boundary code-current; review `5261018282` confirms production Rust/public schema remain unchanged. Do not implement schema GREEN until this exact executes both positive controls, all eleven hostile controls, and then reaches the intended missing direct `properties.evidence.allOf` RED.
- #76 strict evidence deserialization exact `60d19a531c06f035df5291948382de7ee10484c6`: owner-local controls focused GREEN; broad failure is inherited command/runtime ancestry.
- #70 Analyzer Worker exact `991ecccabcf23380443d86873cf2777967112fbf`: Worker/Core suites focused GREEN; decoded whole-workspace failure is inherited. Do not weaken worker semantics or mask with retry/sleep/mutex.
- #51/#55/#59 remain independent result/provenance/subject-binding prerequisites.
- #67 exact `010330580634fc277ae2a18e9504e2d1f8c5d712` retains executed self-verifiability RED plus descriptor-identity RED; CI `35400068729` is queued.
- #102 UTF-8/dialect publication current exact `71e63d9e24ae0f6fbe7c098c8a0a66e4eac500f7`, CI `35463767954` queued. Predecessor `517bc8a...` / CI `35405113169` executed the hardened missing-publication RED; `c42d59c...` added minimum byte-vocabulary semantics/vectors. Review `5257041743` found the same-version `x-cwl-rfc3339Profile` publication contradiction. Review `5257201663` removed heading-only/reject-only publication, and review `5257227909` found label-only/contradictory vector objects could still false-GREEN. Current test-only `71e63d9...` requires same-version normative `MUST` semantics plus concrete vectors with non-empty id, asserted keyword value and evaluated instance, accepting/rejecting coverage, and materially distinct positive/negative assertion inputs. #135 still owns exact Gregorian semantics.
- #135 Gregorian `submitted_at` parity current exact `107ce5356c477f2a5a67392b2ac4e8bb7fe6169f`, CI `35461212064` queued. Review `5253891162` requires normative Gregorian profile semantics/vectors; review `5257046293` adds the versioning boundary: after causal RED, use a new vocabulary/dialect version if 1.0.0 is already immutable, or coordinate Gregorian semantics into 1.0.0 before its first publication. Never release then mutate the same URI/version.

## DDD and ownership boundary

`sandbox_execution` is Core. `application_service` and `artifact_analysis` are Supporting bounded contexts. Podman/gVisor/containerd/Kubernetes/VM are infrastructure adapters. Generated names and serialized receipts are correlation/evidence unless a reviewed versioned contract grants stronger semantics. Destructive cleanup authority must be runtime-acquired, exact, private, and non-serializable. Requested configuration, backend-applied inspection, acquired backend identity, and live effective enforcement are distinct evidence levels.

Core foundation repositories are optional released contract owners rather than mandatory source dependencies. Consumers use immutable released contracts/ACLs; mutable branch dependencies, sibling source copies, and cross-service SQL are prohibited.

## Buyer-visible gaps

| Gap | Current authority | Required next evidence |
| --- | --- | --- |
| Git Flow production-release authority is unresolved: current #10 witness treats the live default branch (`develop`) as the future release source, while canonical `.github#1552` requires an exact protected production `main`/`master`; current `main` is unprotected. Immutable-release policy is separately unverified. | #140; #10 current test-only `bb587667...` / CI `35485675824`; review `5259471263`; `.github#1552`; fresh `develop@60a85c...` protected, `main@6d27dba...` unprotected; ruleset inventory currently exposes only branch target `18156473`. | Keep #10 RED and do not implement live-default-branch GREEN. Obtain a released/versioned central Git Flow contract or explicit branch-model exception; protect/admit the production branch before release use; integrate the exact release candidate through ordinary reviewed governance; then adapt #10 as a thin product-evidence caller and reacquire exact-head gates. Separately verify/enforce GitHub immutable-release policy through the authorized administration surface before publication; bind release receipt to production SHA/tree, tag, package/SBOM digests, provenance/attestation and central workflow version. |
| Lease-owner branch admission still has five executed coordinator outcomes to close; four are owner-local and one is already owned by descendant repository-fitness. | #6 current `854d602...` / CI `35472781891`; executed predecessor `7029537...` / CI `35389946520` has 471/476 physical branch outcomes; #112 current `57da726...` owns cleanup-expiry dual-error precedence. | Execute #6 current deterministic Launching/Terminating/LeaseMismatch tests and require those four outcomes to close without production change. Then adopt the independently proven #112 precedence delta through ordinary ancestry rather than source-copying backward. Reclassify ordinary coverage generation only from current exact evidence. |
| Coverage admission can false-underreport repeated LLVM/codegen records, while the corrected physical-source admission now exposes genuine repository-fitness coverage gaps. | #112 current `57da726...` / CI `35469503714`; predecessor `8991d4a...` executed physical-source admission at 512/512 functions, 5203/5213 lines, 7111/7169 regions, 724/726 branches; reviews `5257464458`, `5257670931`. | Execute current line-326 plus expired-cleanup line-419 coverage repairs, then close only the remaining concrete physical line/region/branch deficits without threshold/denominator weakening. |
| #125 can fail coverage solely because it has not adopted #112’s canonical physical-source parser. | #125 `5bced55...` / CI `35378675229`; review `5257466810`; exact artifacts evaluate to 2177/2177 lines, 3003/3003 regions, 460/460 branches under #112 semantics. | Stabilize #112 first, then ordinary/non-force adopt its coverage parser into #125 while preserving #125 runtime deltas; do not add duplicate tests or source-copy ad hoc. |
| Application-service create-ID witness had stale network-inspection setup. | #127 predecessor `7106388...` / CI `35384682678`; fixture repair `190762b...`; all six controls GREEN on `f9a9588...`. | Preserve the repair while current #127 advances; do not transfer predecessor status. |
| Create-receipt edge tests also predated acquired-network-ID inspection and failed before their receipt semantics. | #127 predecessor `f9a9588...` / CI `35439408261`; review `5256956939`; current repair `d2c22a...` / CI `35460751019`. | Execute current exact and require all eight receipt controls to reach their intended create/receipt/start boundaries before classifying provenance or hold REDs. |
| Network-provenance contradiction RED could pass before inspecting the created network. | #127 current `d2c22a...`; inherited review `5255563318`; predecessor CI `35439408261` was blocked later by the receipt fixture. | Execute current exact past repaired prerequisites and require one exact-name inspection after create before contradiction rejection; then implement only the causal provenance admission if RED is reached. |
| Application-service network authority is not fully exact-ID and foreign-safe end to end. | #127 `d2c22a...`. | After current RED classification, progress provenance → cleanup authority → effective attachment → private authority → non-force termination → mandatory evidence. |
| Pre-attestation hostile-service witness accepted arbitrary entrypoint presence as a trusted hold. | #128; reviews `5254951818`, `5255195712`; inherited #127 hardening `7f21375...`. | Execute the hardened arbitrary-entrypoint RED on current #127 after repaired prerequisites. Then add an independent positive attest → one-time release → exact ack → readiness lifecycle RED before production GREEN. |
| Leading-dash image references need explicit Podman option termination. | #133 `4191ac0...`, CI `35384712828`, now 4 commits behind moved #127. | After #127 classification, non-force adopt current parent, repair stale delimiter TRACEABILITY, and reacquire delimiter/full evidence. |
| Required CWL vocabulary 1.0.0 can expose a custom keyword without same-version implementable semantics/concrete balanced conformance, or be mutated after release to add them. | #101/#102 current `71e63d9...`, CI `35463767954`; reviews `5257041743`, `5257201663`, `5257227909`; missing-publication RED already executed on `517bc8a...`. | Execute the hardened concrete-vector keyword-completeness RED. Before first immutable publication, require normative `MUST` semantics plus concrete accepting/rejecting vectors with identity, asserted keyword value and evaluated instance and distinct assertion inputs, then either make 1.0.0 internally complete or move Gregorian semantics to a new vocabulary/dialect version. Never release then mutate the same URI/version. |
| Application-service public schema can admit multibyte values rejected by runtime UTF-8 byte limits. | #138/#139 `ab42260...`, CI `35461316380`. | Execute current formatter-clean RED; after released #101/#102, prove exact dialect/vocabulary/byte-keyword surface and vectors. |
| `submitted_at` schema/runtime Gregorian semantics can diverge. | #134/#135 `107ce535...`, CI `35461212064`; reviews `5253891162`, `5257046293`. | Execute current RED. Then consume an immutable owner-safe vocabulary: new version if byte-only 1.0.0 is already released, or include Gregorian semantics/vectors before the first 1.0.0 publication. |
| Self-hosted CI can false-GREEN host/network denial through inconsistent target contracts, guest loopback, closed ports, or an arbitrary unbound private endpoint. | #136/#137 `fdb1f35...`; reviews `5256925734`, `5256936716`; CI `35460412420`; `linux-cluster-ops#326`. | Execute the current aligned RED; require ops-owned publication of the actual s1 host-side/bridge probe identity and real pre-lease failed-reachability output before workflow GREEN. Never substitute guest loopback, arbitrary CIDR service ports, a random private address, or finite point probes for the real boundary. |
| Dynamic analysis receipt completeness has an inverse-evidence RED, and that RED itself required false-GREEN hardening before production repair: an execution boolean can still stand alone without observed `RuntimeBehavior`, while a generic error or vacuous schema rule could previously satisfy the witness. | #53 current test/TRACEABILITY-only `78f721a...` / CI `35509858228`; causal predecessor `9d0da2a...` / CI `35342943374`; reviews `5260321336`, `5260530224`; hardened schema RED `1454ebb...`; exact-error Rust RED `402b605...`; #49/#50 remain separate. | Execute the hardened exact. Require the Rust failure to be exactly `RuntimeBoundaryViolated { boundary_name: "dynamic_execution_without_runtime_behavior" }` and require a direct schema implication from only `dynamic_execution_performed=true` to only a narrow RuntimeBehavior `contains`, with hostile hidden-sibling controls passing. Then add only the matching inverse GREEN while preserving StaticOnly/unavailable controls, network/credential denial, full coverage and positive effective-LSM evidence. Do not promote receipt consistency into worker-containment evidence. |
| Evidence IDs can contradict enclosing job/sequence, while the current repaired head has only cleared a predecessor formatting prerequisite so far. | #61 current `6d49d3f...` / CI `35475300672`; causal RED on `36cea922...`; production repair `c024ecc...`; executed predecessor `acb615de...` stopped at rustfmt; review `5258231836`. | Execute current formatter-clean exact through the typed hostile controls, full Rust gates and owned-production coverage before promoting the identity invariant. Do not claim stock JSON Schema cross-instance parity or transfer predecessor status. |
| Analysis job identity can alias materially distinct canonical artifact descriptors. | #66/#67 `010330580...`, CI `35400068729`. | Execute descriptor witness; repair only after stable analyzer provenance can participate in independently verifiable derivation. |
| EvidenceBundle Rust foundation-cardinality is focused GREEN, but public JSON Schema parity has not executed; the schema RED could still false-GREEN if pointer-local cardinality structure is correct while either the EvidenceRecord item contract or an unrelated public EvidenceBundle contract changes. The artifact-analysis stack also inherits stale command-runtime fixtures plus checkout ancestry. | #65 exact `3bb72c6...` / CI `35323306415`, focused Rust cardinality GREEN; reviews `5259763136`, `5259769146`; verify fails inherited ENTRYPOINT control while both coverage lanes fail later at stale `backend_security_info` fixture; #130 current `ab40d337...` / CI `35521761067` queued; reviews `5260036723`, `5260141554`, `5260636868`, `5260783003`, `5260890616`, `5260908266`, `5261015098`, `5261018282`. | Preserve #65’s proven Rust invariant, then ordinary/non-force adopt current root/#14 into #18 and descendants so inherited ENTRYPOINT, legacy command fixture, and checkout-credential drift are repaired at their owner rather than copied leaf-by-leaf. Independently let #130 execute both positive controls, all eleven hostile controls, and the real direct `properties.evidence.allOf` witness; only after causal schema RED add exactly three minimal foundation branches. Preserve `type: array`, `minItems: 1`, the complete existing EvidenceRecord `items` schema, and every other normalized public EvidenceBundle contract; no hidden inner/outer/parent/item/root predicates, unrelated extra `allOf`, unrelated field drift, item weakening, or singleton/global suppression of non-foundation evidence. |
| Focused artifact-analysis leaves remain blocked by stale command-runtime ancestry. | #63/#65/#76/#70 focused semantics are GREEN; #65 exact logs prove distinct inherited verify and coverage fixture failures. | Stabilize #14/#112, then ordinary/non-force adopt root/#14 into #18 and descendants; reacquire exact-head evidence. Do not copy command-runtime or checkout-workflow fixes into leaf owners. |
| Positive effective-LSM evidence is absent on release candidates. | Hosted negative evidence exists; dedicated SELinux lanes repeatedly lack runners. | Obtain exact-candidate positive effective-LSM evidence; never substitute negative fail-closed evidence. |
| Protected integration governance admits routine administrator bypass and an approval gate not yet converged for the solo-maintainer path. | Ruleset `18156473`; `.github#1340`, `.github#772`, `.github#1351`. | Remove routine admin bypass and prove a satisfiable ordinary protected path without self-approval, direct push, or gate weakening. |
| No immutable consumer authority exists. | Protected `develop` unchanged; production-branch governance #140 unresolved; central security non-terminal; Release inventory empty; immutable-release setting unverified. | Resolve #140 with released central Git Flow governance, integrate the exact release candidate through the protected production branch, satisfy central/security/runtime gates, explicitly verify/enforce immutable releases, then publish version/CHANGELOG, package/release, SBOM, provenance, reproducibility and rollback from one exact source/artifact set. |

## Current bounded action order

1. Let #6 exact `854d602...` / CI `35472781891` execute the four owner-local coordinator branch repairs. Executed predecessor `7029537...` established 471/476 physical branch outcomes; do not copy #112’s separate cleanup-expiry dual-error test backward or transfer predecessor GREEN. Reclassify ordinary coverage generation only from current exact.
2. Let #112 exact `57da726...` / CI `35469503714` execute both deterministic coordinator coverage repairs: termination during Launching and expired-cleanup backend-failure precedence under simultaneous registry loss. Predecessor `8991d4a...` already proved the physical-source parser and identified real deficits; repair remaining exact physical gaps only, without denominator/threshold weakening.
3. Let #127 exact `d2c22a...` / CI `35460751019` execute. Predecessor `f9a9588...` proved create-ID repair and hosted negative rootless/AppArmor but exposed the separate receipt-edge fixture prerequisite; require all eight receipt controls to reach intended semantics and then classify the inherited provenance and arbitrary-entrypoint witnesses on current exact only.
4. If #127 reaches the intended provenance/#128 causes, continue in owner order: provenance admission → no destructive cleanup before ID admission → exact-ID non-force post-admission cleanup → exact effective attachment → private authority/foreign-safe termination → positive attest → one-time release → exact acknowledgement → readiness.
5. Non-force restack #133 only after #127 dependency-safe evidence is classified; it is currently 13 ahead / 4 behind. Repair its stale checked-in delimiter TRACEABILITY during that actual adoption and reacquire focused/full gates.
6. Let #137 exact `fdb1f35...` / CI `35460412420` execute the aligned target-binding/probe-semantics RED. Do not implement workflow GREEN until `linux-cluster-ops#326` publishes the actual s1 host-side/bridge probe identity and real pre-lease failed-reachability evidence. Bind any later in-job guard to that authority; do not invent a private endpoint, reintroduce CIDR service-port probes, or treat finite point probes as complete CIDR proof.
7. Let #102 exact `71e63d9...` / CI `35463767954` execute the hardened required-vocabulary concrete-vector RED. Its predecessor missing-publication RED is already causal. The generic witness rejects heading-only, label-only, reject-only and contradictory-input publication; #135 still owns exact Gregorian semantics. Do not publish 1.0.0 until the versioning contradiction is repaired without post-release semantic mutation.
8. Let #139 exact `ab42260...` / CI `35461316380` execute its formatter-clean UTF-8 mismatch RED; only after immutable #101/#102 release may it consume the versioned byte-vocabulary contract.
9. Let #135 exact `107ce535...` / CI `35461212064` execute the Gregorian-profile RED. After causal execution, use a new vocabulary/dialect version if 1.0.0 has already been published, or coordinate the Gregorian semantics/vectors before the first 1.0.0 publication; never mutate a released URI/version.
10. Let #53 current `78f721a...` / CI `35509858228` execute the hardened inverse-evidence REDs. Do not add production/schema GREEN until the Rust test fails for exactly `dynamic_execution_without_runtime_behavior`, the schema witness rejects hidden sibling predicates, and the checked-in schema lacks the required direct execution=true → RuntimeBehavior relation for the intended cause. After causal RED, make only the minimum bidirectional repair while preserving the already-proven original controls. Let #61 current `6d49d3f...` / CI `35475300672` independently prove its formatter-only repair reaches the typed `InvalidEvidenceIdentity` controls and full gates.
11. Preserve focused GREEN semantics on #63/#65/#70/#76. #65 exact `3bb72c6...` now proves its Rust cardinality invariant, while verify and coverage expose two distinct inherited command-runtime test paths and the old parent retains checkout credential persistence. Stabilize #14/#112, then ordinary/non-force adopt current root/#14 into #18 and descendants; reacquire exact-head evidence instead of copying parent fixes into leaves.
12. Let #130 current `ab40d337...` / CI `35521761067` execute the unchanged-baseline and intended-cardinality positive controls plus all eleven hostile controls: hidden `contains` predicate, hidden counted-branch sibling predicate, parent evidence-array cap, unrelated extra `allOf`, weakened `minItems`, weakened `items`, weakened nested EvidenceRecord field semantics, hidden `evidence_kind` sibling narrowing, hidden item-level sibling predicates, hostile root `not: {}`, and unrelated `analysis_job_id.maxLength` drift; then require the real checked-in schema to fail for missing direct effective foundation cardinality. Its #65 Rust prerequisite is owner-local focused GREEN, but that does not transfer whole-head gates or prove wire parity. Only after causal missing-`allOf` RED may the public schema add exactly three narrow exact-count branches while preserving every other normalized public schema contract and optional non-foundation evidence.
13. Keep #125 exact `5bced55...` unchanged while #112 is still repairing repository-fitness coverage. Its current runtime evidence is GREEN and its apparent coverage deficit is parser-only; after #112 stabilizes, adopt the canonical parser through ordinary/non-force ancestry and reacquire exact-head evidence.
14. Keep release-evidence #10 exact `bb587667...` / CI `35485675824` Draft/RED. Its queued execution may still provide diagnostic evidence, but review `5259471263` and #140 prohibit implementing its live-default-branch witness as production GREEN because canonical `.github#1552` requires a protected production `main`/`master` for this Git Flow topology. First obtain a released/versioned central branch-model contract or explicit QSR exception, protect/admit the production branch before use, and integrate the exact release candidate through ordinary reviewed governance. Then adapt #10 as a thin product-specific evidence caller and reacquire exact-head gates. Independently verify/enforce GitHub immutable-release policy before publication; do not equate `--verify-tag` with immutability.
15. Keep root #1 Draft until positive effective-LSM, central Dependency Review/CodeQL terminal evidence, ordinary protected integration, production-branch governance #140, immutable-release policy evidence, and `.github#1340` + `.github#772/#1351` governance convergence are all proven. Treat `.github#712` queue health as an operational gate for current-head evidence, not permission to rerun blindly or weaken selectors. Never use `current_user_can_bypass=always` as a release shortcut.
16. Merge/release only from one unchanged dependency-safe protected production candidate satisfying every gate and one immutable source/artifact release receipt.