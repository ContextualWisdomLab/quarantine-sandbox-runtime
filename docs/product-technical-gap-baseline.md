# Product and Technical Gap Baseline

Last reviewed on 2026-09-19 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea` and the live owner PR refs below. This file is the repository-wide single-writer current baseline. Predecessor checks never transfer to a moved head, mutable PR heads are not consumer authority, and GitHub `mergeable=true` is not release readiness.

The full historical causal ledger is preserved in [`product-technical-gap-history-2026-09-16.md`](product-technical-gap-history-2026-09-16.md). Historical present-tense statements there are evidence, not current authority.

## Integration root and release authority

Canonical integration root #1 remains exact `a85dc86c00f00354d9ddb9bf7c291c2c1cd40884`, Draft/open, targeting protected `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`. Native CI `35180762143` has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor jobs; positive SELinux received no eligible runner and was cancelled. Central Dependency Review `.github#810` and CodeQL terminal-publication `.github#1929` remain release blockers. Effective organization ruleset `18156473` also still exposes `OrganizationAdmin/always` bypass while requiring one approving review; `.github#1340` owns routine-bypass removal and `.github#772/#1351` own solo-maintainer approval-policy convergence. This repository must not exercise that bypass, self-approve, fabricate review, direct-push, or weaken deterministic gates. GitHub Release inventory remains empty. Hosted sibling GREEN does not substitute for positive effective-LSM, central security terminal evidence, ordinary protected integration, or immutable publication.

## Current owner authority

### Command runtime and repository fitness

Lease-ownership owner #6 is exact `7029537b2165c5ad0adf64183c0aa733257a5d4a`, Draft/open/mergeable on current root #1. Predecessor `785c2cc84861d3bce5fb78a7183cf3102b743bf5` executed CI `35180780227`: repository policy, coverage-parser and formatting passed; six of seven lease-owner controls passed, while `failed_launch_releases_idempotency_reservation_for_retry` failed on retry with `BackendInvocationFailed { operation: "rootless_probe" }`. Current test-only `7029537...` replaces the mutable executable fixture by staging a sibling inode and atomically renaming it over the fake Podman pathname; production/idempotency semantics remain unchanged. Current CI `35389946520` is queued. Issue #131 is completed and no longer an open gate.

Command/runtime owner #14 is actual live exact `73a191c65cfe1dd16447921acc8f96b306583439`. Its hosted verify and negative-rootless evidence are historical/current-owner evidence only; repository-wide coverage and positive effective-LSM/release gates remain incomplete. Repository-fitness descendant #112 is exact `8991d4a22004d07077db78e57e125088fcd16de4`; predecessor `48ad86148be70fff165800d07221409bc7521af5` executed the physical-source/codegen branch RED, and semantic `89c182151051668f4f468c53e64a947d965dda95` now unions true/false outcomes by physical branch identity while failing closed on denominator mismatch. Current #112 CI `35379471155` is queued.

Independent process-lifecycle owner #125 is exact `5bced55c107aff2a2cabb84ebc6d980134e7ed07`. Predecessor full tests and hosted-negative evidence passed before Clippy rejected an `if status_result.is_err() { return status_result; }` form. Current head applies only `status_result?;`; no runtime behavior changed. CI `35378675229` is queued.

### Application-service and network lifecycle

Canonical application-service runtime/lifecycle owner #21 is exact `717edef989d8b6bb7e1f672d91d9258f7942a6b2`, Draft/open/mergeable. It owns runtime invocation identity (#20), exact post-create container lifecycle authority (#40), non-serializable private cleanup authority (#42), and successful-create malformed-stdout receipt recovery (#113). Exact CI `35165254755` has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor jobs; positive SELinux received no runner, so the workflow is not release-GREEN.

Canonical network successor #127 is exact `190762bc349590cb5742b8b4efad7b8a15534c9f`, Draft/open/mergeable, and normally contains current #21 through ordinary two-parent `7934aa99085264db7ee654088c3df0f116a67b66`. Predecessor `71063882948daf5819839d8059c37857a6255abf` finally acquired hosted runners in CI `35384682678`: hosted negative rootless/AppArmor was GREEN, while verify reached the full workspace and all six `podman_application_service_create_identifier_red` cases failed before their intended container-receipt boundary with `BackendCommandFailed { operation: "network_identity_inspect" }`; coverage and branch-coverage generation failed from the same stale prerequisite. The cause was fixture drift after acquired-network-ID admission, not a production defect. Minimum test-only `190762bc...` adds the already-required canonical network-inspect response to that fixture and changes no production Rust/API/schema/network semantics. Current CI `35428581879` is queued. The active owner sequence remains exact one-object network provenance → no destructive cleanup before ID admission → exact-ID non-force cleanup after admission → exact effective attachment by admitted `NetworkID` → public correlation/private authority separation → foreign-safe explicit termination → mandatory effective isolation evidence → #128 hold/attest/release.

Issue #128 remains P0. Review `5254951818` found its current pre-attestation witness can false-GREEN because the fake backend treats the presence of any `--entrypoint=*` as equivalent to a trusted runtime-owned hold. An arbitrary explicit entrypoint can suppress the hostile side-effect marker without proving hold identity, one-time post-attestation release, or trusted release acknowledgement. The RED must be hardened before semantic GREEN so arbitrary-entrypoint substitution remains unsafe, the exact hold primitive is bound, and a positive attested release/acknowledgement path proves the requested service becomes runnable only after successful effective isolation.

Application-service option/operand successor #133 is exact `4191ac0051093c30150734f6b4e98c05f36a0ef4`, Draft/open/mergeable, normally restacked on current #127. Its semantic delta remains one literal `--` immediately before `request.image_reference`, with focused regression and TRACEABILITY. Current CI `35384712828` is queued; after #127 moves, #133 must adopt the current parent non-force before its evidence can be current.

Issue #138 / Draft #139 owns the application-service public-schema UTF-8-byte mismatch. #139 exact `3a2ed2002417692675ec7b21a75a2f1718fc3191` changes only its RED and TRACEABILITY above #21. The witness proves stock Draft 2020-12 character-length admission can disagree with runtime UTF-8-byte limits for 65×`é` request IDs and 513×`é` command arguments. Reviews `5253486572` and `5253616141` removed arbitrary-dialect and URI-only false-GREEN paths: test-only `5d0434019e152832577e62a079f4ec5c17cbcad1` now requires the concrete canonical dialect artifact, Draft 2020-12 base, required CWL vocabulary, and byte keyword surface. Current CI `35408088920` is queued. #101/#102 remains the immutable shared byte-vocabulary prerequisite; #139 may consume only a released contract for GREEN.

Issue #136/#137 is a separate self-hosted-runner boundary: `linux-cluster-ops#326` owns s1 placement/registration and the true pre-lease enablement gate; this repository owns reusable isolation-attestation evidence. #137 exact `57052b925c7f812e72e280d695aa2929a88bd85a` requires machine-verifiable RFC1918, Redis 6379, PostgreSQL 5432, and DNS denial evidence before checkout in the positive-LSM job. In-job pre-checkout evidence remains defense in depth rather than true pre-lease proof.

### Artifact-analysis owner stack

Artifact-analysis parent #18 is actual live exact `703b4b1a047321bb08d1d6cb1de78d01cb350696`, Draft/open/mergeable. Git ref authority is the exact above. Do not copy unstable command/runtime source into artifact-analysis leaves; later foundation adoption must be ordinary/non-force and preserve every valid test/fixture/contract/TRACEABILITY delta.

- #53 dynamic-attestation exact `9d0da2a50e3247fd6cd2e52301d57408d404557d`: predecessor stopped at rustfmt before semantic assertions; current change is formatter-only, CI `35342943374`.
- #61 evidence-record identity exact `acb615de42d0c0013be47d47b8d2988197597100`: predecessor causally accepted foreign/duplicated job-bound evidence IDs; minimal `c024ecc...` binds canonical `<analysis_job_id>:evidence:<sequence>` and returns typed `InvalidEvidenceIdentity`; CI `35348644513`.
- #63 policy-boundary binding exact `b3204f8e1c694c18489b41a1c2a1591117e6e58f`: owner-local contract is focused current-head GREEN; broader failure is inherited command/runtime ancestry.
- #65 foundation-cardinality exact `3bb72c6cdbbc07823ba1c5e59f6a7b3528b29591`: formatter-only prerequisite repair, CI `35323306415`.
- #130 schema-cardinality exact `038d0ce36c04b598c246acf09a82ee2967a9aec8`: ordinary restack on current #65; production/schema remain unchanged until hardened RED executes, CI `35333036539`.
- #76 strict evidence deserialization exact `60d19a531c06f035df5291948382de7ee10484c6`: five owner-local unknown-field controls are focused GREEN; broad failure is inherited command-runtime ENTRYPOINT ancestry.
- #70 Analyzer Worker exact `991ecccabcf23380443d86873cf2777967112fbf`: Worker/Core suites are focused current-head GREEN; decoded whole-workspace failure is inherited `backend_security_info` versus expected isolation contradiction. Do not weaken worker semantics or mask with retry/sleep/mutex.
- #51/#55/#59 remain independent result/provenance/subject-binding prerequisites.
- #67 exact `010330580634fc277ae2a18e9504e2d1f8c5d712` retains the executed self-verifiability RED and adds the descriptor-identity RED: identical bytes/SHA/request/profile with distinct admitted canonical leaf identity must not alias `analysis_job_id` or job-bound evidence IDs. Docs-only current head also narrows the v1.1 changelog claim so a recomputable unsigned companion digest is not overstated as derivation proof. Current CI `35400068729` is queued.
- #102 UTF-8/dialect publication exact `517bc8a2326289a9c93c024430e9665820dc553a`: predecessor `ea2945fcc2d3291b18b6e7a5f6b683eff6117ba9` / CI `35275047055` stopped at rustfmt before the publication witness. Current exact applies only formatter output; production/schema/dialect/vocabulary semantics remain unchanged. CI `35405113169` is queued. Canonical publication paths are `docs/contracts/cwl_artifact_analysis_contract_vocabulary_1_0_0.md` and `tests/fixtures/cwl_artifact_analysis_contract_vocabulary_1_0_0_vectors.json`.
- #135 Gregorian `submitted_at` public-contract parity is exact `61d6581359c019cdea323aec8a2a707695c44af4`, Draft/open/mergeable above current #18; CI `35411589042` is queued. Review `5237628204` removed an arbitrary-dialect false GREEN. Review `5253891162` found a second false GREEN: declaring `x-cwl-rfc3339Profile` as a `string` proves syntax, not semantics. Test-only `b8666577349bc7a77d10889fda810608dd62394c` therefore reuses #102's canonical publication surface and requires normative semantics for `utc_z_only_with_gregorian_day_validation_no_leap_second_notation` plus conformance vectors for valid leap day, non-leap February 29, impossible month/day, leap-second notation, and numeric-offset substitution. Docs-only `61d6581...` records the owner boundary. Production Rust/public schema/dialect/vocabulary publication are unchanged; the first intended RED remains the stock request-schema `$schema`.

## DDD and ownership boundary

`sandbox_execution` is Core. `application_service` and `artifact_analysis` are Supporting bounded contexts. Podman/gVisor/containerd/Kubernetes/VM are infrastructure adapters. Generated names and serialized receipts are correlation/evidence unless a reviewed versioned contract grants stronger semantics. Destructive cleanup authority must be runtime-acquired, exact, private, and non-serializable. Requested configuration, backend-applied inspection, acquired backend identity, and live effective enforcement are distinct evidence levels.

Core foundation repositories are optional released contract owners rather than mandatory source dependencies. Consumers use immutable released contracts/ACLs; mutable branch dependencies, sibling source copies, and cross-service SQL are prohibited.

## Buyer-visible gaps

| Gap | Current authority | Required next evidence |
| --- | --- | --- |
| Lease-owner retry CI can be invalidated by mutating an executable fixture pathname between backend phases. | #6 `7029537...`; predecessor has assertion-level retry failure. | Execute current atomic-replacement fixture exact and require retry plus sibling ownership controls GREEN without production/idempotency change. |
| Branch coverage can false-underreport repeated codegen records. | #112 `8991d4a...`; predecessor has causal RED. | Execute current physical-source union repair and denominator-mismatch control without threshold/denominator weakening. |
| Application-service network candidate had stale create-ID test setup after network-ID admission. | #127 predecessor `7106388...` / CI `35384682678` executed the real failure; current `190762b...` / CI `35428581879`. | Execute the minimum fixture repair; require the six create-ID controls to reach their intended receipt boundary before evaluating later network/hold REDs. |
| Application-service network authority is not fully exact-ID and foreign-safe end to end. | #127 `190762b...`. | After fixture GREEN, progress provenance → cleanup authority → effective attachment → private authority → non-force termination → mandatory evidence. |
| Pre-attestation hostile-service RED can false-GREEN on arbitrary explicit entrypoint presence. | #128; review `5254951818`; current owner #127 `190762b...`. | Harden the witness to bind trusted hold identity, reject arbitrary-entrypoint substitution, and prove one-time post-attestation release/acknowledgement before any semantic GREEN. |
| Leading-dash image references need explicit Podman option termination. | #133 `4191ac0...`, CI `35384712828`, now behind moved #127. | Non-force adopt current #127 first, then execute delimiter witness and prove all parent lifecycle deltas remain intact. |
| Shared UTF-8/serialized-byte vocabulary publication has not reached its hardened publication RED. | #101/#102 `517bc8a...`, CI `35405113169`; predecessor stopped at rustfmt. | Execute current formatter-only successor; require the intended missing-publication RED, then publish the minimum versioned normative vocabulary/conformance contract. |
| Application-service public schema can admit multibyte values rejected by runtime UTF-8 byte limits; consumer RED could false-GREEN on an unrelated or URI-only dialect. | #138/#139 `3a2ed200...`, CI `35408088920`. | Execute current RED; after released #101/#102 contract, prove exact dialect/vocabulary/byte-keyword surface and boundary/overflow vectors. |
| `submitted_at` can be schema-admitted while runtime Gregorian validation rejects it, and a dialect may recognize the profile keyword without defining its semantics. | #134/#135 `61d6581...`, CI `35411589042`; review `5253891162`; test-only semantic-publication RED `b866657...`. | Execute current RED first. After #101/#102 immutable publication, extend the same released required vocabulary with normative Gregorian/Z-only/no-leap-second semantics and the required positive/hostile vectors; do not create a second dialect. |
| Future self-hosted CI may expose LAN/host services. | #136/#137 plus `linux-cluster-ops#326`. | Obtain current local RED, then reusable denial evidence plus real s1 pre-lease failed-reachability proof. |
| Dynamic analysis completeness/execution semantics are not proven end to end. | #53 current exact. | Execute hardened RED before semantic GREEN. |
| Evidence IDs can contradict enclosing job/sequence. | #61 typed candidate after causal RED. | Require exact current-head typed GREEN; do not claim stock JSON Schema cross-instance parity. |
| Analysis job identity can alias materially distinct canonical artifact descriptors. | #66/#67 `010330580...`, CI `35400068729`. | Execute descriptor witness; repair only after stable analyzer provenance can participate in versioned independently verifiable derivation. |
| EvidenceBundle schema may be weaker than Rust foundation-cardinality invariant. | #65 + #130. | Execute current RED; only then add minimal direct schema cardinality constraints. |
| Focused artifact-analysis leaves remain blocked by stale command-runtime ancestry. | #63/#76/#70 focused semantics are GREEN. | Stabilize #14/#112, then ordinary/non-force adopt into #18 and descendants; reacquire exact-head evidence. |
| Positive effective-LSM evidence is absent on release candidates. | Hosted negative evidence exists; dedicated SELinux lanes repeatedly lack runners. | Obtain exact-candidate positive effective-LSM evidence; never substitute negative fail-closed evidence. |
| Protected integration governance still admits routine administrator bypass and a generic approval gate not yet converged for the solo-maintainer owner path. | Ruleset `18156473`; `.github#1340`, `.github#772`, `.github#1351`. | Remove routine admin bypass and prove a satisfiable ordinary protected path without self-approval, direct push, or gate weakening. |
| No immutable consumer authority exists. | Protected `develop` unchanged; root central security non-terminal; Release inventory empty. | Resolve central gates, integrate normally, rerun protected-head CI/security/runtime evidence, then publish version/CHANGELOG, package/release, SBOM, provenance, reproducibility, rollback. |

## Current bounded action order

1. Let #6 exact `7029537...` / CI `35389946520` execute the atomic fixture replacement and reacquire its retry witness before descendant adoption.
2. Let #112 exact `8991d4a...` / CI `35379471155` execute physical branch-union repair without denominator/threshold weakening.
3. Let #127 exact `190762b...` / CI `35428581879` execute the minimum create-ID fixture repair. If GREEN reaches #128, harden the pre-attestation witness before production hold/release work; do not treat arbitrary `--entrypoint` presence as trusted hold evidence.
4. Non-force restack #133 on current #127 after #127's dependency-safe evidence is classified; then execute delimiter witness.
5. Let #102 exact `517bc8a...` / CI `35405113169` execute the formatter-only repair and reach its hardened shared-vocabulary publication RED; do not create GREEN publication artifacts before the cause executes.
6. Let #139 exact `3a2ed200...` / CI `35408088920` execute the twice-hardened application-service UTF-8 mismatch RED; only after #101/#102 release may it consume the versioned byte-vocabulary contract.
7. Let #135 exact `61d6581...` / CI `35411589042` execute the Gregorian-profile RED hardened through normative shared-vocabulary clauses/conformance vectors; no schema/vocabulary GREEN before causal execution, and no mutable #102 source copy.
8. Let #53/#61 and then #65/#130 execute without predecessor promotion.
9. Preserve focused GREEN semantics on #63/#70/#76; stabilize #14/#112 before ordinary adoption into #18.
10. Let #51/#55/#59 and #67 exact `010330580...` / CI `35400068729` execute independently without wake commits.
11. Let #125 exact `5bced55...` / CI `35378675229` execute the minimum Clippy repair and reacquire its own evidence.
12. Let #137 exact `57052b9...` / CI `35372627217` execute unchanged, then require separate real s1 pre-lease evidence from `linux-cluster-ops#326`.
13. Keep root #1 Draft until positive effective-LSM, central Dependency Review/CodeQL terminal evidence, ordinary protected integration, and `.github#1340` + `.github#772/#1351` governance convergence are all proven. Treat `.github#712` queue health as an operational gate for current-head evidence, not permission to rerun blindly or weaken selectors. Never use `current_user_can_bypass=always` as a release shortcut.
14. Merge/release only from one unchanged dependency-safe protected candidate satisfying every gate.
