# Product and Technical Gap Baseline

Last reviewed on 2026-09-07 KST against dependency-root PR #1 causal issue #24 RED at `0f765af1a4eea83029febee3b24c55cd7e7ce4e1`, current foundation stack through #13 `bc79f7ec972908b5a5e63fe32b1b57a360011ce3`, command-runtime PR #14 exact `4a5369de53e0b4c3b2ba446a134b5254098e7aae`, artifact-analysis issue #49 causal RED `70b49a5020037165fae9f68edcf4762a2c5b1055` / CI `34018611889` / coverage `101446928192`, current artifact-analysis parent #18 `c0647152ec052d82969b2ae078891e25e6d4d69a`, issue #66 / Draft #67 executed recomputation RED `c5541dd38258e92a670c767ec16c202d029ab07c` / CI `34060453547` / verify `101559929935`, and issue #69 / Draft #70 Analyzer Worker Execution lineage through executed missing-port RED `53fea75fdfeb82c80e4a7a82b6a69ed7b3403f03` / CI `34068781915` / verify `101582191893`, reviewed-invalid first candidate `4bc77fa89050f313f42ffccd0258bc107aeb0d17`, test-only Core-ownership/termination repair RED `f76299c27cbeef57397d166c9b9e476c0933c749`, and code-current TRACEABILITY `04ff6938303e0e6900a3f7a63d70739caf3578d8`. Protected/default authority remains `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`. This ledger distinguishes protected truth, active-PR implementation, checked-in RED evidence, causally executed RED/GREEN evidence, backend-applied configuration, live effective-runtime proof, queued/cancelled checks, and post-integration protected-head evidence. Predecessor evidence never transfers to a moved head.

## Product responsibility and DDD

Quarantine Sandbox Runtime owns reusable sandbox execution, isolation-policy enforcement, resource bounds, lease/readiness/cleanup attestation, hostile-workload execution evidence, application-service isolation, and artifact-analysis evidence. `sandbox_execution` is the Core bounded context; `artifact_analysis` and `application_service` are Supporting contexts. Podman/gVisor/containerd/Kubernetes/VM remain infrastructure adapters. Wardnet retains verdict/incident/quarantine authority. Contextual-Orchestrator and Noema retain Agent/task/tool/application authorization and capability authority. Consumers integrate through released versioned contracts/ACLs, never sibling source copies, mutable PR heads, cross-service SQL, or direct ownership of backend internals.

Current structural gaps remain bounded: issue #8 owns the future durable admission/session/recovery separation; persistence has no authority yet and requires a dedicated ADR/3NF/recovery contract before introduction; the repository name remains broader in responsibility than its security-biased name and is a pre-GA product-setting decision rather than a source-layout workaround.

## Application-service isolation

The active foundation keeps digest-pinned/no-pull images, rootless execution, read-only rootfs, bounded `/tmp`, capability drop, no-new-privileges, isolated user/PID/IPC namespaces, non-root UID/GID, CPU/RAM/PID bounds, deny-by-default networking, loopback-only publication, readiness, and cleanup as fail-closed invariants. Exact root `24526eb55cf5db48ea07079b314f7d1b676eb48d` proved Podman 4.9.3/rootless behavior on hosted Ubuntu and then correctly failed closed at `IsolationVerificationFailed { control_name: "lsm" }`; that is negative effective-LSM evidence, not positive confinement. Commit `6ad2b1c9d8f616be68dc28b35d017206f26c0787` separated the ordinary hosted negative lane from the dedicated `[self-hosted, linux, cwl-hostile-workload, selinux]` positive lane.

Three P0 application-service RED descendants remain intentionally production-free until causal execution:

- #19 binds applied `/tmp` restrictions/size and wall-time configuration and carries #45 applied UTS/cgroup namespace mode, #46 immutable image digest, and #47 direct-argv/OCI-ENTRYPOINT REDs before later live kernel/process proof.
- #21 carries #20's collision-resistant runtime invocation identity RED, #40's post-create lifecycle-ownership/create-identifier REDs, and #42's cleanup-authorization RED. #40 requires every post-create application-service `start`/inspect/top/port/stop/remove operation to remain bound to the exact long ID returned by successful `podman create`; #42 prevents caller-deserialized lease fields from becoming standalone destructive backend authority. Generated `qsr-app-*` remains correlation/public metadata rather than destructive authority.
- #23 carries #22/#23 exact effective network-attachment REDs plus #41 network-cleanup ownership and #48 acquired-network-identity. A runtime-owned network name is correlation metadata: it neither authorizes `network rm --force` to delete foreign same-principal containers nor proves that later name resolution selects the network originally created by this invocation.

Static Podman inspection is backend-applied configuration evidence, not proof of kernel enforcement. CPU/RAM/PID release claims still require authoritative cgroup-v2 evidence; `/tmp` claims require live mount evidence; wall-time requires behavioral termination/cleanup proof; network claims require acquired identity, exact attachment and real negative-egress evidence; positive LSM claims require an eligible backend.

## Artifact analysis

The active foundation keeps SHA-256 artifact identity, bounded ingestion, deterministic format classification/evidence ordering, analyzer failure attribution, and fail-closed rejection of externally supplied in-process analyzers under `src/artifact_analysis/`. Draft #18 owns issue #17's Claude-plugin package-analysis request contract and issue #49's analyzer-worker isolation boundary while preserving quarantine-sandbox-runtime as evidence/isolation owner; AppGuardrail remains static-scan/SARIF authority, Noema remains admission/activation authority, and Wardnet remains verdict/incident authority.

Issue #49 is a causally executed P0 artifact-analyzer isolation/evidence defect. On descendant exact head `70b49a5020037165fae9f68edcf4762a2c5b1055`, native CI run `34018611889`, coverage job `101446928192`, `tests/artifact_analysis_host_capability_red.rs` proved that an externally supplied `StaticAnalyzer` could reach a runtime-host loopback listener before the controller emitted no-network/no-credential/no-execution evidence. The failure text was `static analyzer reached a runtime-host network socket; the host-process trait call is not an isolation boundary`.

The first causal production repair removes that demonstrated capability exposure without pretending a worker already exists. `AnalysisEngine::new` retains validation/representation of externally supplied analyzers, but `AnalysisEngine::analyze_bytes` returns typed `IsolatedAnalyzerWorkerRequired` before invoking them in the controller process. Runtime-owned bundled non-executing format analysis uses explicit `with_bundled_static_analyzers`. ADR-0009 remains Proposed: commercial completion still requires the backend-neutral Analyzer Worker Execution port, a concrete capability-denying adapter, exact worker/analyzer/artifact/policy provenance, denied loopback/external network and ambient credential/environment access, bounded CPU/RAM/PID/time/storage/output, termination/cleanup/recovery evidence, and protected exact-head release evidence.

Issue #69 / Draft #70 owns the Analyzer Worker Execution prerequisite. The contract RED was hardened for immutable analyzer identity, exact artifact/policy binding, request-pinned isolation-policy SHA-256, non-zero CPU/RAM/PID/wall-time/scratch/output budgets, requested/applied budget equality, bounded/control-free worker/backend/version identifiers, exact 64-character lower-case hexadecimal SHA-256 values, typed normalized findings/failures, and `EvidenceRecord`-compatible per-item summary/attribute bounds. Exact `53fea75fdfeb82c80e4a7a82b6a69ed7b3403f03`, native CI `34068781915`, verify `101582191893` passed formatting and then failed in `cargo test --locked --workspace --all-targets` with E0432 unresolved imports for the complete public `AnalyzerWorker*` port; hosted negative rootless/AppArmor `101582191903` passed. This is the executed first causal RED.

The first backend-neutral candidate `4bc77fa89050f313f42ffccd0258bc107aeb0d17` was not promoted to GREEN. Exact-head review found two valid DDD/lifecycle defects. First, `artifact_analysis` defined `AnalyzerWorkerBudget` and `AnalyzerWorkerIsolationEvidence`, duplicating reusable resource, backend, denied-capability, cleanup, and isolation semantics that ADR-0009 assigns to Core `sandbox_execution`; its cumulative CPU-time milliseconds also differ from existing Core `ResourceRequest.cpu_millicores`, so those units/models cannot be treated as one vocabulary. Second, `cleanup_completed: bool` was not bound to a runtime-observed terminal state for the exact `worker_id`, so a receipt could assert cleanup without demonstrating that the same worker had actually terminated.

Repair RED `f76299c27cbeef57397d166c9b9e476c0933c749` therefore adds `tests/artifact_analysis_worker_core_boundary_red.rs` without further production changes. It requires Core-owned `SandboxWorkerBudget`, `SandboxWorkerIsolationEvidence`, `SandboxWorkerTerminationEvidence`, and `SandboxWorkerTerminationState`; forbids duplicate `AnalyzerWorkerBudget` / `AnalyzerWorkerIsolationEvidence` definitions in Supporting `artifact_analysis`; and requires receipt validation to reject a non-terminal worker or termination evidence whose worker identity differs from the runtime worker. TRACEABILITY descendant `04ff6938303e0e6900a3f7a63d70739caf3578d8` preserves the RED unchanged and records the owner split and standards/research rationale. This repair RED remains checked-in, not causal, until an exact docs-equivalent head executes it for the intended Core-ownership/termination cause. Controller-side request/receipt equality remains anti-contradiction/input-normalization evidence and is not effective confinement.

The artifact-analysis descendant controls remain independent and must survive #49/#69 repair/restack: #50/#51 bounds worker-result ingestion before trusted-controller accumulation; #52/#53 binds dynamic execution/completeness/`RuntimeBehavior` evidence; #54/#55 requires stable analyzer provenance rather than display-ID identity; #56/#57 forbids `ToolFailure` from coexisting with `Completed`; #58/#59 binds nested `ArtifactIdentity` subject SHA-256 to the top-level artifact descriptor; #60/#61 binds evidence-record identity to the enclosing analysis job; #62/#63 binds `RuntimeManifest` and `PolicyBoundary` runtime-boundary facts; #64/#65 owns structural v1 foundation-evidence cardinality without turning mutable `producer_id` text into provenance; #66/#67 owns self-verifiable analysis-job derivation. #67 exact `c5541dd38258e92a670c767ec16c202d029ab07c`, native CI `34060453547`, verify `101559929935` passed formatting and reached `cargo test`, where the hostile reconstruction that changes `request_id`, recomputes the unsigned companion digest, and retains stale opaque `analysis_job_id` is now an executed semantic RED. Because `runtime::deterministic_job_id()` also hashes configured analyzer IDs, #54/#55 stable analyzer provenance remains a prerequisite for an independently verifiable derivation; mutable `producer_id` or another attacker-recomputable checksum is not authority. Worker-dependent siblings remain Draft until a real #69 port and later capability-denying adapter can execute analyzers without returning to controller-host authority. No sibling is closed merely because the controller rejects unsafe in-process execution.

YARA-X/capa/Ghidra/LIEF adapters, dynamic Linux/Windows detonation, and durable signed evidence remain missing commercial slices. New analyzers must execute behind or strengthen the Core sandbox boundary rather than parse hostile artifacts with runtime-host network/environment/filesystem/process authority. Static evidence is not observed runtime behavior. A Rust trait, fake/in-memory worker, serialized receipt, launch flag, matching request/receipt digest, or hard-coded boolean is not proof that the exact worker was confined.

## Command-execution isolation and evidence

Draft #14 is a separate one-shot application-service contract. Each focused issue remains RED-driven; production is changed only after causal execution demonstrates the intended defect.

- #25 pre-attestation execution — current `run_command_at` binds the consumer command as the OCI process and starts it before live seccomp/LSM/capability attestation. RED `585f3d955bddfb95f28e5918cfcadcac632589df` requires no hostile payload side effect before positive proof. GREEN needs a trusted hold/attest/release phase or equivalent; static inspect reordering is insufficient.
- #26 Linux pathname identity — RED `6a3c9f4a3c886057e1de3a2f05154f81188b58aa` requires literal `a\\b` and nested `a/b` to remain distinct through staging/digest/receipt. Production still performs a lossy backslash rewrite.
- #27 result schema — RED `9fd85c0fe4f5bd57eb718b16574145862a4a2a7b` executed causally on predecessor `2d3eae1054bd49aca3e19e82a561de169e025890`; the strict result schema omitted the already-serialized optional `source_artifact_receipt`. Minimal schema candidate `9268842a9ab741448a43979f9252d10838aa5f60` added only the optional strict receipt under schema `1.0.0`. On native CI `34013162142`, both verify and coverage executed `command_execution_result_schema_red` successfully, so the focused #27 contract repair is proven within that predecessor test run. The whole predecessor is not merge-ready because later independent REDs failed.
- #28 runtime identity width — RED `850ecd1f3c5548ba9f8eec636cd47dba0e0114fd` requires at least 128 retained bits in `qsr-cmd-*`. Production generates a 128-bit nonce but retains only 16 hex characters/64 bits.
- #29 non-UTF-8 Git pathname bytes — RED `2733bd46d054ff92861d5383fcbf68fd151e77ef` requires Unix pathname bytes such as `0xff` to survive staging/digest identity. Production still requires `Path::to_str()`.
- #30 host staging confidentiality — RED `b2ac8b81898aaf3f127dffc57cbe7b59008fa256` requires the host staging root to remain owner-only while legitimate rootless container access is provided by a reviewed backend-owned handoff. Production currently widens the staging root to `0o755`.
- #31 host log-storage bound — RED `c75d02c23567dfdc0db441589c03d2df74791bad` requires one finite canonical `k8s-file` `max-size` budget. Reader retention limits do not bound hostile log growth on host storage.
- #32 exact mount set — RED `5373af88aaacba37efea7cc764a24f1cbf4851f5` requires zero unexpected mounts without source and exactly one runtime-owned restricted `/workspace` bind with source, rejecting duplicate/conflicting destinations.
- #33 create-failure cleanup ownership — RED `313aaf694f419b6ef0020c9beae70ac37680dd7b` executed for its intended cause on predecessor `9268842a9ab741448a43979f9252d10838aa5f60`, native CI `34013162142`, verify job `101432344588`: a failed create without an invocation-owned ID flowed through `rm --force --ignore <qsr-cmd-name>` and deleted the modeled pre-existing foreign container. Test repair `701fe7fdfc2829cb82ea7b8021a95e916d065181` replaces the older partial-create fixture's name authority with an exact 64-hex cidfile receipt. Minimal production candidate `d45801bbba44a536f586ed611fae60c3f03eb2ac` requests a private runtime-owned `--cidfile`; failed create with no trustworthy receipt performs no destructive name cleanup, while a valid receipt authorizes removal of exactly that ID with cleanup-failure precedence preserved. Test hardening `af3621de1f943a0618dfd30e20c2d492004a32e4` additionally requires malformed and unreadable cidfile receipts to fail closed without falling back to requested-name cleanup. `docs/doctoring/COMMAND_CREATE_CLEANUP_OWNERSHIP_TRACEABILITY.md` records the Podman/NIST evidence chain. The current #14 exact head `4a5369de53e0b4c3b2ba446a134b5254098e7aae` includes formatting-only follow-up and still requires fresh exact-head GREEN.
- #34 output encoding integrity — RED `1ab140b7c7074d47eb4dc509bdfc15c549289198` requires invalid UTF-8 stdout/stderr to fail closed instead of mutation via `String::from_utf8_lossy`. Binary-safe output requires an explicit future versioned contract.
- #35 applied tmpfs/wall-time binding — RED `83dfc0096f088febc3575e5f3dcda0c282eb734e` injects widened/executable `/tmp`, zero timeout, and request-mismatched timeout. GREEN binds exact applied configuration but does not promote it to live enforcement proof.
- #36 post-create lifecycle ownership — RED `09e4b1577d7e8df78f8d88f18226d442cd70d1e0` requires `start`/inspect/top/wait/kill/logs/remove to use the acquired long container ID rather than mutable `qsr-cmd-*` name authority after successful create. The #33 repair intentionally does not pre-empt this post-success lifecycle migration.
- #37 applied immutable image identity — RED `e23bfb0e6982169b437ae13d2f1cee29c2c59754` requires exact equality between the request digest and Podman `container inspect .ImageDigest` before command output becomes trusted evidence. `.Image`/`.ImageName` are not substitutes.
- #38 direct argv versus image ENTRYPOINT — RED `387bee4ecf61de9be1915f78c2cd43b73d7379cb` requires the complete validated requested argv, including argument boundaries containing spaces, to override image-defined ENTRYPOINT semantics rather than become arguments to an unrelated image executable. `docs/doctoring/COMMAND_ENTRYPOINT_TRACEABILITY.md` records the Podman/NIST evidence chain. Production currently appends `request.command` after the image reference without an entrypoint override.
- #39 applied UTS/cgroup namespace modes — RED `eac6b8afe998cc34171869717b882bba4002b618` independently reports `HostConfig.UTSMode=host` and `HostConfig.CgroupMode=host` while all currently checked isolation evidence remains positive. Each case must fail closed before logs are trusted and clean up the invocation-owned container. Production requests `--uts=private --cgroupns=private` but does not deserialize or bind the applied inspect fields. `docs/doctoring/COMMAND_NAMESPACE_TRACEABILITY.md` records the Podman/Linux/NIST evidence chain; real namespace-handle proof remains an E2E release gate beyond inspect configuration.
- #43 timeout termination enforcement — RED `15da2c53cea404b3fedef926ee29a1e5b31c20d9` makes the request-bounded first `podman wait` expire, makes `podman kill` fail, and makes a later wait succeed. Current production discards the kill result and can therefore report `timed_out=true` after a later natural exit. GREEN must fail closed on unproven termination before logs are trusted, while still attempting cleanup. `docs/doctoring/COMMAND_TIMEOUT_TERMINATION_TRACEABILITY.md` records the Podman/NIST evidence chain and keeps configured timeout (#35) separate from behavioral enforcement.
- #44 result-time authority — RED `ae38d9c9e594774f27943137e27a073b8743c2bc` supplies a far-future `started_at_epoch_seconds` while every current fake-Podman execution/isolation fact is otherwise positive. Current production copies the supplied value into `CommandExecutionResult` but generates completion from the runtime host clock, allowing `finished_at_epoch_seconds < started_at_epoch_seconds`. GREEN must either fail closed or move receipt-time observation behind a runtime-owned/injectable clock boundary; silently clamping contradictory timestamps is not evidence repair. `docs/doctoring/COMMAND_RESULT_TIME_TRACEABILITY.md` records NIST AU-8/SP 800-92 traceability and keeps monotonic timeout enforcement separate from wall-clock presentation evidence.

The command profile must ultimately combine #25's pre-payload proof with #32/#35/#37/#39 applied-state bindings, #33/#36 ownership, #28 collision resistance, #30 host confidentiality, #31 storage bounds, #34 evidence encoding, #38 exact process argv, #43 verified timeout termination, #44 runtime-owned chronology, and real rootless-Podman E2E. No single inspect field, launch flag, caller wall-clock value, observer timeout, or later natural exit substitutes for that integrated proof.

## Verification and release state

Protected/default `develop` is freshly verified at `60a85c7633e03b425b67159ec6822c8178cf87ea` and remains protected. Organization ruleset `18156473` is active on the default branch and requires one approval, stale-review dismissal after push, resolved review threads, seven central required workflows, and rejects deletion/non-fast-forward updates. GitHub Releases are currently absent.

Issue #24's native protected-branch CI RED executed for the intended stale-`main` trigger cause on exact root predecessor `0f765af1a4eea83029febee3b24c55cd7e7ce4e1`. Its smallest trigger repair changes only native CI `push.branches` to `[develop]`; descendants preserve that repair non-force. Post-integration native CI on the exact protected `develop` SHA is still mandatory before release authority can be claimed.

The #14 predecessor `9268842a9ab741448a43979f9252d10838aa5f60` materially advanced command evidence on native CI `34013162142`: the #27 strict result-schema regression passed in both verify/coverage, the hosted negative rootless/AppArmor lane succeeded, and #33 then failed for the intended cleanup-ownership cause. The current #14 head `4a5369de...` is a moved candidate; predecessor evidence does not transfer.

Artifact issue #49 advanced materially on descendant native CI `34018611889`: the host-capability RED executed for the intended controller-process cause. The first production repair blocks externally supplied analyzer invocation before it can inherit host capabilities while keeping runtime-owned bundled non-executing format analysis available. The repair is a safety GREEN candidate, not isolated-worker completion. Current #18 `c0647152...` native CI `34057361069` is a moved exact-head run and must be evaluated independently; predecessor checks do not transfer.

Issue #66 / #67 has crossed its reopened RED gate on exact `c5541dd...`: native CI `34060453547`, verify `101559929935` passed the formatter prerequisite and failed in `cargo test` for the hostile recomputation contract. This authorizes causal design work only after #54/#55 provides stable analyzer derivation identity; it does not authorize a checksum-only repair.

Issue #69 / #70 crossed the initial missing-port gate on exact `53fea75f...`: native CI `34068781915`, verify `101582191893` passed formatting and failed during `cargo test` on the absent public `AnalyzerWorker*` contract, while hosted negative rootless/AppArmor `101582191903` passed. The first candidate `4bc77fa...` is explicitly not GREEN because exact-head review found duplicated Core isolation semantics and missing worker-bound terminal evidence. Repair RED `f76299c...` plus docs-equivalent TRACEABILITY descendant `04ff6938...` requires Core-owned worker budget/isolation/termination/cleanup values and rejects non-terminal or wrong-worker termination evidence. The current repair lane must execute that exact unchanged test delta before production is moved again. Positive LSM and concrete adapter evidence remain separate gates.

The dependency chain remains single-writer/non-force. #6, #9, #10 and #13 preserve the foundation stack; #14 carries command-runtime issues; #18 carries the artifact-analysis foundation and Claude-plugin package request contract. Focused artifact-analysis descendants #51/#53/#55/#57/#59/#61/#63/#65/#67 remain independent. #70 is the prerequisite sibling for the Analyzer Worker Execution port. Direct RED-only #19/#21/#23 retain their owner-path deltas. No child delta is closed or discarded, and predecessor checks never transfer.

The first immutable release remains blocked until one unchanged integrated protected candidate has exact native CI, 100% owned production statement/function/region/branch and edge-case coverage where tooling exposes it, public rustdoc, real rootless isolation E2E, positive LSM/seccomp/capability/resource/network/cleanup evidence, required review/security/SAST/dependency gates, package smoke, SPDX SBOM, provenance, checksum/signature where supported, reproducibility, recovery/rollback proof, and release automation sourced from protected `develop`. Artifact-analysis release evidence must additionally prove that external analyzer code executes behind a bounded capability-denying worker rather than the runtime host process. GitHub Releases remain absent; mutable PR heads are not consumer authority.

## Next bounded slices

1. Obtain causal execution of #69/#70's Core-ownership/termination repair RED on one exact head preserving `tests/artifact_analysis_worker_core_boundary_red.rs` unchanged. If formatting/compile/review prerequisites fail first, repair only that cause and rerun before production changes.
2. After causal RED, move reusable worker budget/isolation/termination/cleanup values into Core `sandbox_execution`; make the `artifact_analysis` request/receipt compose those Core values; remove duplicate `AnalyzerWorkerBudget` / `AnalyzerWorkerIsolationEvidence`; bind terminal evidence to the exact worker identity; preserve existing analyzer/artifact/policy/isolation-policy and requested/applied budget contradiction checks. Keep #50 as aggregate result-channel owner. Do not conflate cumulative CPU-time milliseconds with Core service CPU-rate millicores.
3. Add a concrete rootless capability-denying adapter behind the repaired port and prove denied host-loopback/external network, ambient credentials/environment, broad host filesystem/runtime sockets and uncontrolled subprocess authority; prove CPU/RAM/PID/time/storage/output bounds plus runtime-owned termination/cleanup on a real eligible runner. Port-level GREEN is not adapter/release GREEN.
4. Non-force adopt the final verified moved #18/#69 foundation into worker-dependent #51/#53/#55/#57 and preserve #59/#61/#63/#65/#67 evidence-contract deltas without transferring predecessor checks.
5. Obtain fresh exact-head execution for the #33 cidfile ownership repair and continue #19/#21/#23 application-service REDs without bypassing owner paths.
6. Merge only after one unchanged exact candidate satisfies review, security, coverage, real runtime and positive-LSM gates; then verify native CI on the exact protected integration SHA.
7. Publish the first immutable runtime release only from that integrated protected authority and hand released version/digest pinning to consumer owner paths.
