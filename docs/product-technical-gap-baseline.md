# Product and Technical Gap Baseline

Last reviewed on 2026-09-11 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`, current command-contract parent #13 `bc79f7ec972908b5a5e63fe32b1b57a360011ce3`, and canonical command-runtime PR #14. The #71/#72 process-boundary prerequisite is now causally adapted into #14 rather than copied wholesale from the older #72 tree: RED `102dbf7ead72a4a0d7e87f6a51684ba5c917bee2` proved the bounded public Spawn taxonomy was absent; production `50644233eeea8c9f3a1a4843ea999d863c3f6249` → `d98127705cf3f1221f2b3e3641d4595e32aaf14a` → `db7f1d9d6477c8ffef0899f73f6f13ab64ff45f5` → `6d7874409e20975d9db44041210670dc715d7e25` preserves OS `ErrorKind` only at Spawn, exposes bounded `BackendInvocationFailureKind`, maps actual Spawn to `BackendSpawnFailed`, and keeps Wait/Capture generic. Exact `40b923c61e1a4727fac67cbfd0e1304d91eff956`, CI `34335710292`, then made previously rotating process-creation failures observable as bounded `Other` in different hosted fixtures. The root cause was repository test infrastructure that created or rewrote executable shell-script inodes during concurrent process creation, not a reason to weaken leaf cleanup/isolation/ownership contracts. The immutable-fixture lineage `0d0173727cd600533ab9291ed2c42040f65be259` → `b12360435341b61c16851ed93ce0efc1b2aa2860` → `f9f3d87509ea9c9ee633fb2f76b2b9b4091b7442` removed that hazard from command-cleanup/application-service ownership fixtures. Exact `e94e6043bb7df0d557ed7d5f3d142ee62baed1a4`, CI `34337359199`, proved immutable UTS/cgroup namespace fixtures but exposed the same stale pattern in post-create ownership; `fc94073a95e359d57537225b43ae28119e340e52` and `a9c0aa0c01b56f09c3f77dd541ca58d8a72945a8` moved that regression to an immutable executable. Exact `a9c0aa0...`, CI `34338451401`, then rotated `BackendSpawnFailed { operation: "backend_security_info", failure_kind: Other }` through the remaining shared `tests/podman_command_execution.rs` helper: verify `102423292005` failed the start-failure case before container start, while production coverage `102423291940` failed the malformed-create case before create parsing and 14 neighboring command tests passed. Test-only `b7496f7206ce13bedf71987cb4fd1aeb23041142` adds a data-sourced `source_script` dispatcher to checked-in immutable `tests/fixtures/fake_podman.sh`; `2ff079ef47d0c5fd38ac90bf2cf81d4aa203b1e6` changes the shared helper to create only a unique symlink plus `.script`/`.config` data sidecars. `3750fd2a8f713c2202eb2747533a41cc186aa3a1` rolls the exact RCA into `BACKEND_SPAWN_FAILURE_TRACEABILITY.md`. No production Rust changed in this final fixture conversion. The same owner lineage retains checkout-credential disposal through `ba0826c6b4739a3cafb5c955fac00688a690ded4` and `4b6f7ff121dfdd671decfafd45413b5c7bc4814d`. No predecessor GREEN transfers; the moved exact head must reacquire every test, coverage, review, positive-LSM, security and protected-integration gate. The ledger distinguishes protected truth, active-PR implementation, causal RED, minimum candidate, backend-applied configuration, live effective-runtime proof, queued/cancelled checks, and post-integration protected-head evidence.

### Current exact command-runtime authority

PR #14 first converted issue #25 into an executed causal P0 on exact `ed9318ba96d876341866d84a892d0145e12f6469`, native CI `34340070150`: verify `102428599037` and branch coverage `102428599228` both reached `podman_command_execution_pre_attestation_red::command_payload_is_not_runnable_before_effective_process_attestation` after the typed process-boundary and preceding command-runtime/application-service suites passed. The hostile fixture observed the consumer payload side effect after `podman start` and before effective process attestation failed. Hosted negative rootless/AppArmor `102428599461` was GREEN; dedicated positive-LSM was not acceptance evidence. Cleanup after that point could not undo payload execution.

The next owner-path RED narrowed a useful earlier hold point without closing the P0. Test-bearing `9e36766abc0a553f45c62fa327b34f1315dc0db9`, executed after immutable-fixture prerequisites on exact `eb2330ffb6689b07706a305e55c81871414e9fe5` / native CI `34353894509`, showed that production never invoked `podman init`: an unavailable init primitive did not fail closed, invalid configured isolation was not rejected while the container was still held, and both cases reached `start`/payload side effects. Minimum production `40313a2a8fe058f3cb25d3580b4f1fecbb4ec2e1` plus formatter-only `f1a037931ecc7fcbd6e87836f8fb7048c2c3544e` now performs `create -> acquire exact ID -> podman init -> pre-start static/configuration verification -> start -> live process verification`. Static checks are shared pre/post start but remain configured-state evidence, not effective seccomp/LSM/capability proof.

Backend feasibility is no longer hypothetical. Exact `7da7d3b36ec8830ab16d776e5ed1a93f7a16b8ad`, CI `34379177369`, hosted negative rootless/AppArmor job `102559489017` proved on Ubuntu 24.04 / rootless Podman 4.9.3 that a static runtime-owned gate can be mounted read-only as `/qsr-runtime-gate`, remain the only running process while the consumer is held, expose effective seccomp/capability/ambient/label evidence, preserve its SHA-256, and `exec()` the exact consumer argv only after explicit release. Packaging then received its own causal test: exact `304d63c2722a35b9f05f8466bbea81492a1533a6`, CI `34381329199`, verify `102566637062` failed because the Cargo package exposed no `qsr_runtime_gate`. Minimum production `3bd5d4319aba7c23982bbd18c77d424c25c3a510` adds the bounded fail-closed gate binary; exact CI `34381498621` made `command_hold_gate_packaging_red` GREEN in verify `102567695133` and statement coverage `102567694962`, then reached the unchanged original #25 pre-attestation RED. Hosted negative rootless/AppArmor `102567695588` was GREEN. Follow-up `a65381d50013f42c91b796e71755048ab3b7c584` changes the real held-gate E2E to compile production `src/bin/qsr_runtime_gate.rs` as `x86_64-unknown-linux-musl`; `013aeade4ae3a7fa86484c82b4bc9336c0e7e4ad` removes the duplicate test-only gate fixture; `09d922addd1e025fd7a3d62f6d00a41eaed8edca` updates traceability. These moved heads require fresh exact CI before production-gate real-runtime reuse is claimed GREEN.

Subsequent fixture work made the immutable fake backend init-capable and repaired the deterministic start-failure fixture so its intended `container_start` error is reached after valid pre-start inspection. The one-purpose source materializer that applied that fixture repair has been removed after the source delta was materialized. The current owner therefore contains the repaired test without retaining a self-modifying workflow. Proposed ADR-0008 and `docs/doctoring/COMMAND_HOLD_ATTEST_RELEASE_TRACEABILITY.md` now record the init-held partial boundary, real held-gate capability, packaged-gate RED/GREEN, and remaining integration limit. Full #25 remains open: production `run_command_at` still binds consumer argv directly as the OCI entrypoint, and a host-built default Cargo binary is not the immutable architecture-compatible artifact contract required for arbitrary hostile images.

## Product responsibility and DDD

Quarantine Sandbox Runtime owns reusable sandbox execution, isolation-policy enforcement, resource bounds, lease/readiness/cleanup attestation, hostile-workload execution evidence, application-service isolation, and artifact-analysis evidence. `sandbox_execution` is the Core bounded context; `artifact_analysis` and `application_service` are Supporting contexts. Podman/gVisor/containerd/Kubernetes/VM remain infrastructure adapters. Wardnet retains verdict/incident/quarantine authority. Contextual-Orchestrator and Noema retain Agent/task/tool/application authorization and capability authority. Consumers integrate through released versioned contracts/ACLs, never sibling source copies, mutable PR heads, cross-service SQL, or direct ownership of backend internals.

Current structural gaps remain bounded: issue #8 owns the future durable admission/session/recovery separation; persistence has no authority yet and requires a dedicated ADR/3NF/recovery contract before introduction; the repository name remains broader in responsibility than its security-biased name and is a pre-GA product-setting decision rather than a source-layout workaround.

## Application-service isolation

The active foundation keeps digest-pinned/no-pull images, rootless execution, read-only rootfs, bounded `/tmp`, capability drop, no-new-privileges, isolated user/PID/IPC namespaces, non-root UID/GID, CPU/RAM/PID bounds, deny-by-default networking, loopback-only publication, readiness, and cleanup as fail-closed invariants. Exact root `24526eb55cf5db48ea07079b314f7d1b676eb48d` proved Podman 4.9.3/rootless behavior on hosted Ubuntu and then correctly failed closed at `IsolationVerificationFailed { control_name: "lsm" }`; that is negative effective-LSM evidence, not positive confinement. Commit `6ad2b1c9d8f616be68dc28b35d017206f26c0787` separated the ordinary hosted negative lane from the dedicated `[self-hosted, linux, cwl-hostile-workload, selinux]` positive lane.

Three P0 application-service RED descendants remain intentionally production-free until causal execution:

- #19 binds applied `/tmp` restrictions/size and wall-time configuration before any later live cgroup/mount/watchdog proof.
- #21 carries #20's collision-resistant runtime invocation identity RED, #40's post-create lifecycle-ownership RED, and #42's cleanup-authorization RED. #40 requires every post-create application-service `start`/inspect/top/port/stop/remove operation to remain bound to the exact long ID returned by successful `podman create`; #42 prevents caller-deserialized lease fields from becoming standalone destructive backend authority. Generated `qsr-app-*` remains correlation/public metadata rather than destructive authority.
- #23 carries #22/#23 exact effective network-attachment REDs plus #41 network-cleanup ownership. A runtime-owned network name does not authorize `network rm --force` to delete foreign same-principal containers attached to that network.

Static Podman inspection is backend-applied configuration evidence, not proof of kernel enforcement. CPU/RAM/PID release claims still require authoritative cgroup-v2 evidence; `/tmp` claims require live mount evidence; wall-time requires behavioral termination/cleanup proof; network claims require exact attachment plus real negative-egress evidence; positive LSM claims require an eligible backend.

## Artifact analysis

The active foundation keeps SHA-256 artifact identity, bounded ingestion, deterministic format classification/evidence ordering, analyzer failure attribution, and no execution in static paths under `src/artifact_analysis/`. Draft #18 owns artifact-analysis RED descendants while preserving quarantine-sandbox-runtime as evidence/isolation owner; AppGuardrail remains static-scan/SARIF authority and Noema remains admission/activation authority. YARA-X/capa/Ghidra/LIEF adapters, dynamic Linux/Windows detonation, and durable signed evidence remain missing commercial slices and must reuse or strengthen the Core sandbox boundary rather than execute hostile bytes in the Rust control process. Artifact-analysis descendants must non-force adopt the final #14 foundation while preserving their own tests, doctoring and contract deltas.

## Command-execution isolation and evidence

Draft #14 is a separate one-shot application-service contract. Each focused issue remains RED-driven; production is changed only after causal execution demonstrates the intended defect.

- #25 pre-attestation execution — original RED `585f3d955bddfb95f28e5918cfcadcac632589df` executed causally on exact `ed9318ba96d876341866d84a892d0145e12f6469`, native CI `34340070150`: the hostile fixture proved the consumer became runnable at `podman start` before live attestation. The later init-hold RED `9e36766abc0a553f45c62fa327b34f1315dc0db9`, executed on exact `eb2330ffb6689b07706a305e55c81871414e9fe5` / CI `34353894509`, proved the missing `podman init` hold; minimum source `40313a2a8fe058f3cb25d3580b4f1fecbb4ec2e1` + formatter `f1a037931ecc7fcbd6e87836f8fb7048c2c3544e` now rejects init unavailability and invalid configured isolation before start. Real backend capability is GREEN on exact `7da7d3...` / hosted job `102559489017`. Packaging RED `304d63c2722a35b9f05f8466bbea81492a1533a6` / verify `102566637062` causally proved the runtime package had no gate; minimum production `3bd5d4319aba7c23982bbd18c77d424c25c3a510` packages `qsr_runtime_gate` and exact CI `34381498621` made that packaging regression GREEN before the unchanged original P0 failed. Current follow-up compiles that production source, not a duplicate fixture, in the real rootless held-gate E2E and removes the duplicate fixture. Full GREEN still requires static/versioned architecture-compatible delivery and digest binding, `run_command_at` gate integration, bounded one-time release, the original payload-side-effect RED GREEN on the same implementation, and dedicated positive-LSM E2E. See `docs/doctoring/COMMAND_HOLD_ATTEST_RELEASE_TRACEABILITY.md` and Proposed ADR-0008.
- #26 Linux pathname identity — RED `6a3c9f4a3c886057e1de3a2f05154f81188b58aa` requires literal `a\\b` and nested `a/b` to remain distinct through staging/digest/receipt. Production still performs a lossy backslash rewrite.
- #27 result schema — RED `9fd85c0fe4f5bd57eb718b16574145862a4a2a7b` executed causally on predecessor `2d3eae1054bd49aca3e19e82a561de169e025890`; the strict result schema omitted the already-serialized optional `source_artifact_receipt`. Minimal schema candidate `9268842a9ab741448a43979f9252d10838aa5f60` added only the optional strict receipt under schema `1.0.0`. On native CI `34013162142`, both verify and coverage executed `command_execution_result_schema_red` successfully, so the focused #27 contract repair is proven within that predecessor test run. The whole predecessor is not merge-ready because later independent REDs failed.
- #28 runtime identity width — RED `850ecd1f3c5548ba9f8eec636cd47dba0e0114fd` requires at least 128 retained bits in `qsr-cmd-*`. Exact predecessor `8a00d3c4c1018a4a17cbc8c6f5b54505c3699beb` / native CI `34250672865` executed the collision-width regression and exposed the 16-hex/64-bit suffix. Minimum source repair `4a59aaef4876b80e58782067f7737b110a3ebe77` retains 32 lowercase hex characters / 128 bits from the existing SHA-256 result while preserving the runtime-generated 128-bit nonce and lifecycle references. On native CI `34251341897` verify `102146265381`, the runtime-identity race/width regression passed before the run later failed independently at #37. That focused pass does not transfer to moved heads or constitute merge authority.
- #29 non-UTF-8 Git pathname bytes — RED `2733bd46d054ff92861d5383fcbf68fd151e77ef` requires Unix pathname bytes such as `0xff` to survive staging/digest identity. Production still requires `Path::to_str()`.
- #30 host staging confidentiality — RED `b2ac8b81898aaf3f127dffc57cbe7b59008fa256` requires the host staging root to remain owner-only while legitimate rootless container access is provided by a reviewed backend-owned handoff. Production currently widens the staging root to `0o755`.
- #31 host log-storage bound — strengthened RED `0add9f117f39f78b04526d4c775476b3587ae965` requires exactly one positive finite runtime-owned `--log-opt max-size=<value>` for `k8s-file`; the later `podman logs` retained-output budget is not a host-disk bound. On exact `72053f437a1a834e79f8db5f9d8701abc3d1a0e6`, CI `34261831371` verify `102181436826` passed repository validation, coverage-parser tests, formatting, #38, #28 and #37, then failed exactly because command-container creation supplied no finite `max-size`. Minimum source candidate `789a0f1f14dec1b5c743411fd45f19cd467771d5` adds one backend-owned 1 MiB `max-size=1048576b` beside `--log-driver=k8s-file`; `712f4ba01b138866c06be1f9a62d6d05bd2dec68` adds `COMMAND_LOG_STORAGE_TRACEABILITY.md`. This is not the full #31 GREEN: real rootless-Podman log-flood behavior, bounded host growth, timeout/cleanup, and explicit runtime-side truncation/incompleteness evidence remain required so rotation/loss cannot masquerade as complete output.
- #32 exact mount set — RED `5373af88aaacba37efea7cc764a24f1cbf4851f5` executed causally on exact `d6e2e8a11acd738cb1a4db4cb4e0234067dd445a`, CI `34271419713`, verify `102213674293`. With no source artifact, fake `container inspect` exposed an unrequested `/etc` → `/unexpected-host` bind while all other modeled security evidence was positive; production returned success instead of `IsolationVerificationFailed { control_name: "command_mount_set" }`. Minimum source candidate `e5c6bfa843a537e3a2b330bd40ac21d5d285d481` requires effective `Mounts` cardinality to be zero without source and exactly one with source before existing exact `/workspace` source/type/read-only/`noexec,nosuid,nodev` checks run. This rejects extra and duplicate effective mounts without treating requested argv as applied-state proof. `docs/doctoring/COMMAND_MOUNT_SET_TRACEABILITY.md` records the evidence and primary Podman/NIST basis. Full #32 acceptance still requires source-bearing extra/duplicate cases, issue #25 pre-payload attestation, real rootless-Podman mount behavior/cleanup, exact-head coverage, review and positive LSM.
- #33 create-failure cleanup ownership — RED `313aaf694f419b6ef0020c9beae70ac37680dd7b` executed for its intended cause on predecessor `9268842a9ab741448a43979f9252d10838aa5f60`, native CI `34013162142`, verify job `101432344588`: a failed create without an invocation-owned ID flowed through `rm --force --ignore <requested-name>` and deleted the modeled pre-existing same-name foreign resource. Test repair `701fe7fdfc2829cb82ea7b8021a95e916d065181` replaces the older false-GREEN fixture's name authority with an exact 64-hex ID written to the runtime-provided cidfile. Minimal production candidate `d45801bbba44a536f586ed611fae60c3f03eb2ac` requests a private runtime-owned `--cidfile`, performs no destructive cleanup when failed create yields no trustworthy receipt, and removes only the acquired exact ID when a valid receipt exists while preserving cleanup-failure precedence. `docs/doctoring/COMMAND_CREATE_CLEANUP_OWNERSHIP_TRACEABILITY.md` records the Podman/NIST evidence chain. This candidate still requires fresh exact-head GREEN.
- #34 output encoding integrity — RED `1ab140b7c7074d47eb4dc509bdfc15c549289198` executed causally on exact `d99feb23f44868f6fcde08725cf2f9551b7f529e`, native CI `34274575395`, verify `102224306639`: otherwise-positive fake Podman cases emitting invalid UTF-8 independently on stdout and stderr both reached `Ok(CommandExecutionResult)` because `String::from_utf8_lossy` inserted replacement text. Minimum source candidate `118a025c8a4fe5747d04b8910c2e0958965aebc8` adds `CommandExecutionError::InvalidOutputEncoding { stream }`, strengthens the regression to that exact non-sensitive error plus cleanup, and uses strict `String::from_utf8` after successful cleanup. `docs/doctoring/COMMAND_OUTPUT_ENCODING_TRACEABILITY.md` records the decision and Rust/RFC 8259 basis. Binary-safe stdout/stderr remains a separately versioned follow-on contract; schema `1.0.0` stays text-only.
- #35 applied tmpfs/wall-time binding — RED `83dfc0096f088febc3575e5f3dcda0c282eb734e` injects widened/executable `/tmp`, zero timeout, and request-mismatched timeout. GREEN binds exact applied configuration but does not promote it to live enforcement proof.
- #36 post-create lifecycle ownership — causal RED executed on `bd5382d2df32d4f006f72314a8257417f6ac18e8`, native CI `34280048795`, branch coverage: successful create returned an invocation-owned long ID, but name-addressed lifecycle operations could be rebound to a foreign same-principal resource and the regression ended in `Backend(CleanupFailed)`. Minimum source `85b667b0bd3d5ea8bd7f0593ae91c2df12623579` migrates `start`/inspect/top/wait/kill/logs/remove to the acquired ID while preserving generated `qsr-cmd-*` as correlation/public metadata and #33 as pre-acquisition create-failure authority. `770957179e5cd14b074894ad08a97ef16346dab8` adds `COMMAND_LIFECYCLE_OWNERSHIP_TRACEABILITY.md`. The later identity-width concurrency test was stale because it still compared cleanup names to generated names and collected concurrent shell-process evidence through one append file; `c6f3dce3ecef611a5429b92f2c56afbbe6b43472` repairs only that fixture by generating per-invocation acquired IDs and recording create/acquire/remove facts in independent marker files. Fresh exact-head CI is required before #36 or #28 can be claimed integrated GREEN.
- #37 applied immutable image identity — hostile RED `e23bfb0e6982169b437ae13d2f1cee29c2c59754` executed causally on exact `4a59aaef4876b80e58782067f7737b110a3ebe77`, native CI `34251341897`, verify `102146265381`: the request pinned `sha256:eeee…`, Podman inspection reported `ImageDigest: sha256:ffff…`, yet `run_command_at` returned `Ok(CommandExecutionResult)` and collected `must-not-be-trusted` output. Minimum source candidate `8f3f7f639c9bb04a6e49593663fe5fef787794f5` deserializes optional `ImageDigest` and rejects a supplied value unless it exactly equals the already-validated digest portion of `CommandExecutionRequest.image_reference` before wait/log evidence is trusted. `fa83221d84f407731fce935bb1b9c8e07f0d25b6` updates `COMMAND_IMAGE_DIGEST_TRACEABILITY.md`. On exact `72053f437a1a834e79f8db5f9d8701abc3d1a0e6` verify `102181436826`, the supplied-mismatch regression passed before #31 failed. This is not the full #37 GREEN: a missing `ImageDigest` remains tolerated so existing fake fixtures and application-service issue #46 are not silently rewritten as positive proof. Missing/malformed fail-closed behavior, fixture migration, and real rootless-Podman equality remain required.
- #38 direct argv versus image ENTRYPOINT — test `387bee4ecf61de9be1915f78c2cd43b73d7379cb` has canonical owner-path causal execution. On PR #14 exact `4a5369de53e0b4c3b2ba446a134b5254098e7aae`, re-run coverage job `102139910936` passed the earlier cleanup prerequisite suite and failed exactly because the observed `podman create` argv contained the digest image followed by `/usr/bin/tool`, `argument with spaces`, and `--flag=value`, with no `--entrypoint`. Minimum production candidate `daf4d07820fbfe3abcefc39f0e8ce2443138e385` encodes the complete already-validated command as a compact JSON-array `--entrypoint` before the image and removes the trailing duplicate command layer. On exact `4a59aaef...` and again on `72053f437...`, the focused ENTRYPOINT regression passed before later independent REDs. `docs/doctoring/COMMAND_ENTRYPOINT_TRACEABILITY.md` records the Podman/NIST chain. Real effective-process proof with a non-empty image ENTRYPOINT is still a release gate; application-service issue #47 remains separate.
- #39 applied UTS/cgroup namespace modes — RED `eac6b8afe998cc34171869717b882bba4002b618` independently supplies `HostConfig.UTSMode=host` and `HostConfig.CgroupMode=host` while all other modeled isolation evidence remains positive. Current production deserializes both applied fields and rejects supplied non-private values. On exact `d99feb23f44868f6fcde08725cf2f9551b7f529e`, CI `34274575395`, verify `102224306639` both explicit-host regressions passed before the later independent #34 RED, establishing focused exact-hosted GREEN for that first candidate. #39 remains open because missing fields are still accepted by an optional compatibility bridge; the next RED must omit each field, positive fixtures must declare authoritative `UTSMode="private"` / `CgroupMode="private"`, and acceptance must then require both fields exactly private. `docs/doctoring/COMMAND_NAMESPACE_TRACEABILITY.md` records the Podman/Linux/NIST evidence chain; real namespace-handle proof remains an E2E release gate beyond inspect configuration.
- #43 timeout termination enforcement — RED `15da2c53cea404b3fedef926ee29a1e5b31c20d9` makes the request-bounded first `podman wait` expire, makes `podman kill` fail, and makes a later wait succeed. Current production discards the kill result and can therefore report `timed_out=true` after a later natural exit. GREEN must fail closed on unproven termination before logs are trusted, while still attempting cleanup. `docs/doctoring/COMMAND_TIMEOUT_TERMINATION_TRACEABILITY.md` records the Podman/NIST evidence chain and keeps configured timeout (#35) separate from behavioral enforcement.
- #44 result-time authority — RED `ae38d9c9e594774f27943137e27a073b8743c2bc` supplies a far-future `started_at_epoch_seconds` while every current fake-Podman execution/isolation fact is otherwise positive. Current production copies the supplied value into `CommandExecutionResult` but generates completion from the runtime host clock, allowing `finished_at_epoch_seconds < started_at_epoch_seconds`. GREEN must either fail closed or move receipt-time observation behind a runtime-owned/injectable clock boundary; silently clamping contradictory timestamps is not evidence repair. `docs/doctoring/COMMAND_RESULT_TIME_TRACEABILITY.md` records NIST AU-8/SP 800-92 traceability and keeps monotonic timeout enforcement separate from wall-clock presentation evidence.
- #71/#72 backend invocation taxonomy — the rotating generic `backend_security_info` failures are now represented by a bounded discriminator on the canonical #14 owner. Causal RED `102dbf7ead72a4a0d7e87f6a51684ba5c917bee2` proved the public types were absent. Production through `6d7874409e20975d9db44041210670dc715d7e25` preserves `std::io::ErrorKind` only for process Spawn, maps it to `NotFound | PermissionDenied | ResourceExhausted | Other`, classifies missing pipes as Capture, and leaves Wait/Capture generic. Exact hosted evidence subsequently identified runtime-created executable test fixtures as the source of rotating Spawn/Other precondition failures. The immutable-fixture repairs remove that test hazard without changing production error semantics. `docs/doctoring/BACKEND_SPAWN_FAILURE_TRACEABILITY.md` records decision, alternatives, risk, exact CI evidence, and APA 7 references.

The command profile must ultimately combine #25's pre-payload proof with #31/#32/#35/#37/#39 applied/resource-state bindings, #33/#36 ownership, #28 collision resistance, #30 host confidentiality, #34 evidence encoding, #38 exact process argv, #43 verified timeout termination, #44 runtime-owned chronology, the #71/#72 causal process discriminator, and real rootless-Podman E2E. No single inspect field, launch flag, caller wall-clock value, observer timeout, later natural exit, finite log-file setting, optional inspection field, or generic backend error substitutes for that integrated proof.

## Verification and release state

Protected/default `develop` remains `60a85c7633e03b425b67159ec6822c8178cf87ea` and is reported by GitHub as protected.

Issue #24's native protected-branch CI RED executed for the intended stale-`main` trigger cause on exact root predecessor `0f765af1a4eea83029febee3b24c55cd7e7ce4e1`. Its smallest trigger repair changes only native CI `push.branches` to `[develop]`; descendants preserve that repair non-force. Post-integration native CI on the exact protected `develop` SHA is still mandatory before release authority can be claimed.

The command-runtime evidence has advanced in causal order without transferring predecessor GREEN. #38 failed causally on `4a5369de53e0b4c3b2ba446a134b5254098e7aae` and was minimally repaired by `daf4d07820fbfe3abcefc39f0e8ce2443138e385`. Exact `4a59aaef4876b80e58782067f7737b110a3ebe77` / CI `34251341897` then passed #38 and #28 before failing causally at #37. The #37 supplied-mismatch candidate `8f3f7f639c9bb04a6e49593663fe5fef787794f5`, traceability `fa83221d84f407731fce935bb1b9c8e07f0d25b6`, and baseline `72053f437a1a834e79f8db5f9d8701abc3d1a0e6` were re-executed in CI `34261831371`: verify `102181436826` passed repository validation, coverage-parser tests, formatting, #38, #28 and the #37 mismatch regression, then failed causally at #31 because no finite command-log `max-size` existed. The later #31 source and fixture repairs advanced PR #14 to exact `d6e2e8a11acd738cb1a4db4cb4e0234067dd445a`, where CI `34271419713` passed formatting and the preceding command suites, then failed causally at #32's no-source unexpected mount. The #32 repair and namespace work subsequently advanced exact `d99feb23f44868f6fcde08725cf2f9551b7f529e`; CI `34274575395` passed the #32 path and both #39 explicit-host cases before failing causally at #34's two invalid-UTF-8 output cases. Minimum #34 source `118a025c8a4fe5747d04b8910c2e0958965aebc8`, traceability `d331304cdbb86ca8f2276f5c8e5058bbb7631374`, and later #36 ownership work all require fresh exact-head evidence; no earlier head supplies merge authority.

Exact `770957179e5cd14b074894ad08a97ef16346dab8`, native CI `34280871733`, did not establish integrated GREEN after #36. Verify passed exact checkout/dependency/repository/coverage-parser/rustfmt and then failed `podman_application_service::malformed_port_mappings_fail_closed_after_cleanup` with `BackendInvocationFailed { operation: "backend_security_info" }`; branch coverage independently failed `podman_command_execution::run_command_at_cleans_up_when_isolation_verification_fails` with the same public operation while the same application-service suite passed there; production coverage passed the application-service suite but failed the concurrent identity test because its shared append log observed only one of two create traces. These cross-job differences are not evidence for a deterministic application-service semantic defect or a specific host errno. They prove that the moved head lacks full exact-head stability and that the identity fixture was stale relative to #36. `c6f3dce3ecef611a5429b92f2c56afbbe6b43472` repairs that definite fixture defect without changing production.

The bounded-invocation owner path is no longer merely a downstream prerequisite. Exact #14 RED `102dbf7ead72a4a0d7e87f6a51684ba5c917bee2` / CI `34327610116` established that the current public API erased Spawn taxonomy: branch coverage reached the regression and failed to compile because `BackendInvocationFailureKind` and `BackendSpawnFailed` did not exist. Verify separately found a formatting defect in the new test, repaired by `d966505e7519b6708132ae30840f4f4e67330665`; that repair also changed stale `rootless_probe` wording to the current `backend_security_info` operation. The minimum production chain through `b36f6c6925119d1c65d41d5d08841541ad8f2762` adapts only the #72 process boundary into the current owner and preserves all intervening #14 runtime semantics. Exact `40b923c...` then used the new taxonomy to identify rotating Spawn/Other failures in runtime-created executable fixtures rather than guessed leaf defects. Immutable-fixture work through `2ff079ef47d0c5fd38ac90bf2cf81d4aa203b1e6` now removes that repository-side hazard from the shared command helper as well as the previously converted cleanup/ownership/namespace cases. `3750fd2a8f713c2202eb2747533a41cc186aa3a1` records the complete RCA. Exact `ed9318ba96d876341866d84a892d0145e12f6469` / CI `34340070150` then progressed past those process/fixture boundaries and converted #25 into the next real causal P0: verify and branch coverage both observe payload execution before live attestation. This evidence is intentionally preserved as RED; no predecessor or sibling GREEN can waive it.

The same canonical owner subsequently executed the `podman init` hold-capability RED on exact `eb2330ffb6689b07706a305e55c81871414e9fe5` / CI `34353894509` and adopted the minimum `40313a2a8fe058f3cb25d3580b4f1fecbb4ec2e1` repair. The owner now rejects init unavailability and invalid configured isolation before start, while retaining post-start live attestation. This advances #25 but deliberately does not convert it to integrated GREEN: effective-process proof still occurs after `start`, and the required runtime-owned hold/attest/release plus positive-LSM acceptance remains outstanding. The deterministic start-failure fixture and formatter drift exposed by the new order were repaired without weakening production, and the temporary source-materialization workflow/script were removed after their source delta was committed.

The hold-gate lineage now adds two bounded truths without masking the P0. First, real rootless Podman can hold a trusted gate under the final process controls and expose effective evidence before consumer `exec`. Second, test-only `304d63c2722a35b9f05f8466bbea81492a1533a6` causally failed because no packaged gate existed, while production `3bd5d4319aba7c23982bbd18c77d424c25c3a510` made that packaging regression GREEN in exact CI `34381498621` before the original #25 RED failed. The current descendant replaces the duplicate E2E gate fixture with production `src/bin/qsr_runtime_gate.rs` compiled as static x86_64 musl and records the evidence in `COMMAND_HOLD_ATTEST_RELEASE_TRACEABILITY.md`. It does not yet prove static release packaging, architecture selection, digest-bound delivery, Podman-adapter adoption, positive LSM, or integrated release authority.

The earlier #14 predecessor `9268842a9ab741448a43979f9252d10838aa5f60` materially advanced command evidence on native CI `34013162142`: the #27 strict result-schema regression passed in both verify/coverage, the hosted negative rootless/AppArmor lane succeeded, and #33 then failed for the intended cleanup-ownership cause. Coverage also observed one isolated `BackendInvocationFailed { operation: "backend_security_info" }` in `cleanup_failure_is_not_hidden_behind_effective_isolation_failure` while neighboring cleanup cases passed. Later #38/#37/#31/#32/#39 executions passed that cleanup path, so the isolated backend-security-info specimen is not promoted to those production causes.

The dependency chain remains single-writer/non-force. #6, #9, #10 and #13 preserve the foundation stack; #14 carries command-runtime #25–#44 plus #71/#72's bounded process discriminator and the current ownership/evidence repair lineage. #18 and artifact-analysis sibling descendants must adopt the final stabilized #14 head non-force while preserving their own valid deltas. Direct RED-only #19/#21/#23 retain their owner-path deltas. No child delta is closed or discarded, and predecessor checks never transfer.

The first immutable release remains blocked until one unchanged integrated protected candidate has exact native CI, 100% owned production statement/function/region/branch and edge-case coverage where tooling exposes it, public rustdoc, real rootless isolation E2E, positive LSM/seccomp/capability/resource/network/cleanup evidence, required review/security/SAST/dependency gates, package smoke, SPDX SBOM, provenance, checksum/signature where supported, reproducibility, recovery/rollback proof, and release automation sourced from protected `develop`. GitHub Releases remain absent; mutable PR heads are not consumer authority.

## Next bounded slices

1. Re-execute the real held-gate capability with production `src/bin/qsr_runtime_gate.rs` on the moved exact head. Keep the current musl/x86_64 assertion explicit; do not infer architecture portability from source compilation. If the production gate differs behaviorally from the removed fixture, fix the production gate or test contract causally rather than restoring duplicate authority.
2. Add the next production-adapter RED for #25: `RootlessPodmanAdapter::run_command_at` must receive an immutable/versioned architecture-compatible gate artifact and exact digest, mount/bind it as the initial OCI program, hold the consumer while effective evidence is sampled and policy-evaluated, release exactly once through a bounded control channel, and make the original payload-side-effect regression GREEN. Missing/wrong gate identity, evidence, release, or exec must fail closed with exact-ID cleanup. Do not relabel init/static inspection as effective proof or substitute plain `podman exec`.
3. If any `BackendSpawnFailed` reappears after the immutable-fixture conversion, perform Spawn-specific RCA from the bounded class. If an invocation failure becomes generic, narrow RCA to Wait/Capture. Do not introduce retry, sleep, mutex serialization, `PYTHONPATH`, or guessed errno.
4. Continue #39 with missing-field REDs, migrate otherwise-positive fake-Podman inspect fixtures to authoritative private UTS/cgroup values, and then require both fields exactly `private`; keep real namespace-handle proof as E2E acceptance.
5. Extend #32 with source-bearing duplicate and additional-mount cases without broadening the accepted source profile; compose these applied-state checks into #25's pre-release attestation instead of inspecting only after payload release.
6. Keep #31 open until real rootless-Podman log-flood E2E proves the 1 MiB host bound and explicit runtime-side incompleteness semantics; do not equate reader truncation flags with runtime log-store completeness.
7. Keep #37 open for missing/malformed `ImageDigest` fail-closed fixtures, deliberate positive-fixture migration, and real rootless equality; application-service #46 remains its separate owner path.
8. Continue remaining #14 causal lanes without pre-empting #29/#30/#35/#43/#44 ownership, and keep focused predecessor passes distinct from integrated release proof.
9. Non-force adopt the stabilized #14 foundation into #18 and artifact-analysis sibling descendants while preserving every valid test/fixture/contract/doctoring delta; reacquire exact-head CI/security/coverage on every moved child and continue #19/#21/#23 application-service REDs without bypassing their owner paths.
10. Merge only after one unchanged exact candidate satisfies review, security, coverage, real runtime and positive-LSM gates; then verify native CI on the exact protected integration SHA and publish only from that authority.

## 2026-09-10 command resource-configuration update

Issue #35 now has an executed causal baseline on `97dff877a243f7d772d02e9ca2cf93f470b7fe2f`, native CI `34461059296`: all three resource-config counterexamples false-GREENed while the later #25 REDs remained independent. Production `dd7242e53a79f1a8004e24010ae999fda70e78af` introduces a command-specific applied-configuration predicate: `Config.Timeout` must be non-zero and exactly equal to the requested lease, `HostConfig.Tmpfs` must contain only `/tmp`, and that entry must be exactly the request-bound set `rw,noexec,nosuid,nodev,size=<tmpfs_bytes>` in addition to the pre-existing CPU/RAM/PID checks. The application-service verifier deliberately remains on its existing resource contract rather than silently inheriting command-specific inspect fields.

Otherwise-positive fake Podman evidence was migrated rather than weakening the new fail-closed predicate: `75df58cd2753eaea64fa62ad3557dd6caf19cbf5` updates the checked-in shared dispatcher, `4740624d2ac930a1e87d60bc0be0e45135d5c040` updates the command integration fixture and request-1-second timeout case, and `80461ec28775b29ece3f5366a384f6235d106f4a` extends #35 with missing timeout, missing tmpfs, additional tmpfs destination, duplicate option, and contradictory option counterexamples. `docs/doctoring/COMMAND_RESOURCE_CONFIGURATION_TRACEABILITY.md` records the evidence classification and APA 7 references. Exact-head CI on this moved lineage is required before focused GREEN is claimed; inspect remains backend-applied configuration evidence, not effective cgroup/mount/watchdog proof. Real rootless-Podman `/tmp`, cgroup-v2, wall-time, cleanup, positive-LSM, and #25 hold→attest→release acceptance remain release gates.


## Current authority supersession — command chronology evidence (2026-09-11)

Issue #44's original chronology witness had become false-pass capable because its fake inspect evidence no longer satisfied the current command isolation contract. Draft #111 exact `d45ec905eb55bb2b23737995e96baf698cf4de16` repaired the fixture and CI `34565196344` executed the intended RED after checkout/policy/fmt prerequisites: caller-supplied future wall-clock input still reached successful runtime lifecycle evidence and contradicted runtime-owned result chronology. The canonical repair candidate moves start/finish observation inside the Podman runtime boundary, fails closed on pre-epoch or rollback chronology, keeps elapsed-time enforcement monotonic, and leaves caller time as a non-authoritative compatibility seam. Historical #44 sections below remain causal evidence; this section is the current authority.

## Current authority supersession — gated command runtime and chronology (2026-09-11)

This section supersedes older present-tense snapshots in this ledger while preserving them as causal history. Protected/default `develop` remains `60a85c7633e03b425b67159ec6822c8178cf87ea`. The live canonical #14 source ref is `aca827d7bd45f3df289456176730c781bd6d1164`; Draft #112 is the ordinary descendant owner-repair lane for #44 and related coverage-invariant cleanup. Mutable PR heads are not consumer authority.

The historical statement that production `run_command_at` still launches the consumer directly as OCI PID 1 and still lacks gate integration is no longer current. Canonical #14 production now stages a digest-bound, architecture/byte-order-compatible self-contained gate; creates the container with that gate as the initial OCI program; acquires the exact container ID; checks configured isolation; starts only the gate; verifies live effective seccomp/capability/LSM evidence; performs a bounded one-time exact-ID release; requires trusted `QSR_GATE_RELEASED` acknowledgement; detaches release stdin; then executes the exact consumer argv and performs bounded wait/log collection and exact-ID cleanup. The older direct-consumer text remains below only as historical evidence of the defect that drove #25.

Issue #44 has causal execution and a runtime-owned chronology repair. Draft #111 exact `d45ec905eb55bb2b23737995e96baf698cf4de16`, CI `34565196344`, failed at the intended assertion because caller future time `1000000000000` was published as observed start evidence. Draft #112 moves start/finish observation inside `RootlessPodmanAdapter`, retains the caller time only as a compatibility/test seam, fails closed on pre-epoch or rollback chronology, and keeps `Instant`/bounded runner deadlines as monotonic timeout authority.

Exact #112 predecessor `cf90c395513e404440787cb6981c84e47860d0d2`, CI `34578304083`, is executed hosted evidence: verify and hosted negative rootless/AppArmor are GREEN through repository validation, rustfmt, full workspace/all-target tests, Clippy and rustdoc. Coverage artifacts were generated and failed only the explicit repository-wide 100% admissions at lines `4691/4841 = 96.90%`, functions `435/446 = 97.53%`, regions `6304/6543 = 96.35%`, branches `656/726 = 90.36%`. Coverage artifact digest is `sha256:bcfdfa9fc0c4c60c96518b5fe802e12c0812e71c8da225367f36012b9d6e7f26`; branch artifact digest is `sha256:8576fb11a3ff3daf33bea6d9b3258973af8cd14481d4f8efd0a71f330183524f`. Dedicated positive SELinux remains unproven.

That branch artifact isolated `src/application_service/mod.rs` at 100% lines/functions/regions but 61/62 branches. The only missing outcome was the redundant `!component.is_empty()` predicate inside `repository.split('/').all(...)`: earlier admission already rejects empty repository, `//`, leading `/`, and trailing `/`, so no admitted repository string can yield an empty later slash component. The repair removes only that impossible predicate, preserving `.`/`..` and colon rejection. One-shot run `34583458163` validated repository policy, the focused malformed/mutable image-reference regression, rustfmt, full workspace/all-target tests, Clippy, rustdoc and `git diff --check`, then published ordinary descendant `2fa06f752acb72e18697924102eab3f89f469832` and removed its own workflow. This is structural control-flow repair, not a coverage exclusion or denominator configuration change. The automatically-triggered PR CI for that bot-authored commit concluded `action_required` before creating jobs, so it is not exact-head CI evidence; a normal owner-authored descendant must reacquire the full exact-head gate.

Release remains blocked until one unchanged integrated candidate reaches 100% owned-production line/function/region/branch and edge coverage, qualifying review/security, real rootless #35/#43 cgroup-v2/live `/tmp`/wall-time/over-lease evidence, dedicated positive effective-LSM, protected-head CI, complete successor/traceability adoption, and immutable version/package/SBOM/provenance/reproducibility/rollback publication. #18 remains source-unrestacked until the command/runtime foundation is stable; #111 and #106 remain open until their complete valid deltas are demonstrably succeeded by canonical owner ancestry.
