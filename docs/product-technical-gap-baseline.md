# Product and Technical Gap Baseline

Last reviewed on 2026-09-20 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea` and the live owner PR refs below. This file is the repository-wide single-writer current baseline. Predecessor checks never transfer to a moved head, mutable PR heads are not consumer authority, and GitHub `mergeable=true` is not release readiness.

The full historical causal ledger is preserved in [`product-technical-gap-history-2026-09-16.md`](product-technical-gap-history-2026-09-16.md). Historical present-tense statements there are evidence, not current authority.

## Integration root and release authority

Canonical integration root #1 remains exact `a85dc86c00f00354d9ddb9bf7c291c2c1cd40884`, Draft/open, targeting protected `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`. Native CI `35180762143` has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor jobs; positive SELinux received no eligible runner and was cancelled. Central Dependency Review `.github#810` and CodeQL terminal-publication `.github#1929` remain release blockers. Effective organization ruleset `18156473` still exposes `OrganizationAdmin/always` bypass while requiring one approving review; `.github#1340` owns routine-bypass removal and `.github#772/#1351` own solo-maintainer approval-policy convergence. This repository must not exercise that bypass, self-approve, fabricate review, direct-push, or weaken deterministic gates. GitHub Release inventory remains empty.

## Current owner authority

### Command runtime and repository fitness

Lease-ownership owner #6 is exact `7029537b2165c5ad0adf64183c0aa733257a5d4a`, Draft/open/mergeable on current root #1. Predecessor `785c2cc84861d3bce5fb78a7183cf3102b743bf5` executed CI `35180780227`: repository policy, coverage-parser and formatting passed; six of seven lease-owner controls passed, while the retry case failed at `rootless_probe`. Current test-only `7029537...` atomically replaces the mutable executable fixture pathname; production/idempotency semantics remain unchanged. CI `35389946520` is queued.

Command/runtime owner #14 is live exact `73a191c65cfe1dd16447921acc8f96b306583439`. Its runtime-gate path is design evidence for #128: verified artifact binding, exact `/qsr-runtime-gate` entrypoint, one-time release token, exact-container attach, bounded acknowledgement, then local attach-client cleanup. It is not mutable source-copy authority for application-service. Repository-fitness descendant #112 is exact `8991d4a22004d07077db78e57e125088fcd16de4`; current CI `35379471155` is queued.

Independent process-lifecycle owner #125 is exact `5bced55c107aff2a2cabb84ebc6d980134e7ed07`; its current change is the minimum Clippy repair `status_result?;`, with runtime behavior unchanged. CI `35378675229` is queued.

### Application-service and network lifecycle

Canonical application-service runtime/lifecycle owner #21 is exact `717edef989d8b6bb7e1f672d91d9258f7942a6b2`, Draft/open/mergeable. Exact CI `35165254755` has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor jobs; positive SELinux received no eligible runner, so the workflow is not release-GREEN.

Canonical network successor #127 is now exact `f9a95889119f458954b3833e27f10907d403bb3c`, Draft/open/mergeable, normally containing #21 through ordinary two-parent merge `7934aa99085264db7ee654088c3df0f116a67b66`. Predecessor `71063882948daf5819839d8059c37857a6255abf` finally acquired hosted runners in CI `35384682678`: hosted negative rootless/AppArmor was GREEN, while verify reached the full workspace and all six `podman_application_service_create_identifier_red` cases failed before their intended container-receipt boundary with `BackendCommandFailed { operation: "network_identity_inspect" }`. Test-only `190762bc349590cb5742b8b4efad7b8a15534c9f` repaired only that stale fixture by supplying the already-required canonical lower-case 64-hex/internal/DNS-disabled network inspection response; production semantics were unchanged.

Issue #128 remains P0. Review `5254951818` found the pre-attestation witness could false-GREEN because any `--entrypoint=*` suppressed the hostile side-effect marker. Test-only predecessor `7f21375cd79936d8ce0769999dc602112b500a15` closes that false-GREEN without changing production: a fake hold is recognized only when create argv contains `--interactive`, a test-owned gate artifact mounted read-only at `/qsr-runtime-gate`, and exact `--entrypoint=/qsr-runtime-gate`; independent fixture controls prove `/bin/false` still arms/runs the hostile payload while that exact binding holds it. Review `5255195712` records that repair.

Current-head review `5255563318` found a second evidence false-GREEN in the network-provenance RED: WrongName/ExternalNetwork/DnsEnabled could all pass on an unrelated failure before exact-name inspection. Current test-only #127 `f9a9588...` therefore requires runtime-owned network creation, derives the exact generated `qsr-net-*` correlation from the invocation log, requires exactly one `network inspect --format json <that name>` after creation, and still forbids container create. The single-inspection requirement prevents retry-until-match identity admission. Production Rust/API/schema/network behavior is unchanged. Native CI `35439408261` is materialized but all five jobs remain pre-runner queued; no semantic RED/GREEN or predecessor status transfers.

The production issue remains: current application-service `launch_at` starts the exact acquired container before `verify_effective_isolation`. Before production GREEN, #128 still requires an independent positive lifecycle RED proving that successful effective attestation enables exactly one runtime-owned release transition, with acknowledgement bound to the exact acquired container/request, and only then readiness. The active owner sequence remains exact network provenance → no destructive cleanup before ID admission → exact-ID non-force cleanup → exact effective attachment by admitted `NetworkID` → public correlation/private authority separation → foreign-safe explicit termination → mandatory effective isolation evidence → trusted hold/attest/release/ack.

Application-service option/operand successor #133 is exact `4191ac0051093c30150734f6b4e98c05f36a0ef4`, Draft/open/mergeable. Fresh compare against #127 `f9a9588...` is diverged with merge base `71063882948daf5819839d8059c37857a6255abf`: #133 is 13 commits ahead and 3 commits behind. Its semantic delta remains one literal `--` immediately before `request.image_reference`. Do not restack until #127 current exact is causally classified; then adopt the three parent commits non-force, repair its stale checked-in delimiter TRACEABILITY, and reacquire focused/full evidence.

Issue #138 / Draft #139 owns the application-service public-schema UTF-8-byte mismatch. #139 exact `3a2ed2002417692675ec7b21a75a2f1718fc3191` changes only its RED and TRACEABILITY above #21. Reviews `5253486572` and `5253616141` removed arbitrary-dialect and URI-only false-GREEN paths; current CI `35408088920` is queued. #101/#102 remains the immutable shared byte-vocabulary prerequisite; #139 may consume only a released contract for GREEN.

Issue #136/#137 is a separate self-hosted-runner boundary: `linux-cluster-ops#326` owns s1 placement/registration and the true pre-lease enablement gate; this repository owns reusable isolation-attestation evidence. Reviews `5255674039`, `5255831636`, `5255961705`, and `5255976931` removed raw-substring, helper-shape, unrelated-fixed-probe, inline-comment, and scope→target false-GREEN paths. Review `5256349715` then found an RFC1918 target could use an arbitrary closed service port while symbolic service aliases could fail DNS resolution and masquerade as denial. Test-only predecessor `44d9beb6eaffe4e42c987b95a4ce064c78a567ba` required RFC1918 host targets without arbitrary ports and exact Redis/PostgreSQL/DNS ports on literal private/loopback/link-local IPv4 addresses. Review `5256617995` found loopback is the wrong authority under the selected dedicated-VM topology: `127.0.0.1:*` probes the guest, not the s1 host boundary. Test-only `247c19ccc5d8096f6fd938964c3b20c3c997ebeb` repaired the probe-semantics witness, but fresh reviews `5256925734` and `5256936716` found the older target-binding witness still admitted guest loopback and RFC1918 targets carrying arbitrary service ports. Current test-only #137 exact `fdb1f3543044507f9fc4dd4a087afe96a4cc5ca3` aligns both contracts: Redis/PostgreSQL/DNS require their exact service port on literal non-loopback private/link-local IPv4, while the three RFC1918 scopes require portless in-range literal IPv4. Production workflow behavior remains unchanged and the live workflow still has no attestation step, so this exact remains intentional RED; CI `35460412420` is materialized and queued. One authority gap remains before workflow GREEN: an arbitrary nonexistent private endpoint can still fail without proving it is the actual host-side boundary. `linux-cluster-ops#326` must publish the real host-side/bridge probe identity or equivalent immutable operator-owned endpoint contract and real pre-lease observed-denial evidence. QSR must not invent that identity. Finite point probes are not proof of complete CIDR non-reachability.

### Artifact-analysis owner stack

Artifact-analysis parent #18 is live exact `703b4b1a047321bb08d1d6cb1de78d01cb350696`, Draft/open/mergeable. Do not copy unstable command/runtime source into artifact-analysis leaves; foundation adoption must be ordinary/non-force and preserve every valid test/fixture/contract/TRACEABILITY delta.

- #53 dynamic-attestation exact `9d0da2a50e3247fd6cd2e52301d57408d404557d`: predecessor stopped at rustfmt before semantic assertions; current change is formatter-only, CI `35342943374`.
- #61 evidence-record identity exact `acb615de42d0c0013be47d47b8d2988197597100`: typed `InvalidEvidenceIdentity` candidate after causal RED, CI `35348644513`.
- #63 policy-boundary binding exact `b3204f8e1c694c18489b41a1c2a1591117e6e58f`: owner-local contract focused GREEN; broader failure is inherited command/runtime ancestry.
- #65 foundation-cardinality exact `3bb72c6cdbbc07823ba1c5e59f6a7b3528b29591`: formatter-only prerequisite repair, CI `35323306415`.
- #130 schema-cardinality exact `038d0ce36c04b598c246acf09a82ee2967a9aec8`: ordinary restack on #65; production/schema unchanged until hardened RED executes, CI `35333036539`.
- #76 strict evidence deserialization exact `60d19a531c06f035df5291948382de7ee10484c6`: owner-local controls focused GREEN; broad failure is inherited command/runtime ancestry.
- #70 Analyzer Worker exact `991ecccabcf23380443d86873cf2777967112fbf`: Worker/Core suites focused GREEN; decoded whole-workspace failure is inherited. Do not weaken worker semantics or mask with retry/sleep/mutex.
- #51/#55/#59 remain independent result/provenance/subject-binding prerequisites.
- #67 exact `010330580634fc277ae2a18e9504e2d1f8c5d712` retains executed self-verifiability RED plus descriptor-identity RED; CI `35400068729` is queued.
- #102 UTF-8/dialect publication exact `517bc8a2326289b18b6e7a5f6b683eff6117ba9` is historical; current exact `517bc8a2326289a9c93c024430e9665820dc553a` applies formatter output only after the predecessor stopped before publication witness, CI `35405113169` is queued. Canonical publication paths are `docs/contracts/cwl_artifact_analysis_contract_vocabulary_1_0_0.md` and `tests/fixtures/cwl_artifact_analysis_contract_vocabulary_1_0_0_vectors.json`.
- #135 Gregorian `submitted_at` parity exact `61d6581359c019cdea323aec8a2a707695c44af4`: CI `35411589042` is queued. Review `5253891162` found syntax-only `x-cwl-rfc3339Profile` declaration insufficient; test-only `b866657...` requires normative shared-vocabulary semantics plus hostile/positive conformance vectors. Production/schema/vocabulary publication remain unchanged.

## DDD and ownership boundary

`sandbox_execution` is Core. `application_service` and `artifact_analysis` are Supporting bounded contexts. Podman/gVisor/containerd/Kubernetes/VM are infrastructure adapters. Generated names and serialized receipts are correlation/evidence unless a reviewed versioned contract grants stronger semantics. Destructive cleanup authority must be runtime-acquired, exact, private, and non-serializable. Requested configuration, backend-applied inspection, acquired backend identity, and live effective enforcement are distinct evidence levels.

Core foundation repositories are optional released contract owners rather than mandatory source dependencies. Consumers use immutable released contracts/ACLs; mutable branch dependencies, sibling source copies, and cross-service SQL are prohibited.

## Buyer-visible gaps

| Gap | Current authority | Required next evidence |
| --- | --- | --- |
| Lease-owner retry CI can be invalidated by mutating an executable fixture pathname between backend phases. | #6 `7029537...`; predecessor has assertion-level retry failure. | Execute current atomic-replacement fixture exact and require retry plus sibling ownership controls GREEN without production/idempotency change. |
| Branch coverage can false-underreport repeated codegen records. | #112 `8991d4a...`; predecessor has causal RED. | Execute current physical-source union repair and denominator-mismatch control without threshold/denominator weakening. |
| Application-service create-ID witness had stale network-inspection setup. | #127 predecessor `7106388...` / CI `35384682678`; fixture repair `190762b...`. | Do not transfer predecessor status; current #127 must execute and reach the intended create-ID/provenance/hold boundaries. |
| Network-provenance contradiction RED could pass before inspecting the created network. | #127 `f9a9588...`; review `5255563318`; CI `35439408261`. | Execute current exact and require one exact-name inspection after create before contradiction rejection; then implement only the causal provenance admission if RED is reached. |
| Application-service network authority is not fully exact-ID and foreign-safe end to end. | #127 `f9a9588...`. | After current RED classification, progress provenance → cleanup authority → effective attachment → private authority → non-force termination → mandatory evidence. |
| Pre-attestation hostile-service witness accepted arbitrary entrypoint presence as a trusted hold. | #128; reviews `5254951818`, `5255195712`; inherited #127 hardening `7f21375...`. | Execute the hardened arbitrary-entrypoint RED on current #127. Then add an independent positive attest → one-time release → exact ack → readiness lifecycle RED before production GREEN. |
| Leading-dash image references need explicit Podman option termination. | #133 `4191ac0...`, CI `35384712828`, now 3 commits behind moved #127. | After #127 classification, non-force adopt current parent, repair stale delimiter TRACEABILITY, and reacquire delimiter/full evidence. |
| Shared UTF-8/serialized-byte vocabulary publication has not reached its hardened publication RED. | #101/#102 `517bc8a...`, CI `35405113169`. | Execute current exact; require missing-publication RED, then publish the minimum versioned normative vocabulary/conformance contract. |
| Application-service public schema can admit multibyte values rejected by runtime UTF-8 byte limits. | #138/#139 `3a2ed200...`, CI `35408088920`. | Execute current RED; after released #101/#102, prove exact dialect/vocabulary/byte-keyword surface and vectors. |
| `submitted_at` schema/runtime Gregorian semantics can diverge. | #134/#135 `61d6581...`, CI `35411589042`; review `5253891162`. | Execute current RED; after immutable #101/#102 publication, extend the same released vocabulary with normative Gregorian/Z-only/no-leap-second semantics and vectors. |
| Self-hosted CI can false-GREEN host/network denial through inconsistent target contracts, guest loopback, closed ports, or an arbitrary unbound private endpoint. | #136/#137 `fdb1f35...`; reviews `5256925734`, `5256936716`; CI `35460412420`; `linux-cluster-ops#326`. | Execute the current aligned RED; require ops-owned publication of the actual s1 host-side/bridge probe identity and real pre-lease failed-reachability output before workflow GREEN. Never substitute guest loopback, arbitrary CIDR service ports, a random private address, or finite point probes for the real boundary. |
| Dynamic analysis completeness/execution semantics are not proven end to end. | #53 current exact. | Execute hardened RED before semantic GREEN. |
| Evidence IDs can contradict enclosing job/sequence. | #61 typed candidate after causal RED. | Require exact current-head typed GREEN; do not claim stock JSON Schema cross-instance parity. |
| Analysis job identity can alias materially distinct canonical artifact descriptors. | #66/#67 `010330580...`, CI `35400068729`. | Execute descriptor witness; repair only after stable analyzer provenance can participate in independently verifiable derivation. |
| EvidenceBundle schema may be weaker than Rust foundation-cardinality invariant. | #65 + #130. | Execute current RED; only then add minimal direct schema cardinality constraints. |
| Focused artifact-analysis leaves remain blocked by stale command-runtime ancestry. | #63/#76/#70 focused semantics are GREEN. | Stabilize #14/#112, then ordinary/non-force adopt into #18 and descendants; reacquire exact-head evidence. |
| Positive effective-LSM evidence is absent on release candidates. | Hosted negative evidence exists; dedicated SELinux lanes repeatedly lack runners. | Obtain exact-candidate positive effective-LSM evidence; never substitute negative fail-closed evidence. |
| Protected integration governance admits routine administrator bypass and an approval gate not yet converged for the solo-maintainer path. | Ruleset `18156473`; `.github#1340`, `.github#772`, `.github#1351`. | Remove routine admin bypass and prove a satisfiable ordinary protected path without self-approval, direct push, or gate weakening. |
| No immutable consumer authority exists. | Protected `develop` unchanged; central security non-terminal; Release inventory empty. | Resolve central gates, integrate normally, rerun protected-head CI/security/runtime evidence, then publish version/CHANGELOG, package/release, SBOM, provenance, reproducibility, rollback. |

## Current bounded action order

1. Let #6 exact `7029537...` / CI `35389946520` execute and reacquire its retry witness before descendant adoption.
2. Let #112 exact `8991d4a...` / CI `35379471155` execute physical branch-union repair without denominator/threshold weakening.
3. Let #127 exact `f9a9588...` / CI `35439408261` execute. Require the hardened provenance witness to reach exactly one intended identity inspection and classify inherited create-ID and arbitrary-entrypoint controls on this exact only.
4. If #127 reaches the intended provenance/#128 causes, continue in owner order: provenance admission → no destructive cleanup before ID admission → exact-ID non-force post-admission cleanup → exact effective attachment → private authority/foreign-safe termination → positive attest → one-time release → exact acknowledgement → readiness.
5. Non-force restack #133 only after #127 dependency-safe evidence is classified; repair its stale checked-in delimiter TRACEABILITY during that actual adoption and reacquire focused/full gates.
6. Let #137 exact `fdb1f35...` / CI `35460412420` execute the aligned target-binding/probe-semantics RED. Do not implement workflow GREEN until `linux-cluster-ops#326` publishes the actual host-side/bridge probe identity and real s1 pre-lease failed-reachability evidence. Bind any later in-job guard to that authority; do not invent a private endpoint, reintroduce CIDR service-port probes, or treat finite point probes as complete CIDR proof.
7. Let #102 exact `517bc8a...` / CI `35405113169` reach its hardened shared-vocabulary publication RED; do not create GREEN publication artifacts before the cause executes.
8. Let #139 exact `3a2ed200...` / CI `35408088920` execute its hardened UTF-8 mismatch RED; only after #101/#102 release may it consume the versioned byte-vocabulary contract.
9. Let #135 exact `61d6581...` / CI `35411589042` execute the Gregorian-profile RED; no schema/vocabulary GREEN before causal execution and no mutable #102 source copy.
10. Let #53/#61 and then #65/#130 execute without predecessor promotion.
11. Preserve focused GREEN semantics on #63/#70/#76; stabilize #14/#112 before ordinary adoption into #18.
12. Let #51/#55/#59 and #67 exact `010330580...` / CI `35400068729` execute independently without wake commits.
13. Let #125 exact `5bced55...` / CI `35378675229` execute its minimum Clippy repair and reacquire its own evidence.
14. Keep root #1 Draft until positive effective-LSM, central Dependency Review/CodeQL terminal evidence, ordinary protected integration, and `.github#1340` + `.github#772/#1351` governance convergence are all proven. Treat `.github#712` queue health as an operational gate for current-head evidence, not permission to rerun blindly or weaken selectors. Never use `current_user_can_bypass=always` as a release shortcut.
15. Merge/release only from one unchanged dependency-safe protected candidate satisfying every gate.
