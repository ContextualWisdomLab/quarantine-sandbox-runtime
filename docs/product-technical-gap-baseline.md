# Product and Technical Gap Baseline

Last reviewed on 2026-09-18 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea` and the live owner PR refs below. This file is the repository-wide single-writer current baseline. Predecessor checks never transfer to a moved head, mutable PR heads are not consumer authority, and a GitHub `mergeable=true` result is not release readiness.

The full causal ledger that previously occupied this file is preserved byte-for-byte in [`product-technical-gap-history-2026-09-16.md`](product-technical-gap-history-2026-09-16.md), using the former baseline blob `8fa8911d252f37be1f949d231ab522929e8ed6d8`. Historical present-tense statements there are evidence, not current authority.

## Current owner authority — 2026-09-18

### Command runtime and repository fitness

Canonical command/runtime PR #14 is exact `73a191c65cfe1dd16447921acc8f96b306583439`, Draft/open/mergeable. Native CI `35175479743` completed with `verify` GREEN through exact checkout, dependency lock, repository policy, CI-evidence contracts, rustfmt, full workspace/all-target tests, Clippy and documentation; hosted negative rootless/AppArmor is GREEN. Coverage and branch-coverage produced evidence but failed their explicit 100% admission gates. Dedicated positive SELinux was cancelled without runner assignment. This is strong hosted evidence, not whole-head or release GREEN.

Exact #14 coverage evidence reports raw `4643/4794` lines, `428/439` functions and `6258/6495` regions. Raw branch accounting is `655/726`. Independent inspection of the same LLVM branch artifact shows that repeated codegen records for the same physical source branch undercount six covered outcomes: physical-source union is `661/726` with the denominator unchanged. `src/application_service/coordinator.rs` changes from `19/24` raw to `23/24` physical union and `src/infrastructure/podman.rs` from `143/194` to `145/194`.

Canonical repository-fitness descendant #112 is exact `48ad86148be70fff165800d07221409bc7521af5`, directly based on current #14. Its test-only `coverage_branch_codegen_union_red.rs` requires physical-source true/false union across codegen instantiations while retaining the full physical denominator and 100% threshold. Native CI `35257533087` remains queued. Do not implement or claim GREEN until this unchanged RED executes behind repository/fmt prerequisites.

Independent process-lifecycle owner #125 is exact `f7484afcdf41d2050bee199bf96bfd9243fb4b1c`, Draft/open/mergeable. Predecessor `f5b26660761a283d4dc9bf5b2c14947b51f4b946` executed all 17 bounded-command tests GREEN after the non-panicking `Instant::checked_add` deadline repair, then failed only repository complete-coverage admission at `2219/2222` lines and `3004/3005` normalized regions with functions `205/205`. Current #125 adds the concrete detached-descendant `OutputLimit` plus termination-failure witness; its CI `35094825945` completed non-GREEN. Keep this lane independent from application-service network lifecycle.

### Application-service and network lifecycle

Canonical application-service runtime/lifecycle owner #21 remains the domain owner for runtime identity, exact acquired container authority and non-serializable cleanup authority. Canonical current network successor #127 is exact `4306462ddbd90ce137074e70af7c9c0bbee18d9e`, Draft/open/mergeable, above #21.

#127 predecessor `1a3cd0427bbf99e41c1d8bd4089747561237071f` executed the typed backend-spawn succession path and exposed process-test fixture drift after acquired-network-ID admission. Current `4306462...` is test-only: the fake Podman network-inspection response now supplies the canonical lower-case 64-hex acquired network ID so later lifecycle tests can reach their intended boundaries. Production Rust, typed spawn taxonomy, cleanup semantics and network attachment admission are unchanged. Native CI `35302334780` remains queued, so predecessor execution does not transfer.

Application-service image option/operand succession #133 is now exact `b79496876499ab484b68850327f53c1efc59afaa`, Draft/open/mergeable, based on current #127 exact `4306462...`. Historical #90/#91 had already executed the causal leading-dash image-operand RED and canonical #14 later verified the one-literal-`--` repair, but that delimiter fell out of the newer #127 ancestry. #133 restores only `"--".to_owned()` immediately before `request.image_reference.clone()`, retains a focused regression and owner-local TRACEABILITY, and changes no network/lifecycle semantics. Predecessor #133 exact `4d5794b1a41d49ee80e3f116db705538e247606f`, CI `35203507971`, passed checkout/dependency/repository/coverage-parser prerequisites but failed at `cargo fmt --check` before the focused Rust test; hosted negative rootless/AppArmor was GREEN. Formatter-only `8265dd5e2a1a19c4de2e5b647c3a5ff0788fdf09` repaired that prerequisite. Non-force two-parent `8d18fb5701fb7b5690fed270a24c9d55b2785cd1` then adopted live #127 without losing its acquired-network-ID fixture repair; current `b794968...` makes TRACEABILITY current. Fresh compare from #127 is `ahead_by=11`, `behind_by=0`, with only the three #133-owned files changed. Native CI `35338355351` has materialized and is queued; no predecessor semantic/full-head GREEN transfers.

The active network sequence remains: one-object network provenance → no destructive cleanup before exact ID admission → exact-ID partial cleanup after admission → exact effective attachment of the acquired container by admitted `NetworkID` → public correlation/private lifecycle authority separation → non-force foreign-safe explicit termination → mandatory isolation-evidence admission → issue #128 hold/attest/release. Historical #89/#120/#122/#124/#126 stay open until every valid delta/test/fixture/contract/evidence item is demonstrably succeeded on a verified current-owner lineage.

Issue #128 remains a P0 execution-order gate: hostile application payload must not run before effective isolation attestation. A reusable runtime-gate dependency may be consumed only through stable owner integration, not mutable sibling source copying.

### Artifact-analysis owner stack

Artifact-analysis parent #18 is exact `703b4b1a047321bb08d1d6cb1de78d01cb350696`, Draft/open/mergeable. Its PR body contains stale top-of-body SHA text, but the Git ref is authoritative. Do not mechanically restack it onto #14/#112 while the command/runtime foundation still lacks complete coverage, positive effective-LSM, qualifying review/security, protected integration and immutable publication. Later adoption must be ordinary/non-force and preserve every #18 test/fixture/contract/TRACEABILITY delta.

Policy-boundary binding #63 is exact `b3204f8e1c694c18489b41a1c2a1591117e6e58f`, Draft/open/mergeable. Native CI `35174362030` executed the current head: checkout, dependency lock, repository policy, coverage-parser tests and rustfmt passed, and `artifact_analysis_policy_boundary_binding_red::policy_boundary_evidence_must_match_the_runtime_manifest` passed. The workspace later failed in an inherited application-service fixture because `backend_security_info` returned `BackendInvocationFailed` before the fixture reached its expected read-only-root-filesystem isolation contradiction. Hosted negative rootless/AppArmor was GREEN; positive SELinux received no runner and was cancelled. Treat #63 as current-head focused GREEN only; do not weaken the PolicyBoundary contract to mask stale command-runtime ancestry.

Foundation-evidence cardinality #65 is exact `3bb72c6cdbbc07823ba1c5e59f6a7b3528b29591`, Draft/open/mergeable. Its predecessor `f73c333160a8d6c62dd619f6340c8c97ec3192e9`, CI `35174385856`, passed checkout, dependency lock, repository policy and coverage-parser tests, then failed before Rust tests solely at `cargo fmt --check`. The decoded log identified one deterministic rustfmt 1.97.1 delta in `tests/artifact_analysis_foundation_evidence_cardinality_red.rs`; current `3bb72c6...` applies only that emitted formatting and does not change production/cardinality semantics. Native CI `35323306415` is queued; no predecessor semantic or coverage status transfers.

Schema-cardinality parity #130 is now exact `038d0ce36c04b598c246acf09a82ee2967a9aec8`, Draft/open/mergeable, based on current #65 `3bb72c6...`. It had become four prerequisite commits behind and non-mergeable. Two-parent non-force merge `41bfc6d9086108b4e7c086e6dc44e0db8ef428e3` adopts exact #65 while preserving #130's hardened schema-cardinality RED and owner-local TRACEABILITY; docs-only `038d0ce...` makes that ancestry current. Relative to live #65 the PR has exactly two changed files, and production Rust plus `schemas/evidence-bundle.schema.json` remain unchanged. Native CI `35333036539` has materialized and is queued, so neither causal RED nor schema GREEN is promoted yet.

Strict evidence deserialization #76 is exact `60d19a531c06f035df5291948382de7ee10484c6`, Draft/open/mergeable. Native CI `35174454049` executed all five owner-local unknown-field controls GREEN, including the valid published bundle control and rejection of unknown top-level/artifact/runtime/evidence members. The workspace later reached the independent command-runtime ENTRYPOINT RED: the older ancestry passed requested argv after the image without an explicit entrypoint override. Canonical #14 already owns the executed issue #38 repair that encodes the validated argv as an explicit entrypoint before the image, so this failure is a foundation-adoption gap rather than a reason to weaken `#[serde(deny_unknown_fields)]`. Hosted negative rootless/AppArmor was GREEN; positive SELinux received no runner and was cancelled.

Analyzer Worker parent #70 remains exact `991ecccabcf23380443d86873cf2777967112fbf`. Native CI `35174430661` completed non-GREEN: repository/dependency/coverage-parser/rustfmt prerequisites passed, workspace Test failed, coverage and branch-coverage failed during Rust evidence generation, hosted negative rootless/AppArmor passed, and positive SELinux was cancelled pre-runner. Direct children #78 `ab3fd665766b65d650270956bb7d9ab235fb95ea`, #80 `9c534d71092946159606202312820ae820b219da`, and #82 `ffe7bce172d9ae6498205d5b560117cc2d11ba80` remain ordinary non-force restacks; their current native CI runs remain queued and predecessor GREEN does not transfer.

Focused artifact-analysis prerequisites remain independent. Result-channel #51 exact `ac5f02064d1de49c601dcd25472c53dd9193364c` / CI `35290124008`, analyzer provenance #55 exact `dbccb4aea2a55b4a153b102c6b22e8d5744c6c0d` / CI `35294181565`, and subject binding #59 exact `13899e1c7a257a54600aee0653b894555e42501a` / CI `35298476070` are still queued. Job identity #67 has advanced by ordinary fast-forward to exact `81b4ee8d1b51dc0b47164acd290de779698d8c9b`. Its predecessor `8a99012a60cb1a903ab751937c4b2f8ca49a4fc5` retains the executed self-verifiability RED. Current `81b4ee8...` repairs the separate duplicate private serializer finding at cause by deleting only the unexported `runtime::to_pretty_json` and now-unused `SerializationError` import (`+0/-10`); the canonical public serializer remains `evidence_bundle::to_pretty_json`. Job-identity/analyzer/schema/Gap semantics are unchanged. Native CI `35328176840` is queued, so predecessor execution does not transfer to current-head merge authority. The semantic #67 GREEN remains prerequisite-bound on stable analyzer provenance from #54/#55.

UTF-8/dialect publication #102 exact `ea2945fcc2d3291b18b6e7a5f6b683eff6117ba9` remains a RED-only publication lane until its exact queued CI executes. Its future serialized-byte vocabulary must use the existing CWL nullable-field materialization followed by RFC 8785 JCS and UTF-8 octet counting. Gregorian profile #135 remains a separate dependent RED and must consume the released #102 dialect rather than create a competing mutable contract.

### DDD and product boundary

`sandbox_execution` is Core and owns reusable hostile-workload/application isolation, runtime resource/lifecycle evidence and bounded command execution. `application_service` and `artifact_analysis` are Supporting bounded contexts. Podman/gVisor/containerd/Kubernetes/VM remain infrastructure adapters. Backend-specific authority must not leak into public Supporting/Core contracts.

Generated names and serialized receipts are correlation/evidence data unless a reviewed versioned contract explicitly grants stronger semantics. Destructive cleanup authority must remain runtime-acquired, exact, private and non-serializable. Requested configuration, backend-applied inspection, acquired backend identity and live effective enforcement are distinct evidence levels; no layer may be promoted merely because adjacent receipt values agree.

Core foundation repositories remain optional released contract owners rather than mandatory source dependencies. Consumers must use immutable released contracts/ACLs; mutable branch dependencies, sibling source copies and cross-service SQL are prohibited.

## Buyer-visible gaps and required evidence

| Gap | Current authority | Required next evidence |
| --- | --- | --- |
| Repository branch coverage can false-underreport across repeated codegen records. | #112 RED plus real #14 artifact corroboration: raw `655/726`, physical union `661/726`. | Execute #112 unchanged; if causal, repair only physical branch aggregation. Keep denominator and 100% threshold. |
| Application-service network authority is not yet fully exact-ID and foreign-safe end to end. | #127 `4306462...`; current change is fixture-only and CI queued. | Execute current fixture repair, then progress provenance → pre-ID cleanup → post-ID cleanup → effective attachment → private authority → non-force termination → mandatory evidence in causal order. |
| A domain-valid leading-dash image reference can cross into Podman option grammar unless the provider argv explicitly terminates options. | #133 `b794968...` on live #127, with one-line delimiter repair; predecessor CI stopped at rustfmt before the focused test. | Execute CI `35338355351` on the unchanged current exact, prove the focused image-operand witness GREEN and retain all #127 lifecycle/network tests before any succession/closure claim. |
| Hostile application payload can precede isolation attestation. | Issue #128 remains downstream of the network/lifecycle sequence. | Hold created workload, attest exact sandbox, release only after successful effective isolation proof; preserve cleanup/recovery evidence. |
| Published EvidenceBundle schema is weaker than the Rust v1 foundation-cardinality invariant. | #65 Rust invariant plus #130 exact `038d0ce...`, current non-force restack; schema remains unchanged. | Execute #130 unchanged; if causal, add only direct `contains` + `minContains:1` + `maxContains:1` constraints under `properties.evidence.allOf`, preserving non-foundation multiplicity. |
| Artifact-analysis focused leaves remain partially blocked by stale command-runtime ancestry even when their own contracts are GREEN. | #63 and #76 are current-head focused GREEN; #65 has an exact formatter-only repair awaiting CI. | Preserve leaf semantics, let #65 exact CI execute, stabilize #14/#112, then ordinary/non-force adopt the repaired command foundation into #18 and descendants with full exact-head evidence reacquired. |
| Artifact analyzer execution remains fail-closed without a completed isolated-worker result path. | #70 plus #51/#55/#59 and related leaves. | Complete Core worker isolation/lifecycle, bounded result ingestion, stable analyzer provenance, subject/record/job integrity and truthful completeness; then reacquire exact-head coverage/security/runtime evidence. |
| Positive effective-LSM evidence is absent on current release candidates. | Hosted negative rootless/AppArmor evidence exists; dedicated SELinux jobs repeatedly remain unassigned/cancelled. | Obtain dedicated positive effective SELinux/LSM evidence tied to the exact candidate; do not substitute host capability or negative fail-closed evidence. |
| No immutable consumer authority exists. | Protected `develop` is unchanged and GitHub Release inventory is empty. | Integrate normally only after all exact-head gates, then run protected-head CI/security/runtime evidence and publish version/CHANGELOG, immutable package/release, SBOM, provenance, reproducibility and rollback evidence. |

## Verification and release authority

Protected/default `develop` is exact `60a85c7633e03b425b67159ec6822c8178cf87ea` and is marked protected. Administrative bypass capability is not normal-path evidence. GitHub Release inventory is empty.

One release candidate must remain unchanged while it passes repository validation, rustfmt, full locked workspace/all-target tests, Clippy `-D warnings`, public/private rustdoc `-D warnings`, 100% applicable owned-production statement/function/region/branch plus edge coverage, qualifying independent review/thread/security gates, dedicated positive effective-LSM and applicable real runtime evidence. Normal protected integration must then create an exact protected head that independently receives required CI/security/runtime evidence before version/tag/package/GitHub Release or equivalent immutable publication, SBOM, provenance, reproducibility and rollback can authorize consumers.

## Current bounded action order

1. Let #112 exact `48ad861...` execute its branch-codegen-union RED. If causal, repair physical branch aggregation only; do not shrink denominator, threshold or measured surface.
2. Let #127 exact `4306462...` execute the fixture-only repair. Classify any remaining application-service/network failures at the intended owner boundary rather than weakening fixtures or retrying.
3. Let #133 exact `b794968...` / CI `35338355351` execute on current #127 and prove the restored option-delimiter contract plus parent lifecycle preservation; do not transfer historical #90/#91/#14 GREEN.
4. Let #65 exact `3bb72c6...` execute after the exact rustfmt repair; classify its first Rust-stage result without transferring predecessor status.
5. Let #130 exact `038d0ce...` execute on current #65. Do not add schema cardinality GREEN before the hardened current-head RED is causal.
6. Preserve the focused GREEN semantics on #63 and #76. When #14/#112 is stable, flow canonical command-runtime repairs through #18 by ordinary non-force adoption rather than copying source into leaves.
7. Let #51/#55/#59, current #67 cleanup exact `81b4ee8...`, and #70 children execute independently. Do not wake-commit, transfer predecessor status or implement semantic GREEN before causal evidence.
8. Continue the independent #125 process-lifecycle edge only from its own executed/coverage evidence.
9. Merge/release only from one unchanged dependency-safe protected candidate satisfying every gate above.
