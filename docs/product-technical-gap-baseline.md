# Product and Technical Gap Baseline

Last reviewed on 2026-09-22 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea` and the live owner PR/Issue state below. This is the repository-wide single-writer current baseline. Predecessor checks never transfer to a moved head, mutable PR heads are not consumer authority, and GitHub `mergeable=true` is not release readiness.

The historical causal ledger remains in [`product-technical-gap-history-2026-09-16.md`](product-technical-gap-history-2026-09-16.md). Historical present-tense statements there are evidence, not current authority.

## Integration root and release authority

Integration root #1 remains exact `a85dc86c00f00354d9ddb9bf7c291c2c1cd40884`, Draft/open, targeting protected `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`. Its historical native CI has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor evidence; positive SELinux did not execute. Central dependency/security/governance blockers and organization ruleset approval/bypass convergence remain outside this repository's local authority. This repository must not self-approve, exercise administrator bypass, direct-push, fabricate review, weaken deterministic gates, or equate branch protection metadata with release readiness.

Release owner #10 remains exact `bb587667e049c900f0dec578f083b35ed50f21c7`, CI `35485675824`, Draft/RED. Generic `.github#1552` classifies `default_branch=develop` as Git Flow, so protected `develop` is development integration rather than production release authority. Issue #140 owns the missing protected production-branch/immutable-publication contract. GitHub Release inventory remains empty. No package, tag, GitHub Release, SBOM, provenance, reproducibility, rollback, or consumer bump is authorized until one unchanged dependency-safe protected production candidate satisfies every gate.

## Current owner authority

### Command runtime and repository fitness

Lease-ownership owner #6 is exact `b096f4bf1d4ae1a663538127bf1b1048734c0079`, CI `35541704662`, and has now executed on that exact head. Verify is GREEN through exact checkout, dependency lock, repository policy, coverage-parser tests, rustfmt, full locked workspace/all-target tests, Clippy and rustdoc; hosted negative rootless/AppArmor is GREEN. Coverage and branch-coverage generated evidence but failed complete-production admission, and positive SELinux never acquired an eligible runner before cancellation. #6 therefore has current-exact owner-local execution evidence but remains non-GREEN; it must adopt stabilized command/runtime and coverage authority rather than patch that foundation locally.

Focused P0 #89 is the missing-Podman-isolation-evidence owner. Production `80e6fd19ab5ef11eab3d18b3e80b43f880ce6058` removes secure serde defaults only from `ContainerInspection.EffectiveCaps`, `ContainerInspection.BoundingCaps`, and `NetworkInspection.dns_enabled`. Exact `8afa77c61af38121cbfcdc1051e6c45376e13a1b`, CI `35546707858`, has executed: verify is GREEN through full locked tests, Clippy and rustdoc; hosted negative rootless/AppArmor is GREEN; branch instrumentation ran the focused missing-evidence controls before failing whole-head 100% admission; ordinary production coverage failed separately during evidence generation; positive SELinux had no eligible runner and was cancelled. The security repair is therefore execution-backed, but the historical #89 tree is not itself merge/release-ready and must not be copied wholesale into the newer owner ancestry.

Draft #143 is the owner-adapted successor RED on current repository-fitness #112. Current exact `0bf65d831f2130dbf414fef96cfbfa2828aabfdd`, CI `35685623657`, changes only the RED witness and TRACEABILITY; production Rust/API/schema remain unchanged. Review `5273825033` rejected a prior assumption that explicit JSON `null` capability arrays could be treated as secure empty evidence without exact-version real-runtime proof and removed an ephemeral-port TOCTOU from the positive control. Review `5274042147` then found that setting both `EffectiveCaps` and `BoundingCaps` to `null` in one fixture could false-GREEN if only the first parsed field were rejected. The current exact therefore requires absent EffectiveCaps, absent BoundingCaps, absent dns_enabled, `EffectiveCaps: null`, and `BoundingCaps: null` to fail independently as malformed inspection, while explicit empty arrays remain the positive configured-evidence control and reach a deterministic `InvalidPortMapping` boundary. The minimum post-RED GREEN is to require concrete arrays for both capability fields, require concrete `dns_enabled`, remove secure missing-key defaults, and remove generic null-to-empty normalization. Any future explicit-null compatibility exception requires exact supported-version raw inspect evidence plus independent live capability evidence and must be version-scoped/fail-closed elsewhere. Current #143 CI is queued; production must not move before this corrected RED executes for the intended causes.

Command/runtime owner #14 remains exact `73a191c65cfe1dd16447921acc8f96b306583439`. Repository-fitness descendant #112 remains test-only exact `19c450249e1779cd1d22c4837c8154fed91e1faf`, CI `35543654304`, Draft/open, and has now executed. Verify is GREEN through exact checkout, dependency lock, repository policy, CI-evidence contracts, rustfmt, full locked workspace/all-target tests, Clippy, public rustdoc and private rustdoc; hosted negative rootless/AppArmor is GREEN. Production and branch coverage both generated and uploaded exact-head evidence but failed only complete-production admission, while positive SELinux never received an eligible runner and was cancelled. Its physical-source coverage admission remains canonical. Review `5261966999` removed unrelated `network rm --force` assertions from effective-security coverage; review `5262030576` split configured and live capability sources. Because this ancestry still admits absent isolation keys through secure defaults and also has a generic explicit-null normalization helper, #143—not an old-tree source copy—is the next owner-safe semantic repair lane.

Process-lifecycle #125 remains exact `5bced55c107aff2a2cabb84ebc6d980134e7ed07`; runtime tests are GREEN while coverage completeness depends on #112 physical-source parser. After #143 succession and #112 stabilization, adopt parser/command authority ordinary/non-force and reacquire exact evidence rather than duplicating parser/tests.

### Application-service and network lifecycle

Application-service runtime/lifecycle owner #21 remains exact `717edef989d8b6bb7e1f672d91d9258f7942a6b2`. Historical exact CI has GREEN verify/production coverage/branch coverage/hosted negative rootless/AppArmor; positive SELinux did not execute. #21 retains #42's crate-private, non-serializable `ApplicationServiceCleanupAuthority`, so public/deserialized lease evidence cannot reconstruct destructive selectors.

Canonical network successor #127 is now exact `661b495284cedf4b499e1269ee3d42dd7a4da840`, native CI `35667446932`, Draft/open/mergeable. The immediately preceding current-owner exact `82afbd1366aa7076c97470f3626686b614c8d29b` / CI `35636592189` executed and changed the causal classification materially:

- the repaired missing-create-receipt prerequisite passed;
- hosted negative rootless/AppArmor completed GREEN;
- verify, production coverage, and branch coverage reached `podman_application_service_network_acquired_id_owner_red::acquired_network_identity_precedes_create_and_preserves_attachment_red` and exposed the intended production defect: a hostile effective attachment with `NetworkID="foreign"` still produced `Ok(ApplicationServiceLease)` instead of `IsolationVerificationFailed { control_name: "sandbox_network_binding" }`;
- positive SELinux remained unexecuted.

The failure proves that binding `container create --network` to an acquired backend ID was insufficient. Predecessor production did not deserialize `NetworkSettings.Networks[*].NetworkID`, did not compare the exact acquired container's effective attachment with the admitted ID, and later re-resolved the public `qsr-net-*` correlation for P0 network-state evidence.

Production commit `6629fd30fa7c011f5aaff1b19ec1b116db2696bd` is the minimum current repair candidate for that executed RED. It carries the admitted network ID into `verify_effective_isolation()`, deserializes effective `NetworkSettings.Networks[*].NetworkID`, and requires exactly one effective attachment whose ID equals the admitted ID. Missing, different, or additional attachment fails closed under `sandbox_network_binding` before readiness. Later P0 network-state inspection uses the admitted ID rather than re-resolving public correlation. The same repair retains the admitted ID through all post-admission partial cleanup and performs `podman network rm <full-id>` without network-level `--force`.

When first network-ID admission itself fails, the candidate performs no generated-name network removal. This satisfies the immediate no-destructive-authority rule and deliberately leaves durable reconciliation to issue #141. Fixture commits `43df86fc8d2e6493d14c90bf04080a0349eb9b91` and `463e6d9cc465f638f04c3e3cbe0e1b286b246838` add explicit positive effective-attachment evidence to generic application-service and identity-race doubles. `580ff3d70c1da22707a1f172527878fa395a11a3` restores the unchanged fixed-width entropy seam after the source edit. Doctoring `661b495284cedf4b499e1269ee3d42dd7a4da840` records the executed RED, repair boundary, rejected shortcuts, and Podman references. Current CI `35667446932` must execute unchanged; no predecessor GREEN transfers.

Creation-bound identity remains independently unresolved. Podman's CLI `network create` reports the newly created network name, while `RootlessPodmanAdapter::acquire_network_id` still learns immutable identity in a later exact-name inspection. `tests/podman_application_service_network_create_inspect_toctou_red.rs` proves that a same-name replacement between create and first inspect must not be promoted to attachment/destructive authority. Adding more predicates to the later name lookup is insufficient; immutable identity must be bound to the create operation itself or an equivalent atomic backend receipt. Review `5269671494` verified against Podman 6.0.0 exact upstream source that Libpod network-create can return the created full network object including ID/name/internal/DNS state, but target rootless socket/permission/failure semantics and Podman-machine/Colima portability remain prerequisites before production transport adoption.

Provenance/P0 admission also remains distinct. The same created object must prove expected generated name + canonical ID + `internal=true` + DNS disabled before container creation; canonical ID syntax alone is not provenance. Dependent #142 exact `3aa1425be987851a3c151402bd3efe9edf244323` remains downstream of creation-bound identity and provenance admission. The current #127 candidate carries admitted ID into post-start verification, but #142 must later be ordinary/non-force restacked and executed before its historical post-admission rebind RED can be called completely succeeded.

Successful-lease cleanup authority is not yet repaired by the partial-failure candidate. Public `qsr-net-*` remains consumer correlation. Current explicit `terminate_at()` still derives network cleanup authority from the successful lease's public correlation and still uses network-level `--force`. Existing termination REDs therefore remain active: successful lease registration must retain a separate private exact admitted network ID, explicit termination must use exact-ID non-force network removal, and foreign membership must yield `CleanupFailed` without deleting the foreign member.

Issue #141 owns no-admitted-ID recovery. Durable private intent must precede external create; discovery metadata is locator/corroboration only; final authority requires canonical full-ID reacquisition and ID-based corroboration against durable receipt, expected P0 state, and no foreign attachment. Removal is exact-ID/non-force; ambiguity, replacement, collision, in-use, malformed ID, and restart/idempotency states remain explicit evidence rather than broad-prune triggers.

Issue #128 remains downstream of #127. A successful launch must hold the exact acquired container behind a trusted runtime-owned gate until effective isolation is positively attested, then perform exactly one acknowledged release transition bound to that exact container/request before readiness. Arbitrary entrypoint presence is not a trusted hold.

#133 exact `4191ac0051093c30150734f6b4e98c05f36a0ef4` remains a dependent delimiter repair. Preserve its one-literal Podman `--` image-operand delta and restack only after #127 causal classification, ordinary/non-force.

### Lease evidence and applied-image execution integrity

Issue #84 / Draft #85 owns public application-service lease-evidence admission. #85 exact `e0040d748a8076e17f5103a8a238d2fbd878efbf` contains the complete focused delta: closed `ServiceEndpoint`, `IsolationAttestation`, and `ApplicationServiceLease` wire DTOs plus `TryFrom` validation, `tests/application_service_lease_deserialization_red.rs`, and `APPLICATION_SERVICE_LEASE_DESERIALIZATION_TRACEABILITY.md`. CI `34234632733` executed verify, complete production coverage, complete branch coverage, and hosted negative rootless/AppArmor GREEN; positive SELinux did not complete, so this is historical focused evidence rather than a release gate.

Fresh source comparison shows root #1, #21, and current #127 do not contain #85's repair: current application-service ancestry again derives public `Deserialize` directly for all three evidence types. #85 versus #127 diverges from merge base `5c6a44bb2b35eb17d0315d72db242f4488c3c426`; the focused branch is 10 commits ahead and far behind the current owner. Reviews `5263579990` (#127) and `5263598313` (#21) classify this as a successor-completeness defect. After #127 current exact is causally classified, ordinary/non-force adopt/adapt the complete #85 production/test/TRACEABILITY delta while preserving #42 private cleanup authority and current exact-ID lifecycle semantics. A valid deserialized lease remains evidence-only and cannot reconstruct cleanup capability.

Applied-image execution-integrity issue #46 remains separate P0 authority. Historical #19 is stale evidence only. Command/runtime #112 deserializes `ContainerInspection.ImageDigest` and rejects a present mismatch but models the field as optional/defaulted, so missing/null can fail open. More importantly, current application-service #127 does not observe applied `ImageDigest` at all before resource/LSM/network/port admission; a present mismatch is therefore also unchecked on that path. Review `5262989006` records this current-owner finding.

On dependency-safe current application-service ancestry, #46 RED must bind the exact acquired container ID and separate canonical positive, mismatched, missing, null, malformed/wrong-length, upper-case/noncanonical SHA-256, different-algorithm, and image-name/correlation-substitution states. Every hostile state fails before port/readiness/lease publication. Requested digest and `--pull=never` are configuration, not applied-image evidence. The smallest GREEN is trustworthy observation of canonical `sha256:<64 lower-case hexadecimal characters>` on the exact acquired container followed by byte-for-byte equality with the already-validated requested digest. Missing/malformed/noncanonical/other-algorithm evidence must fail closed rather than be normalized or transcoded. OCI/OpenContainers digest semantics, Podman inspection semantics, and NIST SP 800-190 remain the primary traceability basis.

### Artifact-analysis owner stack

Artifact-analysis parent #18 remains exact `703b4b1a047321bb08d1d6cb1de78d01cb350696`. Focused leaves must not copy command/runtime fixes; after #14/#112 stabilizes, foundation adoption is ordinary/non-force through #18 with every valid leaf delta/test/fixture/contract/TRACEABILITY item preserved.

#53 dynamic-attestation is test/TRACEABILITY exact `78f721afbf269a00721c82c155d9686d0309883f`, CI `35509858228`; its inverse RED requires `RuntimeBoundaryViolated { boundary_name: "dynamic_execution_without_runtime_behavior" }` and a direct public-schema execution=true → RuntimeBehavior relation. #61 evidence-record identity remains formatter-only exact `6d49d3fea3df2ab38ca466586dc60a1a66675c5b`, CI `35475300672`, retaining typed `InvalidEvidenceIdentity` controls. #63 policy binding, #70 Analyzer Worker, #76 strict evidence deserialization, and #51/#55/#59 prerequisites remain independent active obligations.

#67 exact `010330580634fc277ae2a18e9504e2d1f8c5d712`, CI `35400068729`, retains the self-verifiability/descriptor-identity RED: materially distinct canonical artifact descriptors must not alias one analysis-job identity. #65 Rust foundation cardinality remains focused GREEN. Public-schema parity #130 remains test/TRACEABILITY exact `ab40d33787c8eb1e01546d51fd3f554d461b59de`, CI `35521761067`; execute both positive controls and all eleven hostile controls before any schema mutation. Only a real missing direct `properties.evidence.allOf` RED authorizes exactly three narrow foundation occurrence branches.

#102 shared-vocabulary owner and #135 focused Gregorian owner have both executed their original causal REDs. #102 exact `71e63d9e24ae0f6fbe7c098c8a0a66e4eac500f7` / CI `35463767954` proved that required vocabulary 1.0.0 declared `x-cwl-rfc3339Profile` without same-version normative semantics and balanced concrete vectors. #135 exact `107ce5356c477f2a5a67392b2ac4e8bb7fe6169f` / CI `35461212064` independently proved the Gregorian runtime/schema publication mismatch. Commit `e840c7274b6725c3a35f187dbe74bc4f0c7259b8` on #102 is an ordinary two-parent, non-force adoption whose second parent is #135 exact, so the complete focused test/TRACEABILITY delta is preserved by ancestry.

Before immutable publication, #102 added normative same-version profile semantics and an initial five-vector candidate at `4bb415341b788711792af2d86b4e6191cb6bdfae`. Review `5263902467` found the first conformance false-GREEN: those vectors did not independently prove the Gregorian 4/100/400 century split, a 30-day month, or case-sensitive upper-case `Z`. Test-only `a026639258eae37729607794a4e46515061847a4` therefore requires runtime controls and intentionally absent publication cases for `1900-02-29` rejection, `2000-02-29` acceptance, `2026-04-31` rejection, and lower-case `z` rejection. Review `5264818953` found two additional independently observable branches still missing from publication: a valid RFC 3339 fractional-second timestamp and the request contract's upper-case `T` separator restriction. Test-only `a5f8650b231918c4239742a045a169d767bace0b` adds runtime controls for accepting `2024-02-29T23:59:59.123456789Z` and rejecting `2024-02-29t23:59:59Z`, while requiring matching publication cases. Docs-only current exact `110e2c1ebe19ae02bba6083e357cbafb1dc9cbf1`, native CI `35581757517`, records the RFC 3339 §5.6/§5.7/Appendix C rationale and causal repair boundary. The intended semantic RED is now: all six runtime controls remain GREEN while the publication half fails specifically because the corresponding six machine-readable vectors are absent. Do not pre-add those vectors or the upper-case-`T` vocabulary prose before that exact executes; if any runtime control fails, repair only that validator branch first. #135 remains open until one exact #102 successor proves the complete inherited witness plus this hardened six-edge surface. #139 may consume only immutable released vocabulary authority, never mutable #102 source.

Issue #136/#137 remains the self-hosted runner network-boundary lane. #137 exact `fdb1f3543044507f9fc4dd4a087afe96a4cc5ca3` owns reusable target-shape evidence; `linux-cluster-ops#326` must supply actual host-side/bridge probe identity and real pre-lease observed denial. Guest loopback, arbitrary private endpoints, or closed-port proxies are not host-boundary proof.

## DDD and ownership boundary

`sandbox_execution` is Core. `application_service` and `artifact_analysis` are Supporting bounded contexts. Podman/gVisor/containerd/Kubernetes/VM are infrastructure adapters. Generated names and serialized receipts are correlation/evidence unless a reviewed versioned contract grants stronger semantics. Requested configuration, backend-applied inspection, runtime-acquired backend identity, and live effective enforcement are separate evidence levels. Destructive cleanup authority must be runtime-acquired, exact, private, and non-serializable.

Core foundation repositories are optional released contract owners, not mandatory source dependencies. Consumers use immutable released contracts/ACLs; mutable branch dependencies, sibling source copies, and cross-service SQL are prohibited. Aggregate transactions stay short; external/runtime I/O never runs while holding a long DB transaction or explicit lock.

## Buyer-visible gaps

| Gap | Current authority | Required next evidence |
| --- | --- | --- |
| Git Flow production-release authority and immutable publication are unresolved. | #140; #10 `bb587667...`; `.github#1552`; protected `develop`; empty Release inventory. | Establish released/versioned production-branch authority, integrate one protected production head normally, verify immutable-release administration, and publish exact source/artifact/package/SBOM/provenance/reproducibility/rollback evidence. |
| Missing Podman isolation-inspection evidence can still be normalized into secure values on current #112 ancestry; the historical #89 repair is execution-backed but requires owner adaptation. | #89 `8afa77c...` / CI `35546707858` executed; #112 `19c450249...` / CI `35543654304` executed; corrected successor RED #143 `0bf65d83...` / CI `35685623657`. | Execute #143 unchanged. Require absent EffectiveCaps/BoundingCaps/dns_enabled plus independent `EffectiveCaps:null` and `BoundingCaps:null` hostile cases to fail closed, while explicit empty arrays reach the deterministic positive downstream boundary. Only then require concrete parser evidence, remove generic null-to-empty normalization, update rustdoc/TRACEABILITY, and prove complete #89 succession on one integrated exact. |
| Command/runtime current exact is semantically verified but 100% production/branch coverage admission remains RED. | #112 `19c450249...` / CI `35543654304`: verify + hosted negative GREEN; coverage/branch admission failed; positive SELinux unexecuted. | After #143 semantic succession, repair only the remaining deterministic owned-production coverage deficits on the integrated owner exact. Preserve independent capability sources and leave network cleanup syntax to #127/#141; do not lower thresholds or exclude hard paths. |
| #125 coverage completeness depends on parser authority absent from its own ancestry. | #125 `5bced55...`; #112 parser. | Stabilize #143/#112, adopt parser ordinary/non-force, reacquire exact evidence. |
| Application-service effective network attachment can diverge from the admitted network identity; the first repair candidate is unexecuted on its current exact. | #127 `661b495...` / CI `35667446932`; executed RED `82afbd1... / 35636592189`; production repair `6629fd3...`. | Execute current exact unchanged. Require foreign/missing/additional effective attachments to fail as `sandbox_network_binding`, post-admission cleanup to use exact ID non-force, and no generated-name deletion before ID admission. |
| #127 can still promote a same-name replacement network because immutable ID is learned only by a later name lookup after creation. | #127 `661b495...`; create→first-inspect RED `d4f05af6...`; Podman 6.0 Libpod atomic create receipt is a verified candidate. | After the current attachment candidate is causally classified, prove the create→first-inspect witness reaches the intended replacement-ID promotion. Then bind immutable ID to creation or an equivalent atomic receipt; later name/P0 predicates alone are insufficient. |
| Current canonical application-service ancestry permits public lease/endpoint/attestation deserialization without invariant-preserving admission. | P0 #84; focused #85 `e0040d748...` / CI `34234632733` historical hosted GREEN; current #127 lacks repair. | After #127 causal classification, ordinary/non-force adopt/adapt all valid #85 production/test/TRACEABILITY delta, preserve #42 private cleanup authority, and run #84 hostile admission + #42 forged-cleanup denial + current lifecycle/network tests on one exact head. |
| Application-service can publish a lease without proving the exact acquired container uses the requested immutable digest. | P0 #46; current #127 does not observe applied digest; #112 has narrower missing/null fail-open. | After #143 integration, #127 classification, and #84 succession, ordinary/non-force adopt #19/#45/#46/#47 evidence; execute exact/mismatch/missing/null/malformed/noncanonical/different-algorithm/name-substitution REDs; then require exact canonical applied SHA-256 before port/readiness/lease. |
| Network lifecycle is not yet one verified creation-bound, exact-ID, foreign-safe authority chain. | #48; #127 `661b495...`; #141; #142; current explicit termination still correlation/force based. | Progress current attachment candidate → creation-bound identity/provenance → #142 replay → durable no-ID recovery → successful-lease private exact authority → foreign-safe non-force termination → #128 hold/attest/release. |
| No-admitted-ID fail-closed behavior can leak orphan networks indefinitely. | P0 #141; current #127 candidate intentionally performs no generated-name cleanup when ID admission fails. | After #127 classification, implement durable bounded reconciliation with full-ID reacquisition/corroboration and exact-ID non-force removal; cover replacement, collision, ambiguity, in-use, already-absent, crash/restart, idempotent retry. |
| Self-hosted LAN/host-service denial can false-GREEN against the wrong endpoint. | #137; `linux-cluster-ops#326`. | Require ops-owned actual host-side/bridge identity and pre-lease observed denial. |
| Dynamic execution boolean can stand alone without RuntimeBehavior evidence. | #53 `78f721a...`. | Execute hardened RED, then add only matching inverse Rust/schema repair while preserving StaticOnly/unavailable/network/credential constraints. |
| Evidence IDs can contradict enclosing analysis job/sequence. | #61 `6d49d3f...`. | Execute typed invalid-identity controls and full gates; do not substitute schema-only cross-instance claims. |
| Analysis-job identity can alias materially distinct canonical artifact descriptors. | #67 `010330580...`. | Execute descriptor/self-verifiability RED; repair only with stable analyzer provenance. |
| Rust foundation cardinality is focused GREEN but public schema parity is unproven. | #65 focused GREEN; #130 `ab40d337...`. | Execute #130 positive + eleven hostile controls; require real missing-`allOf` RED before exactly three narrow schema branches. |
| Focused artifact-analysis leaves remain blocked by stale command/runtime ancestry. | #63/#65/#70/#76; #18 parent; #51/#55/#59 prerequisites. | Stabilize #143/#112, ordinary/non-force adopt foundation through #18, reacquire leaf exact-head evidence. |
| Shared RFC3339 profile publication can false-GREEN without independent century/month/case/fractional-second/separator vectors. | #102 `110e2c1...` / CI `35581757517`; inherited #135; reviews `5263902467`, `5264818953`; #139 consumer. | Execute current exact. Require all six runtime controls to remain GREEN and confirm publication fails only because the six 1900/2000/April-31/lower-case-z/fractional-second/lower-case-t vectors are absent. Then add exactly those six vectors plus explicit upper-case `T` normative wording, reacquire generic + inherited Gregorian gates, verify #135 complete succession, and publish immutable authority before #139 adoption. |
| Positive effective-LSM evidence is absent on release candidates. | Hosted negative evidence exists; dedicated positive lanes repeatedly lack eligible runners. | Obtain exact-candidate positive effective-LSM evidence; never substitute negative fail-closed evidence. |
| Protected integration governance still exposes approval/bypass gaps. | organization ruleset and central governance owners. | Prove an ordinary protected path without administrator bypass, self-approval, direct push, or gate weakening. |
| No immutable consumer authority exists. | Release inventory empty; production branch governance unresolved. | Satisfy owner/security/runtime/governance gates on one protected production candidate, publish immutable source/artifact authority, then bump consumers only to that release. |

## Current bounded action order

1. Execute #143 exact `0bf65d83... / 35685623657` unchanged. Confirm absent EffectiveCaps, absent BoundingCaps, absent dns_enabled, `EffectiveCaps:null`, and `BoundingCaps:null` each fail at the intended malformed-inspection boundary while explicit empty capability arrays reach deterministic `InvalidPortMapping`.
2. Only after that causal RED, apply the minimum #112-owner parser repair: require concrete EffectiveCaps/BoundingCaps arrays and concrete dns_enabled; remove secure missing-key defaults and generic null-to-empty normalization; update rustdoc/TRACEABILITY accordingly; then reacquire one integrated exact-head repository/fmt/full-tests/Clippy/rustdoc/100%-coverage/runtime evidence set. Mark #89 completely succeeded only after every valid production/test/TRACEABILITY delta is proven on that successor. Any explicit-null compatibility exception must be separately proven on exact supported-version real rootless evidence and version-scoped.
3. Execute #127 exact `661b495... / 35667446932`. First require the executed attachment RED to GREEN for the intended reason: foreign/missing/additional effective attachments fail before readiness, positive fixtures carry explicit `NetworkSettings.Networks[*].NetworkID`, post-admission partial cleanup uses exact-ID/non-force network removal, and failed first ID admission performs no generated-name network deletion. Transfer no status from `82afbd...`.
4. If current #127 reaches the create→first-inspect replacement witness next, classify that causal RED. Then select the smallest mechanism binding immutable network identity to the create operation itself or equivalent atomic receipt; do not treat name/prefix/label/age/dangling state or later-name predicates as creation authority. Re-run provenance/P0 admission, attachment, #142 continuity, and no-pre-ID-delete controls on the same successor lineage.
5. After #127 network authority is dependency-safe, ordinary/non-force restack/replay #142 and then repair successful-lease private exact network cleanup authority plus foreign-safe explicit termination. Start #141 only as an ordinary successor after parent classification; keep its durable recovery semantics separate from immediate launch cleanup.
6. After #127 is causally classified, ordinary/non-force adopt/adapt the complete valid #84/#85 lease-evidence delta into current application-service ancestry. Preserve #42 private non-serializable cleanup authority and current exact-ID lifecycle semantics; run #84 hostile wire admission plus #42 forged-cleanup denial on the same exact head.
7. After #143, #127, and #84 succession are dependency-safe, ordinary/non-force adopt/adapt the complete valid #19/#45/#46/#47 evidence onto current application-service ancestry. Remove cleanup command-shape coupling. Execute exact/mismatched/missing/null/malformed/noncanonical/different-algorithm/name-substitution image REDs on the exact acquired container; only then apply minimum fail-closed canonical SHA-256 image binding before port/readiness/lease publication.
8. Restack #133 only after #127 becomes dependency-safe, preserving its delimiter delta and actual parent adoption.
9. #6 already has current-exact verify/hosted-negative GREEN but coverage admissions remain RED. After #143/#112 command-runtime stabilization, ordinary/non-force adopt that foundation into #6 and reacquire its exact coverage/runtime evidence before any #9 restack. #125 likewise waits for stable parser adoption rather than duplicate tests.
10. Execute #137 only with real ops-owned host-boundary identity/evidence.
11. Execute #53, #61, #67, and #130 hardened exact REDs before production/schema GREEN. Preserve #63/#65/#70/#76 and #51/#55/#59 prerequisites; repair stale command/runtime ancestry through #18 only.
12. Execute #102 exact `110e2c1... / 35581757517`. Require the six runtime edge controls—1900 invalid, 2000 valid, April-31 invalid, lower-case-z invalid, fractional-second valid, lower-case-t invalid—to remain GREEN and the publication half to fail specifically because those six vectors are absent. Only after that causal RED, add exactly those six same-version vectors plus explicit upper-case `T` normative wording and reacquire generic vocabulary completeness plus the complete inherited #135 witness on one exact successor. Close #135 only after verified complete succession; #139 may adopt only the resulting immutable released vocabulary authority.
13. Keep #10 Draft/RED until #140 and released central Git Flow governance establish production-branch authority; independently verify immutable-release administration before publication.
14. Keep root #1 Draft until positive effective-LSM, central dependency/security terminal evidence, ordinary protected production integration, governance convergence, and immutable release evidence all exist.
15. Merge/release only from one unchanged dependency-safe protected production candidate satisfying every gate and one immutable source/artifact release receipt.
