# Product and Technical Gap Baseline

Last reviewed on 2026-09-21 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea` and the live owner PR refs below. This file is the repository-wide single-writer current baseline. Predecessor checks never transfer to a moved head, mutable PR heads are not consumer authority, and GitHub `mergeable=true` is not release readiness.

The full historical causal ledger is preserved in [`product-technical-gap-history-2026-09-16.md`](product-technical-gap-history-2026-09-16.md). Historical present-tense statements there are evidence, not current authority.

## Integration root and release authority

Canonical integration root #1 remains exact `a85dc86c00f00354d9ddb9bf7c291c2c1cd40884`, Draft/open, targeting protected `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`. Native CI `35180762143` has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor jobs; positive SELinux received no eligible runner and was cancelled. Central Dependency Review `.github#810` and CodeQL terminal-publication `.github#1929` remain release blockers. Effective organization ruleset `18156473` still exposes `OrganizationAdmin/always` bypass while requiring one approving review; `.github#1340` owns routine-bypass removal and `.github#772/#1351` own solo-maintainer approval-policy convergence. This repository must not exercise that bypass, self-approve, fabricate review, direct-push, or weaken deterministic gates.

Release-evidence owner #10 remains test-only exact `bb587667e049c900f0dec578f083b35ed50f21c7`, CI `35485675824`. Review `5259471263` records the higher-order branch-model contradiction: generic owner `.github#1552` classifies `default_branch=develop` as Git Flow, so protected `develop` is development integration rather than production release authority. Issue #140 owns the product-scoped production-branch and immutable-publication gap. Do not implement #10 as “release from live default branch”; first obtain released/versioned central branch-model authority or an explicitly accepted QSR exception, admit an exact protected production `main`/`master` head, and only then adapt the product-specific release evidence through ordinary/non-force ancestry.

GitHub branch protection and release immutability remain distinct controls. A protected source branch, a verified tag, or `gh release create --verify-tag` is not by itself evidence that the published release/tag/assets are immutable. No package, tag, GitHub Release, SBOM, provenance, reproducibility, rollback, or consumer bump is authorized until one unchanged protected production candidate satisfies every prerequisite below.

## Current owner authority

### Command runtime and repository fitness

Lease-ownership owner #6 is formatting-only exact `b096f4bf1d4ae1a663538127bf1b1048734c0079`, CI `35541704662`. Executed predecessor `854d602744fd6c37696eb12c8e4bd753df639e2a` / CI `35472781891` proved the new Launching conflict, both Terminating retry outcomes, and LeaseMismatch owner-local witnesses GREEN under branch instrumentation; verify then stopped only at rustfmt, while ordinary coverage independently exposed an inherited Podman `rootless_probe` fixture mismatch. #6 must not patch command/runtime fixtures or treat its old pre-union branch count as canonical physical-source admission.

Focused P0 #89 is the authoritative missing-Podman-isolation-evidence repair. Production `80e6fd19ab5ef11eab3d18b3e80b43f880ce6058` removes secure serde defaults only from `ContainerInspection.EffectiveCaps`, `ContainerInspection.BoundingCaps`, and `NetworkInspection.dns_enabled`: absent evidence now fails through the existing malformed-inspection ACL while explicit `[]`, `[]`, and `false` remain distinct accepted observations. Docs-only predecessor `1a17944d4f591511d723354b4f213d065a599c80` / CI `35169293917` executed exact checkout/dependency/repository/coverage-parser/rustfmt/full locked workspace tests GREEN, the four focused missing-evidence controls GREEN, and hosted negative rootless/AppArmor GREEN; verify stopped only at Clippy `field_reassign_with_default` in two test fixtures. Current test-only exact `8afa77c61af38121cbfcdc1051e6c45376e13a1b`, CI `35546707858`, rewrites only those fixture initializers and is awaiting exact-head execution. Production/API/schema semantics are unchanged by the current movement. #14/#112 must adopt this valid production/test/TRACEABILITY delta only through ordinary/non-force ancestry after #89 is dependency-safe; copying the three field changes into descendants is not authorized.

Command/runtime owner #14 remains exact `73a191c65cfe1dd16447921acc8f96b306583439`. Repository-fitness descendant #112 is now current test-only exact `19c450249e1779cd1d22c4837c8154fed91e1faf`, CI `35543654304`, Draft/open/mergeable. Executed predecessor `57da726d5fe3e856ba696252e2f36c2539b30a88` / CI `35469503714` established 515/515 functions, 5233/5241 physical lines, 7168/7223 physical regions, and 725/726 physical branch outcomes under the canonical physical-source union admission; verify separately stopped at two deterministic rustfmt defects, which were repaired without semantic change.

Review `5261966999` found that two #112 effective-security coverage witnesses incorrectly required historical `network rm --force` log shape owned by #127/#141. Test-only `89d57e9f5c3df6d8af16a0aefd48806469212bdc` removes exactly those two cross-owner assertions; #112 no longer dictates network cleanup command shape. Review `5262030576` then found a capability-evidence false GREEN: one fake `process_caps` populated all live capability columns and configured `BoundingCaps` lacked its own hostile case. Current `19c450249...` independently injects configured `EffectiveCaps`/`BoundingCaps` and live `CAPEFF`/`CAPBND`/`CAPINH`/`CAPPRM`/`CAPAMB`; every hostile case must fail exactly as `IsolationVerificationFailed { control_name: "all_capabilities_dropped" }`. Production Rust/API/schema/network behavior is unchanged. Remaining coverage work after current execution is limited to fresh exact evidence for command setup 608/614, readiness line/branch 1888/1887, and region-only deficits. Do not weaken the physical coverage contract or fabricate environment failures to cover them.

#89 is now an explicit prerequisite of #14/#112. Secure-default parsing still exists on the current #14/#112 ancestry, so #112 cannot become a complete command/runtime successor merely by reaching 100% coverage on that ancestry. The correct succession is: execute #89 current exact; when dependency-safe, ordinary/non-force adopt its valid delta into #14/#112; preserve #112’s coverage-parser, chronology, capability-source, and owner-boundary deltas; then reacquire one exact-head repository/fmt/full-tests/Clippy/rustdoc/coverage/runtime evidence set. A moved integrated head receives no predecessor GREEN transfer.

Independent process-lifecycle owner #125 remains exact `5bced55c107aff2a2cabb84ebc6d980134e7ed07`. CI `35378675229` has verify/runtime GREEN; its coverage artifact evaluates to complete physical lines/regions/branches only under #112’s canonical parser. After #112 stabilizes, #125 must ordinary/non-force adopt that stable parser rather than duplicate tests or copy parser source ad hoc.

### Application-service and network lifecycle

Canonical application-service runtime/lifecycle owner #21 remains exact `717edef989d8b6bb7e1f672d91d9258f7942a6b2`. Its exact CI has GREEN verify, production coverage, branch coverage, and hosted negative rootless/AppArmor; positive SELinux did not execute, so it is not release authority.

Canonical network successor #127 is exact `ba37ca70ca1dba08c87e2bdafebbda46cdf6fa22`, CI `35534547447`. Executed predecessor `d2c22a423870e5566b198587e285ecbf66ea0526` reached the identity-race fixture and exposed missing backend network-ID inspection. Test-only `4053fc1a245a2a49b619f4d26db15cd07bc98087` repaired that prerequisite with distinct canonical backend IDs. Review `5261603733` then found the identity witness still duplicated the cleanup owner by requiring `network rm --force <public qsr-net correlation>`; test-only `0daa56eff16dc582332c04cc01bb05d736d9befa` removes that assertion and instead requires both independent launches to bind distinct canonical acquired network selectors while retaining distinct public correlations. Current production cleanup semantics remain intentionally owned by the dedicated provenance/partial-cleanup/termination RED chain.

Issue #141 owns the post-failure no-admitted-ID recovery obligation. The launch path must remain non-destructive if network creation succeeds but canonical backend-ID admission fails; public names, prefixes, labels, age, or dangling state are locators, not destructive authority. Permanent silent orphaning is nevertheless an operability/capacity defect. The successor contract therefore requires a private durable recovery receipt, bounded candidate discovery, canonical full-ID reacquisition, receipt/provenance/P0-state/no-foreign-attachment corroboration, then exact-ID non-force removal. Broad prune, name-only/label-only deletion, re-resolved-name deletion, or network-level `--force` remain prohibited. No DB transaction or explicit lock may be held across Podman I/O or long-running reconciliation.

Issue #128 remains downstream of #127. Successful application-service launch must hold the exact acquired container behind a trusted runtime-owned gate until effective isolation is positively attested, then perform exactly one release transition with acknowledgement bound to that exact container/request before readiness. Arbitrary `--entrypoint` presence is not a trusted hold.

#133 exact `4191ac0051093c30150734f6b4e98c05f36a0ef4` remains 13 commits ahead / 8 behind current #127 from merge base `71063882948daf5819839d8059c37857a6255abf`. Its one-literal Podman `--` image-operand repair must be preserved, but restack waits for #127 causal classification and must be ordinary/non-force.

Issue #136/#137 remains the self-hosted runner network-boundary lane. #137 exact `fdb1f3543044507f9fc4dd4a087afe96a4cc5ca3` owns reusable target-shape evidence, while `linux-cluster-ops#326` must publish the actual s1 host-side/bridge probe identity and real pre-lease observed-denial evidence. QSR must not invent an arbitrary private endpoint, guest-loopback substitute, or closed-port proxy for real host-boundary denial.

### Artifact-analysis owner stack

Artifact-analysis parent #18 remains exact `703b4b1a047321bb08d1d6cb1de78d01cb350696`. Focused artifact-analysis leaves must not copy command/runtime fixes; after #14/#112 stabilizes, foundation adoption is ordinary/non-force through #18 with every valid leaf delta/test/fixture/contract/TRACEABILITY item preserved.

#53 dynamic-attestation remains test/TRACEABILITY-only exact `78f721afbf269a00721c82c155d9686d0309883f`, CI `35509858228`. Its hardened inverse RED requires exact `RuntimeBoundaryViolated { boundary_name: "dynamic_execution_without_runtime_behavior" }` and a direct public-schema execution=true → RuntimeBehavior relation without hidden sibling predicates. Production inverse GREEN remains blocked until that exact executes causally.

#61 evidence-record identity remains formatter-only exact `6d49d3fea3df2ab38ca466586dc60a1a66675c5b`, CI `35475300672`, pending independent execution of the typed `InvalidEvidenceIdentity` controls. The valid delta must continue to enforce evidence identity against enclosing job/sequence rather than relying on JSON Schema cross-instance equivalence.

#63 policy-boundary binding exact `b3204f8e1c694c18489b41a1c2a1591117e6e58f`, #70 Analyzer Worker exact `991ecccabcf23380443d86873cf2777967112fbf`, and #76 strict evidence deserialization exact `60d19a531c06f035df5291948382de7ee10484c6` retain focused GREEN semantics; their broader failures belong to inherited command/runtime ancestry and must not be leaf-patched. #51/#55/#59 remain independent result/provenance/subject-binding prerequisites.

#67 exact `010330580634fc277ae2a18e9504e2d1f8c5d712`, CI `35400068729`, retains the self-verifiability and descriptor-identity RED: materially distinct canonical artifact descriptors must not alias the same analysis-job identity. That problem remains active even though the broader artifact-analysis stack is waiting on command/runtime ancestry.

#65 Rust foundation-cardinality exact `3bb72c6cdbbc07823ba1c5e59f6a7b3528b29591` retains owner-local focused GREEN. Its whole-head verify/coverage failures are inherited command-runtime fixture/ENTRYPOINT ancestry and do not invalidate the focused invariant or authorize leaf repair. Public-schema parity remains #130 test/TRACEABILITY-only exact `ab40d33787c8eb1e01546d51fd3f554d461b59de`, CI `35521761067`. #130 must execute both positive controls and all eleven hostile controls, including complete EvidenceRecord preservation and whole-schema normalized digest guards, before any public schema mutation. Only after the real checked-in schema fails for missing direct `properties.evidence.allOf` may exactly three narrow foundation occurrence branches be added; every other public EvidenceBundle semantic must remain equivalent under the normalized guard.

#102 exact `71e63d9e24ae0f6fbe7c098c8a0a66e4eac500f7` owns the required CWL byte-vocabulary publication and immutable version semantics. #135 exact `107ce5356c477f2a5a67392b2ac4e8bb7fe6169f` owns Gregorian profile semantics. #139 exact `ab42260cda53dc4ce02bae4fb56b492a81c99456` is an application-service consumer RED and may consume only immutable released vocabulary authority. Never release a vocabulary URI/version and mutate its required keyword semantics later.

## DDD and ownership boundary

`sandbox_execution` is Core. `application_service` and `artifact_analysis` are Supporting bounded contexts. Podman/gVisor/containerd/Kubernetes/VM are infrastructure adapters. Generated names and serialized receipts are correlation/evidence unless a reviewed versioned contract grants stronger semantics. Destructive cleanup authority must be runtime-acquired, exact, private, and non-serializable. Requested configuration, backend-applied inspection, acquired backend identity, and live effective enforcement are separate evidence levels.

Core foundation repositories are optional released contract owners rather than mandatory source dependencies. Consumers use immutable released contracts/ACLs; mutable branch dependencies, sibling source copies, and cross-service SQL are prohibited. Aggregate transactions stay short; external/runtime I/O never runs while holding a long DB transaction or explicit lock.

## Buyer-visible gaps

| Gap | Current authority | Required next evidence |
| --- | --- | --- |
| Git Flow production-release authority and immutable publication are unresolved. | #140; #10 `bb587667...`; `.github#1552`; protected `develop`; production `main` not admitted as current protected release authority. | Obtain released/versioned branch-model authority or explicit exception, admit one protected production head, integrate normally, verify immutable-release administration, then publish one exact source/artifact set with package/SBOM/provenance/reproducibility/rollback. |
| Missing Podman isolation-inspection fields can be normalized into secure values on current command/runtime ancestry unless #89 is adopted. | #89 current `8afa77c...` / CI `35546707858`; production repair `80e6fd1...`; predecessor `1a17944d...` focused/full-test evidence; #14/#112 remain downstream. | Execute #89 current exact. Then ordinary/non-force adopt its valid production/test/TRACEABILITY delta into #14/#112 and reacquire all exact-head gates. Never source-copy or reintroduce serde secure defaults. |
| Command/runtime coverage witnesses can false-GREEN through cross-owner network cleanup assertions or correlated capability evidence. | #112 current `19c450249...` / CI `35543654304`; reviews `5261966999`, `5262030576`; repairs `89d57e9...`, `19c450249...`. | Execute current exact, then after #89 succession run one integrated exact head. Preserve independent configured/live capability sources and leave network cleanup shape to #127/#141. Close only fresh physical-source deficits 608/614/1888/1887 and region-only gaps. |
| #125 coverage is parser-only incomplete on its own ancestry. | #125 `5bced55...`; #112 canonical parser. | Stabilize #112, then ordinary/non-force adopt parser authority and reacquire exact-head evidence; no duplicate behavioral tests or ad-hoc parser copy. |
| Network identity, cleanup, attachment, termination, and pre-attestation hold are not yet one verified exact-ID lifecycle. | #127 `ba37ca7...` / CI `35534547447`; #128; reviews `5261441524`, `5261603733`. | Execute #127 current exact, then progress provenance → no pre-ID deletion → exact-ID non-force cleanup → exact effective attachment → private authority → foreign-safe termination → positive isolation attest → one-time release/ack → readiness. |
| No-admitted-ID fail-closed behavior can leak orphan networks indefinitely. | P0 #141; #127 prerequisite. | After #127 classification, implement durable bounded reconciliation with full-ID reacquisition/corroboration and exact-ID non-force removal; cover replacement, collision, ambiguity, in-use, already-absent, crash/restart, and idempotent retry. |
| Self-hosted LAN/host-service denial can false-GREEN against the wrong endpoint. | #137 `fdb1f35...`; `linux-cluster-ops#326`. | Require ops-owned actual host-side/bridge identity and pre-lease observed denial before workflow GREEN. |
| Dynamic execution boolean can stand alone without RuntimeBehavior evidence. | #53 `78f721a...` / CI `35509858228`. | Execute exact hardened RED, then add only the matching inverse Rust/schema repair while preserving existing StaticOnly/unavailable/network/credential constraints. |
| Evidence IDs can contradict their enclosing analysis job or sequence. | #61 `6d49d3f...` / CI `35475300672`. | Execute the formatter-clean exact through typed `InvalidEvidenceIdentity` controls and full gates; do not transfer predecessor status or substitute schema-only cross-instance claims. |
| Analysis-job identity can alias materially distinct canonical artifact descriptors. | #67 `010330580...` / CI `35400068729`. | Execute the descriptor/self-verifiability RED and repair only after stable analyzer provenance can participate in an independently verifiable derivation. |
| Rust foundation cardinality is focused GREEN but public EvidenceBundle schema parity is still unproven. | #65 focused GREEN; #130 `ab40d337...` / CI `35521761067`. | Execute #130 positive + eleven hostile controls, require real missing-`allOf` RED, then add exactly three narrow direct exact-count branches with no unrelated public-schema drift. |
| Focused artifact-analysis leaves remain blocked by stale command/runtime ancestry. | #63/#65/#70/#76 focused semantics; #18 parent; #51/#55/#59 prerequisites. | Stabilize #14/#112, then ordinary/non-force adopt command/runtime authority through #18 and reacquire leaf exact-head evidence; do not copy parent fixes leaf-by-leaf. |
| Shared byte/Gregorian vocabulary can be semantically incomplete or mutated after release. | #102 `71e63d9...`; #135 `107ce535...`; #139 `ab42260...`. | Execute owner REDs; make one immutable internally complete version or introduce a new version before consumer adoption. |
| Positive effective-LSM evidence is absent on release candidates. | Hosted negative evidence exists; dedicated SELinux lanes repeatedly lack runners. | Obtain exact-candidate positive effective-LSM evidence; never substitute negative fail-closed evidence. |
| Protected integration governance still exposes routine administrator bypass and an approval path not yet converged for solo maintenance. | Ruleset `18156473`; `.github#1340`, `.github#772`, `.github#1351`. | Prove an ordinary protected path without administrator bypass, self-approval, direct push, or gate weakening. |
| No immutable consumer authority exists. | Production-branch governance unresolved; Release inventory last verified empty; immutable-release setting unverified. | Satisfy every owner/security/runtime/governance gate on one protected production candidate, then publish immutable version/package/release and bump consumers only to that released contract. |

## Current bounded action order

1. Execute #89 exact `8afa77c...` / CI `35546707858`. Require repository/fmt/full tests/Clippy/rustdoc and focused missing-evidence controls on this exact. Classify coverage separately; no predecessor promotion.
2. When #89 is dependency-safe, ordinary/non-force adopt its complete valid production/test/TRACEABILITY delta into #14/#112. Preserve #112 `19c450249...` coverage-parser, network-owner-boundary, and independent capability-source work. Reacquire one integrated exact-head CI; no source copy and no force restack.
3. On that integrated command/runtime exact, close only fresh physical-source deficits. Keep network cleanup semantics with #127/#141 and keep coverage thresholds/denominators at 100%.
4. Execute #6 `b096f4b...` and, only after the command/runtime foundation stabilizes, adopt the canonical parser/foundation non-force while preserving lease-owner semantics and coordinator outcomes.
5. Execute #127 `ba37ca7...` / CI `35534547447`. Require distinct acquired network selectors, then progress only along the causal provenance/cleanup/attachment/termination/#128 owner chain. Do not transfer predecessor GREEN.
6. Start #141 implementation only as an ordinary successor after #127 causal classification. Immediate pre-ID no-delete remains authoritative; recovery is a later bounded reconciliation capability, not permission for guessed deletion.
7. Restack #133 only after #127 becomes dependency-safe, preserving its delimiter delta and repairing its TRACEABILITY on the actual parent adoption.
8. Execute #137 and require real ops-owned host-boundary identity/evidence before workflow GREEN.
9. Execute #102/#135/#139 in owner order; never consume mutable sibling source or mutate a released vocabulary version.
10. Execute #53, #61, #67, and #130 hardened exact REDs before their corresponding production/schema GREEN. Preserve focused GREEN semantics on #63/#65/#70/#76 and the #51/#55/#59 prerequisite boundaries while repairing stale command/runtime ancestry only through #18.
11. Keep #125 unchanged until #112 parser/foundation authority stabilizes, then adopt non-force and reacquire exact evidence.
12. Keep #10 Draft/RED until #140 and released central Git Flow governance establish production-branch authority; independently verify immutable-release administration before publication.
13. Keep root #1 Draft until positive effective-LSM, central dependency/security terminal evidence, ordinary protected production integration, governance convergence, and immutable release evidence all exist.
14. Merge/release only from one unchanged dependency-safe protected production candidate satisfying every gate and one immutable source/artifact release receipt.
