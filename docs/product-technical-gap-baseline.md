# Product and Technical Gap Baseline

Last reviewed on 2026-09-16 KST against protected/default `develop@60a85c7633e03b425b67159ec6822c8178cf87ea`, canonical application-service owner #21 exact `65f69de6eb1cf78b316b38424f8c35c316cd0672`, current canonical network-owner successor #127 exact `c896f0a120f2c9c6add80dc7885003d58bda4259`, independent process-lifecycle owner #125 exact `f58dfc6657745b3431502f617c7febad6737efcd`, and command-runtime/repository-fitness owner #112 exact `c8a2986c1201957d98261eeb7585d2ce8b43eb8a`. This ledger distinguishes protected truth, active-PR implementation, checked-in RED evidence, executed causal evidence, focused behavior GREEN, whole-head GREEN, backend-applied configuration evidence, live effective-runtime proof, queued/cancelled checks, and post-integration protected-head evidence. Predecessor evidence never transfers to a moved head.

## Current owner supersession — 2026-09-16 15:05 KST

This section is the current authority and supersedes every older present-tense ordering below while retaining those sections as causal history.

### Canonical network lifecycle and cleanup authority

Network lifecycle work remains on canonical application-service owner ancestry. Draft #127 is an ordinary descendant of #21 exact `65f69de6eb1cf78b316b38424f8c35c316cd0672`; current exact head is `c896f0a120f2c9c6add80dc7885003d58bda4259`.

Current-lineage predecessor exact `323c5f834930ecabfa29831c4901599a5522a90d`, CI `35035358796`, actually executed the acquired-network-ID RED: production created the per-invocation network and went directly to container creation without first inspecting the exact generated name to acquire backend network identity. Formatting-only descendants repaired the independent rustfmt gate. Production `ea280bf6e10b83324dd0d4a6f8a82c213d9b06f7` then added exact-name network inspection, exactly-one-record/canonical lower-case 64-hex Podman `.ID` admission, and ID-bound container `--network` selection with no generated-name fallback. `6708a090d3090fe3e5bc5543c5b87320ce289113` records that repair and `b96fcb990346ff696069cd9cdbcd5c8ccf5405e4` makes TRD code-current. No moved-head GREEN transfers.

Fresh review exposed two different cleanup-authority defects around that identity boundary.

1. **Before exact ID admission:** current `launch_at` creates the network, attempts exact-name identity inspection, and on malformed/unadmittable identity calls `cleanup_network(&plan)`, which resolves the generated `qsr-net-*` name again and executes network-level force removal. Test-only `eda111120e2d11b8ff35c8de187053db2c9d0f2b` adds `tests/podman_application_service_network_identity_failure_cleanup_owner_red.rs`: malformed identity evidence must fail closed before container creation and must issue no `network rm` because no exact destructive network authority has been admitted. The witness deliberately does not freeze the eventual public error taxonomy before causal execution.
2. **After exact ID admission:** current later partial-launch cleanup helpers discard the acquired ID and return to generated-name `network rm --force`. Test-only `7980b1ab6417db4cd7be895a874aa22ad1dcb4ee` adds `tests/podman_application_service_network_partial_cleanup_owner_red.rs`: after ID-bound create, a partial failure must retain the admitted ID, use exact non-force network removal, preserve an unrelated object selected by re-resolving the correlation name, and preserve the original launch failure when exact cleanup succeeds.

Docs-only current exact `c896f0a120f2c9c6add80dc7885003d58bda4259` adds `docs/doctoring/PODMAN_NETWORK_IDENTITY_ADMISSION_TRACEABILITY.md`; `e81a27b1e78bdfd1542e6f8be580db1f67210ae8` carries the adjacent `PODMAN_NETWORK_ATTACHMENT_TRACEABILITY.md`. Podman's current primary documentation distinguishes create-output network **name** from inspected `.ID`; container `--network` accepts name or ID; `network rm --force` removes containers using the named network while ordinary removal returns in-use failure. The owner contract therefore treats generated correlation, admitted backend identity, effective attachment proof, and private destructive authority as separate concepts.

The remaining network sequence is now: execute the pre-ID RED → remove correlation-name destruction before exact identity admission → execute/repair the post-ID partial-cleanup RED → verify the exact acquired container has exactly one effective `NetworkSettings.Networks` attachment whose `NetworkID` equals the admitted network ID before port/readiness → keep generated `qsr-net-*` only as public correlation while retaining the admitted network ID separately as non-serializable cleanup authority → explicit termination by exact non-force ID so foreign membership yields `CleanupFailed` without deletion → replay missing `EffectiveCaps`/`BoundingCaps`/`dns_enabled` evidence admission on this owner ancestry and remove false-safe defaults only after its intended RED executes.

Historical #124 exact `6c653d36f16b18af353949e0f0152c8527f48396` executed different/missing/additional effective-attachment failures and historical #89 executed the missing-security-evidence failures. Those results prove defects but do not transfer current-head execution/GREEN. #120/#122/#124/#89 remain open until every valid delta/test/fixture/contract/evidence item is demonstrably succeeded through verified owner work. Historical/conflicted integration probe #126 remains open and is not closure authority for the diverged #124 tree.

Current #127 CI is `35062609345`: positive SELinux `104685933284`, hosted negative rootless/AppArmor `104685933461`, coverage `104685933487`, branch coverage `104685933495`, and verify `104685933498` remain queued with empty step lists and no assigned runner in consecutive fresh sweeps. Therefore `c896f0a...` carries checked-in RED source plus current TRACEABILITY, not executed causal RED or GREEN.

### Independent bounded-command process lifecycle

Issue #74 / Draft #125 remains independent from the network owner. Predecessor `c4933fd3c5c990bcdf9d440b6e465a4ea6f89b61` executed on hosted Ubuntu 24.04 and ran all 16 bounded-command behavior tests GREEN, including descendant-held-pipe deadline enforcement and post-child `OutputLimit` precedence. It then exposed repository-fitness/rustfmt gates. Subsequent ordinary descendants repaired those without changing process semantics.

Predecessor exact `98d1a6d7d604a53b75bfdf7559f770ad0cfee161` later exposed an impossible generic capture-wait branch that prevented exact branch coverage even though the runtime has only finished/deadline/output-limit outcomes. Production `cddf6e9d60a66511537495372f073f25afd31759` narrows the internal state to exhaustive `CaptureWaitOutcome::{Finished, Deadline, OutputLimit}` without coverage exclusion or panic shortcut. Current exact `f58dfc6657745b3431502f617c7febad6737efcd` adds a separate active `Duration::MAX` RED: an unrepresentable monotonic deadline must fail closed without panic, and the repair boundary is non-panicking deadline admission rather than saturation, arbitrary clamp, or production `catch_unwind`. `docs/doctoring/BOUNDED_COMMAND_DEADLINE_TRACEABILITY.md` records that decision.

Current #125 CI `35047105870` remains queued. Production deadline admission is unchanged from `cddf6e9...` until the current exact witness executes; predecessor behavior GREEN remains history only.

### Command-runtime / repository fitness

Draft #112 historical exact `756f5f7fd93891806ffd779718174798764a0e52` executed verify and hosted negative rootless/AppArmor GREEN. Coverage/branch-coverage then reached repository-wide complete-coverage gates: functions were 512/512, branches 718/726, and physical source lines 5203/5213. Application-service deficits on that report remain #21/#127 ownership and must not be source-copied into #112 merely to close the number.

Current test-only descendant #112 exact `c8a2986c1201957d98261eeb7585d2ce8b43eb8a` stages a separate repository-fitness RED. Coverage admission already treats LLVM regions ending on the next line at column 1 as half-open, but `_uncovered_lines` can still report that terminal line as uncovered. The RED requires diagnostic intervals to match admission without changing the coverage denominator and preserves same-line and terminal-column>1 controls. Production coverage policy is unchanged. CI `35058814349` remains queued.

Unresolved #112 review threads about application-service receipt/exact-container-ID cleanup authority, independent capability evidence, and duplicate network-inspection cardinality remain dependency-integration blockers owned by #21/#127, not authorization for sibling source copies.

### Protected integration and release

Protected/default `develop` remains exact `60a85c7633e03b425b67159ec6822c8178cf87ea` and is marked protected. Active organization ruleset `18156473` applies to the default branch and requires one approving review, review-thread resolution, central OpenCode/merge-scheduler/security/Strix/Semgrep/Noema/CodeQL workflows, and prohibits deletion and non-fast-forward updates. Current-user administrative bypass capability is not release evidence and is excluded from the normal integration path.

GitHub Release inventory remains empty. Merge/release requires one unchanged dependency-safe protected exact head satisfying repository validation, rustfmt, full locked workspace/all-target tests, Clippy `-D warnings`, public/private rustdoc `-D warnings`, complete owned-production statement/function/region/branch and edge coverage, qualifying review/thread/security, dedicated positive effective-LSM and applicable real runtime evidence, post-integration protected-head CI, version/CHANGELOG, immutable package/release, SBOM, provenance, reproducibility and rollback.

### Current bounded actions

1. Execute #127 exact `c896f0a120f2c9c6add80dc7885003d58bda4259`; if the pre-ID identity-admission RED reaches its intended cause, apply only the minimum repair that forbids correlation-name destructive cleanup before exact identity admission.
2. On the same #127 lineage, execute/repair the post-ID partial-cleanup witness, then the effective-attachment, public-correlation/private-authority, explicit-termination, and missing-security-evidence witnesses in causal order.
3. Execute #125 exact `f58dfc6657745b3431502f617c7febad6737efcd`; preserve predecessor process behavior GREEN as history and add non-panicking deadline admission only after the current RED executes.
4. Execute #112 exact `c8a2986c1201957d98261eeb7585d2ce8b43eb8a`; if the half-open diagnostic mismatch executes, repair only diagnostic line attribution without shrinking admission coverage.
5. Keep #89/#120/#122/#124/#126 and other prerequisites open until all valid deltas/tests/fixtures/contracts/evidence are mapped to executed verified successor work. Merge/release only from an unchanged dependency-safe protected candidate satisfying every gate above.

## Earlier current-owner snapshot — 2026-09-16 (retained causal history)

This section was current before the supersession above. The older 2026-09-15 sections below it also remain causal history and must not be read as present-tense ordering where they conflict with the top section.

### Canonical network lifecycle and evidence admission

Network lifecycle work remains on canonical application-service owner ancestry. Draft #127 is an ordinary descendant of #21 exact `65f69de6eb1cf78b316b38424f8c35c316cd0672`; current exact head is `323c5f834930ecabfa29831c4901599a5522a90d`. Production Rust/API/schema/runtime behavior is still unchanged on #127. The branch carries owner-local RED contracts for created-network identity, effective attachment, private destructive authority/foreign-safe cleanup, and now fail-closed admission of mandatory Podman isolation evidence.

The effective-attachment defect already has executed causal evidence on historical/noncanonical #124 exact `6c653d36f16b18af353949e0f0152c8527f48396`, CI `34950883231`, verify `104321307774`: after repository/fmt gates and typed backend-spawn regression passed, all three `podman_application_service_network_binding_red` hostile cases reached production and received `Ok(ApplicationServiceLease { ... })` where `sandbox_network_binding` failure was required for different, missing, and additional effective network attachment. Hosted negative rootless/AppArmor was GREEN on the same head. That proves the product defect, but no execution or GREEN transfers to #127.

#127 subsequently hardened the witnesses without changing production. `96d31affabc21b5915e3438f3de8ba1b62ef0e1d` decouples effective-attachment REDs from the acquired-ID prerequisite so they can reach container inspection under both the current correlation-name selector and the intended acquired-ID selector. `7bdb2fe626f0d10cb2560ae1218560f25f8ea46c` fixes the fake Podman inspect model so `HostConfig.NetworkMode` represents the selected reference while `NetworkSettings.Networks` remains keyed by the network name and carries stable `NetworkID`. `8a9790357f48812bbdb8a3de031a2ed27f521973` prevents a future repair from moving the full Podman ID into public lease metadata merely to obtain cleanup authority. `PODMAN_NETWORK_ATTACHMENT_TRACEABILITY.md` and `docs/TRD.md` now distinguish current production from the target contract: current production still creates/binds/cleans by generated `qsr-net-*` name, checks only the network object after start, lacks exact-container exclusive attachment verification, derives private cleanup authority from public metadata, and uses network-level `--force`; the target is exact-name network inspect before container creation → acquired full Podman `.ID` → ID-bound create → exact exclusive effective attachment proof → generated name retained only as public correlation → acquired ID retained privately → exact non-force cleanup.

A separate inherited P0 evidence-admission defect is now staged on the same canonical lineage. Current production still marks `ContainerInspection.EffectiveCaps`, `ContainerInspection.BoundingCaps`, and `NetworkInspection.dns_enabled` with `#[serde(default)]`; missing backend evidence can therefore become empty capability sets or `false` and be mistaken for affirmative isolation evidence. Historical #89 exact `cac5f37e03b30c8b5f73911e7388aab34e23c50c` already executed causal REDs for all three missing-field cases and established that explicit empty/false values remain admissible. #127 exact `323c5f834930ecabfa29831c4901599a5522a90d` now dependency-safely replays those focused REDs on #21 ancestry in `tests/podman_missing_isolation_evidence_owner_red.rs`; it does not merge or source-copy the diverged #89 tree and does not yet remove the three defaults. The owner-path RED must execute on the current lineage before the minimum production repair is applied.

Current #127 CI is `35035358796`: branch coverage `104603157091`, coverage `104603157311`, positive SELinux `104603157344`, hosted negative rootless/AppArmor `104603157372`, and verify `104603157449` remain queued with `runner_id=0` and no steps. Therefore `323c5f...` is checked-in owner-path RED source, not executed causal RED or GREEN. #120/#122/#124/#89 remain open until their valid deltas/tests/fixtures/contracts/evidence are demonstrably and completely succeeded by verified owner work; the withdrawn `ReadinessTimeout` inference is not execution evidence.

The minimum admissible network GREEN remains: reject missing mandatory isolation evidence as malformed/unavailable rather than defaulting it secure; acquire the exact Podman network `.ID` before container creation; bind `--network` to that ID; verify the exact exclusive effective attachment set on the acquired container before readiness publication; retain `qsr-net-*` only as public correlation metadata; keep exact destructive network authority private; remove only exact owned resources; and use non-force network cleanup so foreign membership fails closed rather than broadening deletion authority.

### Independent bounded-command process lifecycle

Issue #74 / Draft #125 remains independent from the network owner. Predecessor exact `806aecd62a6f764446cdf2b753b7d4bed6cc0999` executed the descendant-held-pipe causal RED. Production repair `2f9da0007465084a65aae7d22d186706bb0f8b2d` gave each Unix invocation its own process group and applied the original deadline to capture completion; review then found post-child overflow could be overwritten by a later capture timeout, so `215abaacacc6f92392a52152bfe096f9d53621ac` preserved that RED and `9a39f9bbe07f3bdf00171e4e9926d01cd00c42f7` preserved `OutputLimit` while terminating only the owned process group.

Predecessor exact `c4933fd3c5c990bcdf9d440b6e465a4ea6f89b61` later executed on hosted Ubuntu 24.04 in CI `34987717952`: coverage `104444034867` and branch coverage `104444035247` compiled that exact head and ran all 16 bounded-command unit/concrete tests GREEN, including the descendant-held-pipe deadline and post-child overflow precedence cases. Those lanes then failed repository fitness because the test-only concrete-test source was scanned as production; verify stopped at rustfmt. Ordinary descendants repaired only those gates: `3d791dec4e270641913d6ba6716afe5e4ed30f0d` self-declares the concrete-test source `cfg(test)`, `00fc87920a17b97cd7398030070742d0444a2bbe` narrows the production-source scanner without hiding conditional-production code, and current `98d1a6d7d604a53b75bfdf7559f770ad0cfee161` applies the exact rustfmt output. Production runtime semantics remain unchanged from `9a39f9b...`.

Current #125 CI `35024452373` remains queued on all five lanes: branch coverage `104567978936`, coverage `104567979037`, verify `104567979071`, hosted negative rootless/AppArmor `104567979109`, positive SELinux `104567979118`; all still have no runner/steps. The `c4933fd...` behavior result is valid predecessor evidence, not current-head whole-GREEN.

### Command-runtime / repository fitness

Draft #112 repository/DDD lexer repair has current exact `756f5f7fd93891806ffd779718174798764a0e52`. Predecessor exact `606caab42531e447459dcabce1419f75448e1607` acquired real hosted execution in CI `35012155157`. Branch coverage `104526516974` executed the repository/DDD architecture suite and the repaired non-code wildcard lexer behavior was GREEN before a later independent AppArmor fake-backend spawn specimen. Hosted negative rootless/AppArmor `104526517260` was GREEN. The branch-coverage failure therefore does not invalidate the focused DDD repair, but it does block whole-head acceptance.

The same exact `606caab...` then acquired verify `104526517419`. Repository policy and all 25 CI-evidence contract tests passed; `cargo fmt --check` failed deterministically before Test and printed only formatting deltas in `src/lib.rs`, `src/sandbox_execution/mod.rs`, and `tests/ddd_architecture.rs`. Ordinary fast-forward descendant `756f5f7fd93891806ffd779718174798764a0e52` applies exactly that rustfmt output: re-export ordering and line wrapping only. Compare `606caab... → 756f5f7...` is one commit touching those three files with no semantic production/runtime/API/schema change. Its new CI `35038887142` is queued: branch coverage `104614082089`, hosted negative rootless/AppArmor `104614082204`, positive SELinux `104614082231`, coverage `104614082323`, verify `104614082342`.

The predecessor branch-coverage spawn specimen is retained, not guessed away: `podman_apparmor_mode_suffix::bare_apparmor_profile_without_effective_mode_is_not_positive_proof` expected `IsolationVerificationFailed { control_name: "lsm" }` but received bounded `BackendSpawnFailed { operation: "backend_security_info", failure_kind: Other }`; adjacent enforce/complain cases passed. Because the public class does not expose raw errno, no retry/sleep/mutex, inferred errno, or weakened LSM expectation is authorized. Reproduce on current exact head or obtain additional admissible diagnostics before any process-semantic repair.

### Current bounded actions

1. Execute #127 exact `323c5f834930ecabfa29831c4901599a5522a90d`. If the re-adapted missing-evidence owner RED reaches the three intended assertions, remove only the false-safe `#[serde(default)]` behavior for `EffectiveCaps`, `BoundingCaps`, and `dns_enabled`, then reacquire exact-head GREEN before proceeding to network production semantics.
2. On the same #21 lineage, require corrected acquired-ID, effective-attachment, public-correlation/private-authority, and explicit-termination witnesses to reach their intended hostile boundaries. Only then implement the minimum ID-bound create, exclusive attachment verification, private exact authority, and non-force foreign-safe cleanup GREEN.
3. Execute #125 exact `98d1a6d7d604a53b75bfdf7559f770ad0cfee161`; preserve predecessor `c4933fd...` behavior GREEN as history only and repair any current-head gate without changing bounded-command semantics unless a causal runtime failure demands it.
4. Execute #112 exact `756f5f7fd93891806ffd779718174798764a0e52`; verify the deterministic rustfmt gate is closed, preserve the focused DDD evidence, and RCA the AppArmor spawn specimen only if it recurs with sufficient diagnostics.
5. Keep #89/#120/#122/#124/#119 as causal/succession evidence and do not close them until every valid delta/test/fixture/contract/evidence item is mapped to an executed verified successor. Merge/release only from an unchanged dependency-safe protected candidate satisfying review/thread/security, complete owned-production rustdoc and statement/function/region/branch/edge coverage, positive effective isolation, real runtime acceptance, central workflows, and immutable package/SBOM/provenance/reproducibility/rollback evidence.

## Current process-boundary supersession — 2026-09-15

This section supersedes older process-prerequisite statements below while retaining them as causal history. #124 test-only exact `76d6d2ee6423f871967245e299411c4199ff875a` is now an **executed causal RED**, not a source-predicted RED. Native CI `34902839308`, verify job `104172571319`, checked out that exact SHA, passed dependency lock/repository policy/coverage-parser/rustfmt steps, then failed in `cargo test --locked --workspace --all-targets` because `BackendInvocationFailureKind` was not exported (`E0432`) and `ApplicationServiceError::BackendSpawnFailed` did not exist (`E0599`). Hosted negative rootless/AppArmor job `104172571122` was GREEN on the same exact head. Dedicated positive SELinux remained a separate self-hosted lane and is not transferred into this evidence.

After that executed RED, #124 ordinary-descendant production commit `54fb9e33fbafd2b2549d95e4691464749e2dbf63` selectively adapted only canonical #72's typed process boundary into current #119 ancestry: `BoundedCommandError::Spawn(io::ErrorKind)`, `Capture` for missing output pipes, bounded `BackendInvocationFailureKind::{NotFound, PermissionDenied, ResourceExhausted, Other}`, `BackendSpawnFailed`, provider-neutral generic invocation text, and infrastructure ACL/unit coverage preserving Timeout/OutputLimit/Wait/Capture distinctions. It did not alter network lifecycle production semantics. CodeRabbit then identified one valid compatibility finding: the existing `tests/podman_application_service.rs::missing_or_non_rootless_backend_fails_before_isolation_resources_are_created` test still expected generic `BackendInvocationFailed` for the same missing executable. Ordinary descendant `7b750f9ca3f992f94c1d79476bbf887c52ed7c17` migrates only that stale expectation to `BackendSpawnFailed { failure_kind: NotFound }`; the real command-failure/non-rootless controls remain unchanged and Wait/Capture remain generic post-spawn failures. Current exact #124 head `4745358366d8d9a3193ea5f1c665d1f6d46071c8` adds code-current traceability for that review repair. Native CI `34924780198` has five jobs materialized, but verify `104240413370`, coverage `104240413441`, branch coverage `104240413154`, hosted negative rootless/AppArmor `104240413315`, and positive SELinux `104240413305` are all pre-runner with `runner_id=0` and no steps. Current-head CodeRabbit re-review is temporarily rate-limited and has produced no review thread or formal review. Therefore the process repair is **implemented and review-repaired but not yet exact-head GREEN**.

No network production mutation is authorized until #124 current exact head becomes GREEN under the applicable exact-head gates. After that, replay the unchanged #119 inherited attachment/foreign-safe-cleanup/acquired-identity witnesses, then execute #120's explicit-termination foreign-member RED and #122's acquired-ID attachment RED. Only those executed causal layers authorize the minimum network lifecycle GREEN. Retry, sleep, extra mutex, fixture weakening, inferred errno, force push, destructive rebase, old-tree wholesale merge, predecessor-GREEN transfer, or correlation-name destructive authority remain prohibited.

## Current authority supersession — 2026-09-15

This section supersedes older present-tense statements below while retaining them as causal history. The repository boundary is unchanged: `sandbox_execution` is Core; `artifact_analysis` and `application_service` are Supporting bounded contexts; Podman and other runtimes remain infrastructure adapters. Public correlation data is not destructive runtime authority.

The current P0 network-lifecycle chain is #23 → #119 → #120 → #122 → #118 → #117. #23 exact `95f9e2c73b03a8b6a38f8accbb363adfea56b723` carries the #22/#23 attachment, #41 foreign-safe cleanup, and #48 acquired-network-identity RED contracts. #119 exact `ccfbcd4ed4e70f95b547b4fb3bc76a8276ca0efd` intentionally keeps production and inherited hostile fixtures unchanged. Native CI `34849935231` has now partially executed: hosted negative rootless/AppArmor is GREEN, while `verify` reached `cargo test --locked --workspace --all-targets` and failed earlier in `podman_application_service::malformed_port_mappings_fail_closed_after_cleanup`, where the fixture expected `InvalidPortMapping` but received `BackendInvocationFailed { operation: "rootless_probe" }`. Coverage and branch-coverage also failed during broad evidence generation; dedicated positive SELinux remains queued. Because the broad run stopped before the network-owner binaries established attachment, foreign-member, or acquired-ID causes, this is executed process-boundary evidence but **not** a causal network RED.

That `rootless_probe` recurrence belongs to the existing backend process-invocation taxonomy owner #71/#72 rather than to the network bounded slice. Draft #72 exact `900d0273625115f7bcc602d888baadded0caeb4f` owns the bounded `Spawn(io::ErrorKind)` → `BackendSpawnFailed { failure_kind }` discriminator while keeping Wait/Capture/Timeout/OutputLimit distinct. #72 had been closed without merge despite a still-valid owner delta and is now reopened. Direct ancestry-preserving probe #123 places #72 over #119 and is Draft/conflicted; this proves wholesale owner merging is not a safe integration path. The newer supersession above records that #124 has now executed the causal RED, selectively applied the minimum typed process-boundary contract onto current network ancestry, and repaired the valid stale-expectation finding. No force rebase, retry, sleep, mutex serialization, fixture weakening, or inferred errno is authorized.

Review of #119 identified a separate explicit-termination gap: the partial-launch #41 witness does not exercise `RootlessPodmanAdapter::terminate_at`, which independently performs network cleanup. #120 therefore adds an independent successful-launch → foreign-member → `terminate_at` hostile witness. Review found the first fake backend weakened causality by making every non-force removal fail even without a foreign member; exact #120 head `14803233e9e763fa60d3d4ade9887c0ba45be7ac` repairs that fixture so normal cleanup succeeds without the marker and only an actually attached foreign member makes non-force removal fail. The review thread is resolved. Native CI `34878903685` remains pre-runner: hosted verify, coverage, branch coverage, and negative rootless/AppArmor jobs still have `runner_id=0` and no steps; the positive SELinux job is separately unassigned on `[self-hosted, linux, cwl-hostile-workload, selinux]`. The #120 witness is therefore checked-in RED, not executed causal RED.

Draft #122 exact `113122281ea67a3c7dedd6158d08cf0a15ce3b3b` is a test-only descendant of #120. It stages the acquired-ID-aware attachment mismatch contract without changing production: create the expected deny-by-default `qsr-net-*` network, inspect **that same created name** to acquire the full Podman network `.ID` before container creation, bind `--network` to that ID, preserve `sandbox_network_binding` as the hostile mismatch cause, clean the exact acquired network without network-level `--force`, and never reach readiness after mismatch. CodeRabbit found that the first fixture accepted an arbitrary inspect selector; exact repair `113122...` rejects unrelated selectors and asserts network-create < identity-inspect < container-create ordering. Native CI `34898243362` did execute the hosted lanes: verify `104157606644` failed at rustfmt before Test, coverage `104157606365` and branch coverage `104157606554` ran and failed during evidence generation, and hosted negative rootless/AppArmor `104157606591` ran and passed. Positive SELinux `104157606664` remains queued without a runner. Because the hostile attachment assertion never executed, #122 remains staged source evidence rather than an executed causal network RED; no predecessor status transfers.

Current production on the network baseline still creates the Podman network by generated `qsr-net-*` name, binds container `--network` to that re-resolvable name, publishes the same name as `RuntimeLeaseMetadata.network_id`, and explicit termination executes `podman network rm --force <lease.network_id>`. The minimum admissible network GREEN must instead acquire the created Podman network `.ID` before container creation, bind `--network` to that acquired identity, verify the effective exclusive attachment set before readiness publication, retain generated `qsr-net-*` only as public correlation metadata, carry exact destructive network authority privately, and remove the owned network without network-level force deletion. If a foreign attachment makes deletion unsafe, cleanup must fail closed as `CleanupFailed` while preserving the foreign resource.

The process-invocation discriminator has now been implemented on #124 only after an executed causal RED and its first valid review finding has been repaired, but current exact `4745358...` must still become GREEN before any network production mutation. Then the unchanged network-owner deltas must reacquire exact causal execution: #119's inherited attachment/cleanup/identity witnesses, #120's explicit-termination foreign-member witness, and #122's acquired-ID-aware attachment mismatch witness. Only after those RED layers exist may the minimum network production repair be applied and validated on one unchanged exact head through repository validation, rustfmt, full locked workspace/all-target tests, Clippy `-D warnings`, public/private rustdoc `-D warnings`, complete owned-production statement/function/region/branch and edge coverage, and real runtime evidence where the claim concerns effective confinement.

#118 exact `ddf3981b2a0ed312316228d935231699a9316b6c` remains blocked on that canonical network owner. Its earlier one-shot application-service candidate correctly advanced several container-lifecycle and public-receipt/private-authority concerns but still derived destructive network authority from generated correlation metadata and used network-level force removal; it must not be published unchanged. #117 remains the final ordinary, non-force ancestry reconciliation that must make canonical #21 exact `65f69de6eb1cf78b316b38424f8c35c316cd0672` an actual ancestor after #118 is repaired and stable.

Protected/default `develop` remains exact `60a85c7633e03b425b67159ec6822c8178cf87ea`. Organization ruleset `18156473` is active on the default branch and requires one approving review, review-thread resolution, and the central required workflows from `ContextualWisdomLab/.github@main`; non-fast-forward updates and branch deletion are prohibited. Current-user bypass capability is not release authority and is not part of the normal integration path.

The organization queue-health owner is also incomplete rather than a reason to churn leaf heads. `.github` Draft #2204 exact `2d38aea791a26e1de9804f784478d50353f8e9c2` adds `ContextualWisdomLab/quarantine-sandbox-runtime` to the bounded read-only queue-health allowlist and contract test. Its parent #2202 has same-head evidence that a hosted job can remain positively unassigned for 4h 50m 26s and later acquire a runner and succeed without source mutation. Accordingly, no no-op commit, rerun storm, selector change, cancellation, synthetic success, or predecessor-status transfer is authorized merely to wake product lanes.

## Product responsibility

Quarantine Sandbox Runtime owns reusable sandbox execution, isolation-policy enforcement, resource bounds, lease/readiness/cleanup attestation, and artifact-analysis evidence. `sandbox_execution` is the Core bounded context; `artifact_analysis` and `application_service` are Supporting contexts. Podman/gVisor/containerd/Kubernetes/VM remain infrastructure adapters. Wardnet retains verdict/incident/quarantine authority. Agent/chat consumers retain task/tool/application authorization, identity, secrets, and user-action authority.

## DDD and repository structure

| Gap | Current evidence | Status | Required action |
| --- | --- | --- | --- |
| Artifact analysis had been flattened under root modules. | PR #1 keeps implementation under `src/artifact_analysis/` and repository fitness tests forbid obsolete root paths. | Corrected on active PR | Preserve through protected integration. |
| Podman infrastructure had leaked into Core and Supporting application-service types. | PR #1 keeps Podman under `src/infrastructure/`, backend-neutral Core errors/contracts, and composition-boundary application translation. | Corrected on active PR | Keep dependency direction and unique-ADR fitness tests GREEN. |
| Pre-publication ADR identities conflicted. | Canonical ADR line is `0001`–`0006`; ADR-0006 remains Proposed while the runtime decision is unmerged. | Corrected on active PR | Promote only after protected integration and then-current runtime evidence. |
| Admission, session lifecycle, recovery, and network/egress still meet inside process-local application-service coordination. | Issue #8 defines the intended bounded-context extraction and forbids durable/distributed responsibilities from accumulating in generic application-service internals. | Known structural gap | Extract only with executable contracts and compatibility boundaries; no cosmetic folder churn. |
| Repository name remains security-biased relative to application-service isolation responsibility. | Product scope spans hostile artifact analysis and reusable application isolation. | Known naming gap | Re-evaluate before GA through repository-settings owner path with redirects/consumer migration. |

## Application-service isolation

| Capability | Current evidence | Status | Required action |
| --- | --- | --- | --- |
| Immutable application identity | Registry-style image references require SHA-256 digest identity; explicit host-backed/alternate-store `containers/image` transports are rejected and launch uses `--pull=never`. Draft #19 additionally carries #46, which proves the actually applied `ImageDigest` must equal the requested immutable digest before lease publication. | Configured intent corrected; applied-image RED pending | Execute #46 causally, then bind exact applied digest without treating inspect state as full live process proof. |
| Rootless backend | Exact root `24526eb55cf5db48ea07079b314f7d1b676eb48d` executed on GitHub-hosted Ubuntu 24.04 / Podman 4.9.3 and passed the explicit rootless/backend-version contract. | Real hosted evidence obtained | Re-prove every moved exact head; rootless capability does not prove per-sandbox LSM confinement. |
| Read-only / writable surfaces | Launch uses read-only rootfs, `--read-only-tmpfs=false`, bounded `/tmp` with `rw,noexec,nosuid,nodev`, image volumes ignored, no generated hosts file, no proxy inheritance, and no systemd/sdnotify integration. Unsupported Podman 4.9.3 `--no-hostname` has been removed on the root lineage and downstream command contracts keep it absent. | Configured intent plus partial runtime inspection | Bind exact applied tmpfs mount set/options/size, then require live mount proof. |
| Privilege / namespace isolation | Current repair checks empty container Effective/Bounding caps and process Effective/Bounding/Inheritable/Permitted/Ambient sets, no-new-privileges, seccomp, user/PID/IPC namespaces, numeric non-root identity, and enforcing SELinux/AppArmor evidence. Draft #19 now carries #45 for applied UTS/cgroup namespace modes. Exact hosted E2E `24526eb...` failed closed at `lsm`, proving that the ordinary hosted image cannot supply positive effective confinement. | Negative effective-LSM evidence obtained; #45 and positive proof pending | Execute #45 causally, keep inspect configuration distinct from live namespace-handle proof, and retain the dedicated SELinux-capable release lane. |
| Network isolation and ownership | Historical authority: Draft #23 carries #22/#23 exact attachment REDs, #41 network-cleanup ownership, and #48 acquired-network-identity. Current authority is the top #127 supersession. | Current-lineage acquired-ID repair implemented; pre-ID/post-ID cleanup, effective attachment, private authority and termination REDs pending | Execute the top-section owner sequence; never use generated correlation or network-level force as destructive authority. |
| Credential isolation | P0 request has no provider/user credentials, arbitrary environment map, host device/runtime socket, or broad host mount. | Active contract | Any future secret flow requires an explicit purpose-bound broker. |
| CPU/RAM/PID bounds | `HostConfig.Memory`, `NanoCpus`, and `PidsLimit` are inspected against the request. | Backend-applied binding only; live proof incomplete | Verify the exact sandbox's authoritative cgroup-v2 values before release claim. |
| tmpfs / wall time | Launch applies bounded `/tmp` and `--timeout`, but current root does not deserialize/bind `HostConfig.Tmpfs` or `Config.Timeout`; inspect state alone would still not prove live enforcement. Draft #19 preserves hostile REDs for missing/wrong hardening, contradictory/duplicate tmpfs options, widened writable mounts, timeout mismatch, exact cleanup/non-publication, and exact inspect state without live proof. | P0 RED preserved on a root descendant; exact-head execution pending | Execute #19 on current ancestry; after the intended `resource_limits` RED, add only the smallest inspect-binding GREEN, then live cgroup/mount/wall-time proof. |
| Process invocation | Application-service `command` is documented as direct argv/no shell, but current Podman plan appends it after the image and therefore may inherit an image OCI `ENTRYPOINT`. Draft #19 carries #47 test-bearing `115945244d3e7fd2e6a2b04090c16935233a011e`. | P1 direct-argv RED staged; production unchanged | Execute #47 causally; then replace ENTRYPOINT with exact JSON-array argv for non-empty commands and verify effective executable/argv on real Podman before release. |
| Process lifecycle | Historical rows below predate the #21/#118 integration work. Current application-service container authority must remain runtime-acquired and non-serializable where destructive; current network authority is the top #127 owner sequence. | Container owner work partially implemented on descendants; network owner unresolved | Complete #127 owner sequence, ordinary-adopt into #118, then reconcile #21 ancestry through #117 without source copying or force updates. |
| Subprocess spawn pressure | Historical #124 evidence remains causal history; current process-lifecycle authority is the independent #125 top section. | Typed process taxonomy historically repaired; current bounded-command deadline RED pending | Do not use retries/sleeps/mutex masking. Drive the exact current owner and preserve typed failure distinctions. |
| Ownership/idempotency | Draft #6 implements caller-scoped lease ownership/idempotency while retaining canonical root subprocess behavior. | Implemented on descendant | Preserve caller-scoped ownership separately from backend invocation identity; reacquire exact-child evidence after every root movement. |
| Runtime invocation identity | Canonical #21 now contains the application-service runtime/lifecycle owner lineage, including collision-resistant invocation identity, acquired container authority, private non-serializable cleanup authority, and hardened capability/network-inspection evidence. Final integration remains #117 after #118's network prerequisite is repaired. | Implemented on canonical owner lineage; integration pending | Do not duplicate #21 source. Make #21 an actual ancestor of the stable repaired #118 line and reacquire exact-head evidence. |
| gVisor/containerd/Kubernetes | Architecture targets only. | Missing | Add independent adapters after P0 contract stabilizes; public contracts remain backend-neutral. |

## Attestation evidence model

Configured launch intent, backend-applied inspection, acquired backend identity, and live effective enforcement are different evidence levels. A security control may advance only to the level actually proved for the exact sandbox identity. In particular:

- Podman inspect can bind requested configuration but cannot by itself prove kernel cgroup or mount enforcement or that wall-time termination actually occurred.
- A separately inspectable internal network does not prove that the running container is attached only to that network.
- A generated network name does not prove that later name resolution selects the network this invocation created; exact backend identity must be admitted and retained.
- A runtime-owned network does not imply ownership of every container attached to it; network-level force deletion is outside the sandbox cleanup authority.
- A serialized lease or receipt is evidence/correlation data, not sufficient destructive backend authority.
- CPU/RAM/PID effective claims require authoritative cgroup-v2 evidence where the backend exposes it.
- `/tmp` effective claims require live mount evidence showing tmpfs, exact mount point, required restrictions, and bounded size.
- Wall-time effective claims require behavioral/runtime-owned termination and cleanup evidence or an equivalent reviewed watchdog.
- Host AppArmor/SELinux availability is not per-sandbox confinement. Ordinary hosted CI is a negative fail-closed lane; positive LSM evidence remains a separate release gate.

## Artifact analysis

| Capability | Current evidence | Status | Required action |
| --- | --- | --- | --- |
| Static foundation | SHA-256 identity, bounded ingestion, format detection, analyzer interface, deterministic evidence, and failure attribution exist on active foundation. | Active PR | Preserve under `artifact_analysis`; complete exact-head coverage/review. |
| Claude plugin package quarantine profile | Issue #17 defines immutable source/artifact/AppGuardrail identity, fixed probes, credential-free execution, bounded resources, deny-by-default network, cleanup/recovery, and evidence-only receipts. Draft #18 contains contract-first validation/path/probe/resource REDs and remains behind its command-runtime parent. | RED staged, not production truth | Do not overtake root/resource/command P0 blockers; reconcile ancestry non-force before execution/implementation. |
| YARA-X / capa / Ghidra / LIEF | No production adapters yet. | Missing | Add one evidence producer per bounded increment with exact version/digest provenance. |
| Dynamic Linux/Windows detonation | No release-grade detonation worker exists. | Missing | Consume sandbox Core/stronger backend; never execute hostile bytes in control process. |
| Durable evidence/signing | In-memory evidence only. | Missing | Add persistence only after owner/retention/recovery/3NF/idempotency ADR. |

## Command-execution isolation

The one-shot command path is separate Core `sandbox_execution` behavior from readiness-gated Supporting application-service leases. Historical statements that #25 was still the highest unresolved command boundary are superseded by current #14 descendants: the trusted runtime-gate hold/attest/release composition, exact acquired container lifecycle authority, direct ENTRYPOINT repair, chronology ownership, and several cleanup/security repairs exist on that owner stack. They do not transfer into stale artifact-analysis descendants until ordinary ancestry adoption. #35/#43 real cgroup-v2, live `/tmp`, behavioral wall-time/over-lease termination and leak-free cleanup evidence, complete owned-production coverage, positive effective-LSM, qualifying review/security, protected integration, and immutable release evidence remain open gates.

## Verification and release state

- Protected/default `develop` remains exact `60a85c7633e03b425b67159ec6822c8178cf87ea` and is covered by active organization ruleset `18156473`; normal integration requires one approval, review-thread resolution, and the central required workflows. No bypass is release evidence.
- Root #1 exact head is now `5c6a44bb2b35eb17d0315d72db242f4488c3c426`. Native CI `34176680115` has verify, complete production coverage, branch coverage, and hosted negative rootless/AppArmor GREEN; dedicated positive effective-LSM job `101907363137` completed **CANCELLED** without a runner after its bounded queue window, so positive effective-LSM evidence is unavailable on that head. Current central CodeQL/Security evidence on that root is not release-authoritative, so #1 stays Draft.
- Current canonical network owner is the top #127 supersession at exact `c896f0a120f2c9c6add80dc7885003d58bda4259`; CI `35062609345` is queued on all five lanes. Older #119/#120/#122/#124 evidence remains causal history only.
- Current independent process owner is #125 exact `f58dfc6657745b3431502f617c7febad6737efcd`; CI `35047105870` remains queued.
- Current command-runtime/repository-fitness owner is #112 exact `c8a2986c1201957d98261eeb7585d2ce8b43eb8a`; CI `35058814349` remains queued.
- #118 remains blocked on stable canonical network owner completion and must not publish its stale correlation-name cleanup candidate unchanged.
- #117 remains Draft/conflicted until repaired #118 is stable; final completion requires actual #21 ancestry rather than source-equivalent reimplementation.
- `.github` queue-health enrollment remains observability, not a substitute for product CI or security gates.
- The earlier issue #24 protected-branch trigger RED and minimum repair remain historical causal evidence. `develop` is still the protected/default integration authority.
- GitHub Releases remain absent until one exact integrated protected candidate satisfies all release gates. No mutable PR head is consumer authority.

## Protected integration and release authority

PR-head GREEN is necessary but not sufficient for release authority. A normal merge can produce a different protected integration SHA, so the first release requires native CI/security/runtime evidence on the exact protected `develop` head created by integration. Issue #24 therefore treats a missing post-integration CI run as a release-evidence failure, not as equivalent to a successful PR check. Event path filters are part of that contract because a documentation-only integrated SHA is still a new protected identity whose evidence cannot be inherited from its predecessor.

The release-evidence child #10 separately retains the protected-source RED: release preflight must resolve repository authority and prove the release source is the protected/default branch rather than hard-code stale `main` assumptions. Native CI trigger repair and release-source validation are related but distinct gates.

## Consumer and release contract

Wardnet remains SOC/gateway/verdict authority; contextual-orchestrator remains LLM/Agent orchestration authority; Noema remains capability/admission authority. Consumers may use only a future immutable released runtime artifact and versioned contract/ACL. Direct Podman/containerd calls, sibling source imports, mutable PR heads, and cross-service SQL are not integration mechanisms.

The first release remains blocked until one exact integrated protected candidate carries complete owned statement/function/region/branch and edge coverage plus owned-production rustdoc, realistic rootless isolation E2E, positive effective LSM/seccomp/capability/resource/network/cleanup evidence, required review/security/SAST gates, package smoke, SPDX SBOM, provenance, checksum/signature as supported, reproducibility, upgrade/rollback evidence, and an immutable artifact identity.

## Next bounded slices

1. Execute #127 exact `c896f0a120f2c9c6add80dc7885003d58bda4259`; establish the pre-ID cleanup-authority RED, then apply only the minimum repair that forbids generated-correlation destruction before exact identity admission.
2. Execute and repair the post-ID partial-cleanup RED; retain the admitted ID through every later failure path with exact non-force network removal.
3. Reacquire effective-attachment, public-correlation/private-authority, explicit-termination, and missing mandatory isolation-evidence REDs on the same owner ancestry; do not transfer #124/#89 predecessor conclusions as current-head evidence.
4. Drive #125 exact `f58dfc6657745b3431502f617c7febad6737efcd` through its current CI; after causal `Duration::MAX` execution, add only non-panicking deadline admission and retain predecessor behavior GREEN as history.
5. Drive #112 exact `c8a2986c1201957d98261eeb7585d2ce8b43eb8a` through current CI; if the half-open diagnostic RED executes, repair diagnostic attribution without reducing coverage admission.
6. Ordinary-adopt stable canonical network owner ancestry into #118; re-adapt its application-service source-fix and reacquire its own exact-head evidence. Do not copy owner implementation.
7. Reconcile #117 so canonical #21 becomes actual ancestry while preserving #112/#118 command-runtime and DDD work. Resolve conflicts as repair findings rather than discarding valid deltas.
8. In parallel owner lanes, continue #19 resource/namespace/image/argv evidence and #14/#18 dependency-safe command/artifact-analysis stabilization without weakening leaf semantics to mask foundation failures.
9. Merge normally only after one unchanged dependency-safe candidate satisfies review/thread/security, complete owned-production coverage/rustdoc, positive effective isolation, real runtime acceptance, and all required central workflows on the exact integration path.
10. Publish the first immutable runtime release only from the exact integrated protected head, with version/CHANGELOG, package smoke, SBOM, provenance, reproducibility and rollback evidence; only then hand released version/digest pins to consumer owner paths.
