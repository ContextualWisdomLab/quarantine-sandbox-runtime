[package]
name = "quarantine-sandbox-readiness"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Fail-closed loopback readiness evidence for quarantine sandboxes"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

[dev-dependencies]

[[bin]]
name = "qsr-readiness"
path = "src/main.rs"
