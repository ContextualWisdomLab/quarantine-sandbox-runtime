#![forbid(unsafe_code)]
#![deny(missing_docs)]

//! Source-agnostic, credential-free artifact analysis runtime.
//!
//! The first test-first commit intentionally exposes no implementation. The
//! integration tests define the public contract that the next commit must
//! satisfy.
